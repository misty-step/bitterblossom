# Dev/test plane for the powder-chew local drill (bitterblossom-959).
# `dev = true` is what makes `substrate = "local"` legal at all
# (docs/spine.md: "the local substrate is dev/test machinery").
dev = true
db_path = ".bb/plane.db"

[ingress]
bind = "127.0.0.1:7078"

[budget]
max_cost_per_day_usd = 1.0
