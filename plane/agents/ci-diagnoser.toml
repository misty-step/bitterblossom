# CI failure diagnoser: hermetic API-auth reflex agent. It reads failed
# GitHub Actions evidence and writes a bounded fix packet; it never edits.
version = 1
role = "diagnoser"
harness = "pi"
model = "deepseek/deepseek-v4-flash"
secrets = ["OPENROUTER_API_KEY", "GH_TOKEN"]
skills = [
  "harness-kit/diagnose#ci-failure",
  "harness-kit/ci#failure-triage",
  "bitterblossom/operator-min",
]
