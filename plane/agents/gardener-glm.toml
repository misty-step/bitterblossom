# Model-eval gardener candidate: GLM family for ledger-to-ticket judgment.
version = 1
role = "gardener"
harness = "pi"
model = "z-ai/glm-5.1"
secrets = ["OPENROUTER_API_KEY", "GH_TOKEN", "BB_API_TOKEN"]
