# Deprecated Prompts

These files are superseded by `scripts/ralph-prompt-template.md`. Kept for historical reference only.

## Why deprecated

- `ralph-loop-v2.md`: Contains stale patterns (stdout detection instead of file-based, outdated git config, missing Claude Code flags). The canonical template is now `scripts/ralph-prompt-template.md`.
