agent = "bb-builder-rust"
substrate = "sprites"
required_artifacts = ["REPORT.json"]

[workspace]
host = "misty-step/lane-1"

[budget]
timeout_minutes = 90
max_runs_per_day = 5
max_cost_per_run_usd = 2.0
turn_cap = 80

[[trigger]]
kind = "manual"
