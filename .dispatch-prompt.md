Fix all Cerberus Council review failures for PR #136 (proxy provider):

## Critical Fixes Needed

1. **Add proxy package Go tests** (internal/proxy/proxy_test.go):
   - Table-driven tests for StartCommand(), StartEnv(), HealthURL()
   - Test IsRunning() and WaitForHealthy() with mock HTTP

2. **Add proxy JS tests** (internal/proxy/anthropic-proxy.test.mjs):
   - Test translateRequest() with: string system msg, array system msg, tool_use blocks, tool_result blocks, empty/missing fields
   - Test streaming format translation

3. **Fix race condition in proxy process detection** (internal/dispatch/dispatch.go):
   - Replace `ps aux | grep` with PID file approach
   - Proxy writes PID to /tmp/anthropic-proxy.pid on startup
   - Script checks PID file + `kill -0` instead of grep

4. **Use proxy package helpers instead of inline bash** (internal/dispatch/dispatch.go):
   - Refactor buildOneShotScript to use proxy.StartCommand()/StartEnv()
   - Or move shell generation into proxy package: proxy.AppendStartupScript()

5. **Minor fixes:**
   - Fix `tc.index` undefined: use `tc.index ?? 0`
   - Fix typo: `127..0.0.1` → `127.0.0.1` in internal/lifecycle/settings_test.go
   - Add Node.js availability check during provision
   - Log JSON parse errors instead of silent catch

## File Locations
- /home/sprite/bitterblossom/internal/proxy/proxy.go - existing helpers
- /home/sprite/bitterblossom/internal/proxy/anthropic-proxy.mjs - proxy script
- /home/sprite/bitterblossom/internal/dispatch/dispatch.go - dispatch logic with buildOneShotScript
- /home/sprite/bitterblossom/internal/lifecycle/settings_test.go - typo at line 46

## Requirements
- All changes must be backward compatible
- All tests must pass: go test ./...
- Follow existing code style
- Use table-driven tests for Go

Make ALL these changes, then run go test ./... to verify.