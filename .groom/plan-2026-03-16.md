# Grooming Plan — 2026-03-16

## Theme: Simplification Sprint

Direction locked: slash accumulated complexity from three generations of tooling (shell → Go → Elixir) so the existing pipeline works reliably. No new features until the factory is clean.

Factory audit earlier today proved the point: using the deprecated `run-once` command instead of `start` ran only the builder, not the full pipeline. The deprecated command shouldn't exist.

## Current Sprint (now)

| # | Pri | Effort | Title |
|---|-----|--------|-------|
| #701 | P1 | S | Delete run-once and loop CLI commands |
| #675 | P1 | M | Delete builder-result.json artifact protocol |
| #680 | P1 | M | Sprites should serve the repo, not just factory/* branches |
| #702 | P1 | M | Delete dead scripts/ |

Recommended execution order: #701 → #675 → #680 → #702

## Next Up

| # | Pri | Effort | Title |
|---|-----|--------|-------|
| #703 | P2 | M | Delete Go bb CLI — one language, one surface |
| #676 | P2 | S | Conductor re-leases merged issues |
| #681 | P2 | M | Builder runs blocked on external CI |
| #687 | P2 | S | Rename sprites: Weaver, Thorn, Fern, Muse |

## Reduced / Closed / Merged / Demoted

- **Merged:** #685 + #700 → #675 (all artifact protocol problems)
- **Closed:** #698 (Go preflight — contradicts Go elimination), #515 (PR shepherding — implemented)
- **Demoted to BACKLOG.md:** #686 (Muse sprite), #506 (adversarial review)
- **Promoted from BACKLOG.md:** none

## BACKLOG.md Updates

- Archived: PubSub EventBus (#614), cost tracking (#616), OTEL spans (#619), PR shepherding (#515) — all done
- Added: Muse sprite (#686), adversarial review (#506) as High Potential
- Updated prerequisites to reference current issue numbers
- Removed stale #686 from multi-repo prerequisites

## Retro Patterns Applied

- Factory audit is a forcing function: supervised run today exposed `sprite exec --` bug, stale skill references, deprecated entry point confusion
- "Deepen the operator surface before adding orchestration layers" — this sprint deletes layers instead of adding them
- "Code is a liability" — the cut list: ~13 scripts, 2 CLI commands, entire Go surface
