Reading additional input from stdin...
OpenAI Codex v0.137.0
--------
workdir: /Users/phaedrus/Development/harness-kit
model: gpt-5.5
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: medium
reasoning summaries: none
session id: 019eb4a0-c42c-7ab1-ada1-12ed6945d637
--------
user
You are an adversarial code reviewer. Below is a diff against a Rust event-plane service (bitterblossom: webhook/cron ingress -> SQLite ledger -> dispatch agent harness runs onto Fly Sprites). Review ONLY the diff, reading surrounding context from the repo at /Users/phaedrus/Development/bitterblossom as needed (read-only).

The acceptance oracle for this change set:
1. Model/auth policy enforced at config load: claude/codex never run with API keys (ANTHROPIC_API_KEY/OPENAI_API_KEY forbidden as agent secrets; auth="api" rejected on those harnesses); webhook/cron-triggered tasks only bind auth="api" agents (pi via OpenRouter).
2. Hermetic exec for api-auth agents: env_clear + allowlist, workspace-local HOME, only declared secrets cross. Plane env (e.g. OPENAI_API_KEY) must never leak into workloads.
3. Local substrate rejected unless plane.toml dev=true (production = remote substrate only).
4. Webhook trigger filters (pointer + equals/any_of/max, ANDed, fail-closed) drop out-of-scope deliveries with 200 ack and no run row.
5. pi harness adapter: provider/model passthrough, JSONL parsing (message_end/turn_end), message_update flood filtered via sh -c pipeline.
6. Read-only API (/api/runs, /api/runs/<id>, /api/dlq, /api/tasks) + HTML view, bearer-token gated when BB_API_TOKEN set.

Hunt for: security holes (injection via filter values/query params/HTML escaping, auth bypass, secret leakage paths), correctness bugs (state machine, race conditions, fail-open paths), and oracle violations. Severity-tag findings (blocking|serious|minor) with file:line and one-line evidence. Ignore style. Output: findings list then a one-line verdict.

DIFF:
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 17f36ba..7aa10fc 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -6,7 +6,7 @@ on:
   pull_request:
 
 jobs:
-  rust:
+  verify:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v4
@@ -14,9 +14,7 @@ jobs:
         with:
           components: rustfmt, clippy
       - uses: Swatinem/rust-cache@v2
-      - name: Format
-        run: cargo fmt --check
-      - name: Clippy
-        run: cargo clippy --all-targets -- -D warnings
-      - name: Test
-        run: cargo test
+      # The same gate agents run locally: fmt, clippy, tests, plane
+      # config validation, spine LOC budget.
+      - name: Verify
+        run: ./scripts/verify.sh
diff --git a/CLAUDE.md b/CLAUDE.md
index 8bd6079..e506caa 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -19,8 +19,11 @@ One Rust crate, one binary (`bb`), two personalities:
 
 - `bb serve` — webhook ingress, cron scheduler, queue, dispatch.
 - `bb <verb>` — operator/agent CLI on the same core (`run`, `runs`,
-  `dlq`, `task`, `recover`, `check`). Every workflow also runs ad hoc
-  from a terminal with no webhook.
+  `dlq`, `task`, `recover`, `check`). Every workflow also runs as
+  dispatch work from a terminal with no webhook. Reflex work =
+  trigger-fired (webhook/cron), api-auth agents on cheap OpenRouter
+  models, hermetic. Dispatch work = operator-initiated, may use
+  claude/codex on subscription auth. Never Anthropic/OpenAI API keys.
 
 Mode boundary (harness-kit `meta/CONTRACTS.md`): ad-hoc operator sessions
 live in harness-kit (Mode A); recurring event-driven workflows live here
@@ -35,7 +38,7 @@ src/                 The spine (≤ ~5k LOC budget)
   ledger.rs          SQLite run ledger, state machine, leases, dead letters
   ingress.rs         Webhook HMAC + dedupe, cron schedules
   dispatch.rs        Budget check → lease → prepare → execute → collect
-  substrate/         local + sprites adapters (WorkspacePlan seam)
+  substrate/         sprites adapter + dev/test local exec (WorkspacePlan seam)
   harness.rs         claude/codex/pi command building + output parsing
   budget.rs          Enforced vs advisory tiers, named honestly
   recovery.rs        Boot classification of inherited runs (probe, no orphaning)
@@ -47,16 +50,35 @@ backlog.d/           Work source; _done/ is the archive
 docs/                Vision, ADRs, plans, spine contract, archive/
 ```
 
-## Build & Test
+## Verification
+
+The repo gate is one entrypoint, identical locally and in CI
+(`.github/workflows/ci.yml`):
 
 ```bash
-cargo build                                 # binary: target/debug/bb
-cargo fmt --check
-cargo clippy --all-targets -- -D warnings
-cargo test                                  # 37 tests, no network, no tokens
+./scripts/verify.sh    # fmt, clippy, tests, bb check on both planes, LOC budget
 ```
 
-CI (`.github/workflows/ci.yml`) runs exactly those three gates.
+A green gate is necessary, never sufficient. Changes to dispatch,
+substrate, harness, or workload config also need **live evidence** —
+exact recipes, all repeatable:
+
+- **Reflex/dispatch run, end to end**: `export GH_TOKEN=$(gh auth token)`
+  (plus `OPENROUTER_API_KEY` in env), then
+  `bb --config plane run review --payload '{"repo":"o/r","pr":N}'`.
+  Evidence = ledger row (`bb runs show <id>`: state, cost, tokens) AND
+  the externally visible effect (the PR comment).
+- **Containment storm drill**: dev plane + stub harness, N rapid signed
+  webhook deliveries vs a small `max_runs_per_day`; assert FIFO runs,
+  `blocked_budget` rows, parked task, `budget_blocked` notifications.
+  (Worked example: 2026-06-10 drill — 5 events, 3 ran, 2 blocked.)
+- **Read API / HTML QA**: `bb serve` + curl every `/api/*` route and `/`
+  with and without `BB_API_TOKEN`.
+
+Feedback loop after shipping a workload change: run one live review,
+then check `/api/runs` (or `bb runs list --json`) for cost and outcome —
+the ledger is the system of record, OpenRouter usage accounting feeds it
+per attempt.
 
 ## Gotchas (earned by pain)
 
diff --git a/README.md b/README.md
index b324660..aaf6514 100644
--- a/README.md
+++ b/README.md
@@ -2,8 +2,15 @@
 
 The event plane for agent workloads. Define a **task**, bind an **agent**,
 attach a **trigger** — all as files — and the plane runs it durably on a
-Fly Sprite (or a local process) with cost, budget, and trace visible from
-the CLI.
+remote substrate (Fly Sprites today) with cost, budget, and trace visible
+from the CLI.
+
+Two kinds of work, named so we can talk about them:
+
+- **Reflex** work — standing, trigger-fired (webhook/cron). The plane
+  reacts without judgment, on cheap open-weight models, hermetically.
+- **Dispatch** work — deliberate, operator- or agent-initiated from a
+  terminal (`bb run`). May run as the operator on subscription auth.
 
 ```
 plane.toml                  # db path, ingress bind, notify webhook, global budget
@@ -16,7 +23,7 @@ One Rust binary, two personalities:
 
 ```bash
 bb serve                    # the plane: webhook ingress, cron, queue, dispatch
-bb run <task>               # the same workflow, ad hoc from a terminal
+bb run <task>               # the same workflow as dispatch work, from a terminal
 bb runs list --json         # durable ledger: state, agent@version, cost, duration
 bb dlq replay <id>          # dead letters replay as new runs with lineage
 bb task park|unpark <task>  # budget breaches park; unpark is explicit
@@ -32,10 +39,12 @@ cargo build
 ./target/debug/bb --config examples/demo-plane run demo
 ```
 
-`examples/demo-plane/` is a complete commented config. The `local`
-substrate keeps every task terminal-runnable and tests cheap; the
-`sprites` substrate restores checkpoints, syncs repos, and executes the
-harness on a [Fly Sprite](https://sprites.dev) over WebSocket exec.
+`examples/demo-plane/` is a complete commented config. Production
+planes dispatch to a remote substrate only: `sprites` restores
+checkpoints, syncs repos, and executes the harness on a
+[Fly Sprite](https://sprites.dev) over WebSocket exec. The `local`
+substrate is dev/test machinery, rejected unless plane.toml sets
+`dev = true`.
 
 ## Guarantees
 
@@ -50,6 +59,12 @@ harness on a [Fly Sprite](https://sprites.dev) over WebSocket exec.
   per-run cost is advisory — a breach parks the task and notifies.
 - Secrets are resolved per-exec from the plane's environment and travel
   on stdin, never argv, never persisted.
+- Model & auth policy is code, not intent: claude/codex run on the
+  operator's subscription auth only (`ANTHROPIC_API_KEY` /
+  `OPENAI_API_KEY` are rejected as agent secrets), reflex triggers
+  (webhook/cron) bind only `auth = "api"` agents — cheap open-weight
+  models via OpenRouter on open harnesses (pi). Api-auth execs are
+  hermetic: scrubbed env, workspace-local HOME, declared secrets only.
 - The plane holds no judgment: workloads are config, agents own their own
   decomposition, and a workload-specific branch in the spine is wrong by
   definition.
diff --git a/docs/spine.md b/docs/spine.md
index a8e7de9..e175c7a 100644
--- a/docs/spine.md
+++ b/docs/spine.md
@@ -4,7 +4,11 @@ Bitterblossom v3 is one Rust binary, `bb`, with two personalities:
 
 - `bb serve` — the plane: webhook ingress, cron scheduler, queue, dispatch.
 - `bb <verb>` — the operator/agent CLI sharing the same core, so every
-  workflow also runs ad hoc from a terminal with no webhook.
+  workflow also runs as dispatch work from a terminal with no webhook.
+
+Vocabulary: **reflex** work is standing and trigger-fired (webhook/cron);
+**dispatch** work is deliberate, operator- or agent-initiated (`bb run`).
+The model & auth policy below hangs on that distinction.
 
 The product is the config surface. A workload is files, not Rust:
 
@@ -34,20 +38,38 @@ max_cost_per_day_usd = 25.0       # global daily ceiling, enforced pre-dispatch
 
 ```toml
 version = 1                       # bump on any change; recorded on every attempt
-harness = "claude"                # claude | codex | pi
-model = "claude-fable-5"
-bin = "claude"                    # optional: override the harness binary path
+harness = "pi"                    # claude | codex | pi
+model = "moonshotai/kimi-k2.6"
+provider = "openrouter"           # pi only; defaults to "openrouter"
+auth = "api"                      # api | subscription (defaults by harness)
+bin = "pi"                        # optional: override the harness binary path
 args = []                         # optional: extra CLI args appended verbatim
+secrets = ["OPENROUTER_API_KEY"]  # env names resolved per-exec, never persisted
 ```
 
 Swapping a task's agent is a one-line edit to `task.toml`; the ledger
 records which agent name + version produced every attempt.
 
+### Model & auth policy (enforced at load — `bb check` fails, not dispatch)
+
+Two auth classes, two work classes:
+
+- **`subscription`** (claude/codex default): the agent runs *as* the
+  operator on OAuth subscription auth. API keys are forbidden —
+  `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` as agent secrets fail the load, as
+  does `auth = "api"` on those harnesses. Subscription agents bind only
+  to manual-only tasks (**dispatch** work).
+- **`api`** (pi default): cheap open-weight models via OpenRouter. The
+  only class allowed on webhook/cron triggers (**reflex** work). Execs
+  are hermetic: scrubbed environment, workspace-local HOME, declared
+  secrets only — nothing of the operator's identity crosses the exec
+  boundary.
+
 ## tasks/<name>/task.toml
 
 ```toml
 agent = "reviewer"                # agents/reviewer.toml
-substrate = "local"               # local | sprites
+substrate = "sprites"             # remote-only; "local" needs plane dev = true
 
 [workspace]                       # materialized by the substrate preparer
 host = "bb-demo"                  # sprites: the sprite name (host lease key)
@@ -72,10 +94,44 @@ route = "demo"                    # POST /hooks/demo
 secret_env = "BB_HOOK_DEMO"       # HMAC-SHA256 secret env var
 dedupe_key = "header:X-GitHub-Delivery"   # or "json:<pointer>"
 
+# Containment filters (ANDed; fail-closed on missing pointers). An
+# authenticated delivery failing any filter is acknowledged with 200
+# but never becomes a run — scope lives in config, not card prose.
+[[trigger.filter]]
+pointer = "/repository/full_name"
+any_of = ["misty-step/bitterblossom"]
+[[trigger.filter]]
+pointer = "/pull_request/draft"
+equals = false
+[[trigger.filter]]
+pointer = "/pull_request/additions"
+max = 4000
+
 pre_command = ""                  # optional adapter commands run in the
 post_command = ""                 # workspace before/after the agent
 ```
 
+## Observability
+
+The ledger is the system of record; everything reads from it:
+
+- `GET /` — server-rendered HTML operator view (tasks, budgets, parked
+  state, recent runs; auto-refreshes).
+- `GET /api/runs[?task=&state=]`, `GET /api/runs/<id>` (run + attempts +
+  events), `GET /api/dlq`, `GET /api/tasks` — the agent-facing read API,
+  same shapes as the `--json` CLI.
+- Auth: set `BB_API_TOKEN` on the plane and send
+  `Authorization: Bearer <token>` (browsers may use `?token=`). Unset =
+  open, acceptable only on the loopback default bind.
+
+Cost attribution rides OpenRouter's per-response usage accounting
+(`usage.cost` arrives with every pi response — no extra calls), parsed
+per attempt into the ledger. Decision 2026-06-10: no OTel/Langfuse
+sidecar for now — the OTel GenAI semantic conventions are still
+experimental and both add infra the ≤5k LOC spine doesn't need; if
+deeper traces are wanted later, `bb runs export` is the integration
+seam (map attempts onto `gen_ai.*` spans then).
+
 ## Run lifecycle
 
 A durable run row exists in SQLite **before any trigger gets its ack**.
diff --git a/examples/demo-plane/agents/claude.toml b/examples/demo-plane/agents/claude.toml
index 07f3b52..38955d8 100644
--- a/examples/demo-plane/agents/claude.toml
+++ b/examples/demo-plane/agents/claude.toml
@@ -1,4 +1,7 @@
+# A subscription-auth agent: claude on the operator's OAuth. Subscription
+# agents run *as* the operator and bind only to manual-only (dispatch)
+# tasks — the plane rejects them on reflex triggers, and rejects
+# ANTHROPIC_API_KEY / OPENAI_API_KEY as secrets outright.
 version = 1
 harness = "claude"
 model = "claude-fable-5"
-secrets = ["ANTHROPIC_API_KEY"]   # resolved per-exec, never persisted
diff --git a/examples/demo-plane/agents/pi.toml b/examples/demo-plane/agents/pi.toml
new file mode 100644
index 0000000..8a64b67
--- /dev/null
+++ b/examples/demo-plane/agents/pi.toml
@@ -0,0 +1,6 @@
+# An api-auth agent: pi on OpenRouter. The only agent class allowed on
+# reflex (webhook/cron) triggers — hermetic exec, declared secrets only.
+version = 1
+harness = "pi"
+model = "moonshotai/kimi-k2.6"   # provider defaults to "openrouter"
+secrets = ["OPENROUTER_API_KEY"] # resolved per-exec, never persisted
diff --git a/examples/demo-plane/tasks/demo/task.toml b/examples/demo-plane/tasks/demo/task.toml
index dd496c9..77d92da 100644
--- a/examples/demo-plane/tasks/demo/task.toml
+++ b/examples/demo-plane/tasks/demo/task.toml
@@ -1,4 +1,4 @@
-agent = "codex"
+agent = "pi"               # reflex triggers require an api-auth agent
 substrate = "sprites"        # local | sprites
 
 [workspace]
diff --git a/plane/agents/review-coordinator.toml b/plane/agents/review-coordinator.toml
index 49d0cd1..491e6d8 100644
--- a/plane/agents/review-coordinator.toml
+++ b/plane/agents/review-coordinator.toml
@@ -1,5 +1,9 @@
-# Review coordinator: claude headless — native subagent fan-out makes the
-# coordinator-as-filter pattern a single harness run.
-version = 1
-harness = "claude"
-model = "claude-fable-5"
+# Review coordinator: pi via OpenRouter. Reflex work runs hermetic on
+# cheap open-weight models — never subscription auth, never
+# Anthropic/OpenAI API keys (the plane rejects both at `bb check`).
+# Model choice 2026-06: Kimi K2.6 — strongest agentic tool-use of the
+# cheap open-weight cluster (~$0.95/$4.00 per 1M via OpenRouter).
+version = 2
+harness = "pi"
+model = "moonshotai/kimi-k2.6"
+secrets = ["OPENROUTER_API_KEY", "GH_TOKEN"]
diff --git a/plane/tasks/review/card.md b/plane/tasks/review/card.md
index 1ebfc0c..1abfc6e 100644
--- a/plane/tasks/review/card.md
+++ b/plane/tasks/review/card.md
@@ -1,8 +1,8 @@
 # Code review commission
 
-You are the **review coordinator** on the bitterblossom event plane. Your
-job: produce exactly ONE structured review on a pull request. You are
-never the authoring agent.
+You are the **reviewer** on the bitterblossom event plane. Your job:
+produce exactly ONE structured review on a pull request. You are never
+the authoring agent.
 
 ## Input
 
@@ -19,17 +19,17 @@ gh pr view <pr> --repo <repo> --json title,body,additions,deletions,changedFiles
 gh pr diff <pr> --repo <repo>
 ```
 
-## Risk-tiered compute
+## Risk-tiered effort
 
-- **Trivial** (< 10 changed lines): review it yourself. No fan-out.
-- **Standard**: spawn 2–3 parallel subagent reviewers with fresh context.
-- **Large** (> 100 lines or > 20 files): spawn the full bench and read
-  surrounding source for anything uncertain.
+- **Trivial** (< 10 changed lines): one direct pass; verdict and out.
+- **Standard**: run the three passes below in order, then filter.
+- **Large** (> 100 lines or > 20 files): same passes, but read the
+  surrounding source files for anything you are uncertain about before
+  keeping a finding.
 
-## Reviewer lanes (subagents)
+## Review passes (run sequentially, take notes per pass)
 
-Give each reviewer ONLY the diff and its commission — not your reasoning.
-Each lane names what to IGNORE, not just what to find:
+Each pass names what to IGNORE — discipline beats coverage:
 
 1. **Correctness** — logic errors, broken invariants, unhandled failure
    paths, concurrency bugs. IGNORE: style, naming, formatting, docs.
@@ -38,13 +38,13 @@ Each lane names what to IGNORE, not just what to find:
 3. **Simplification** — dead code, needless abstraction, duplicate logic.
    IGNORE: anything requiring product judgment, micro-optimizations.
 
-Reviewers emit structured findings: `severity (blocking|serious|minor)`,
+Findings are structured: `severity (blocking|serious|minor)`,
 `file:line`, one-sentence claim, one-sentence evidence.
 
-## Coordinator filter (your judgment)
+## Filter (your judgment)
 
 - Dedupe overlapping findings; keep the strongest phrasing.
-- Kill nitpicks, speculation, and anything a lane was told to ignore.
+- Kill nitpicks, speculation, and anything a pass was told to ignore.
 - Verify uncertain findings by reading the actual source before keeping
   them.
 - **Bias toward approval**: a finding survives only if you would block or
@@ -61,7 +61,7 @@ gh pr comment <pr> --repo <repo> --body "<review>"
 Review format (markdown): a one-line verdict (`✅ approve-leaning` /
 `⚠️ concerns` / `🛑 blocking`), then findings grouped by severity with
 `file:line`, then a short "reviewed by bitterblossom review factory"
-footer with the run id from the environment if present.
+footer.
 
 Then print, as your final answer, a JSON summary:
 `{"verdict": "...", "blocking": N, "serious": N, "minor": N, "comment_posted": true}`
diff --git a/plane/tasks/review/task.toml b/plane/tasks/review/task.toml
index 0f23398..2685736 100644
--- a/plane/tasks/review/task.toml
+++ b/plane/tasks/review/task.toml
@@ -1,10 +1,13 @@
 agent = "review-coordinator"
-substrate = "local"
+substrate = "sprites"        # workloads never run on the operator's machine
+
+[workspace]
+host = "lane-1"              # pi + gh installed; secrets travel per-exec
 
 [budget]
-timeout_minutes = 15
+timeout_minutes = 30         # K2.6 paces slower than claude; cost is the cap that matters
 max_runs_per_day = 20
-max_cost_per_run_usd = 3.0   # advisory; Cloudflare tokenomics target is $1-2 median
+max_cost_per_run_usd = 0.50  # advisory; measured K2.6 baseline ~$0.03-0.10/review
 
 [[trigger]]
 kind = "manual"              # bb --config plane run review --payload '{"repo":"o/r","pr":N}'
@@ -13,4 +16,22 @@ kind = "manual"              # bb --config plane run review --payload '{"repo":"
 kind = "webhook"             # POST /hooks/review (GitHub pull_request events)
 route = "review"
 secret_env = "BB_HOOK_REVIEW"
-dedupe_key = "json:/pull_request/head/sha"
+dedupe_key = "json:/pull_request/head/sha"   # one run per head SHA, ever
+
+# Containment is mechanism, not card prose: out-of-scope deliveries are
+# acknowledged (so GitHub doesn't retry) but never become runs.
+[[trigger.filter]]
+pointer = "/repository/full_name"
+any_of = ["misty-step/bitterblossom"]        # repo allowlist
+
+[[trigger.filter]]
+pointer = "/action"
+any_of = ["opened", "ready_for_review", "synchronize"]
+
+[[trigger.filter]]
+pointer = "/pull_request/draft"
+equals = false
+
+[[trigger.filter]]
+pointer = "/pull_request/additions"
+max = 4000                                   # oversized PRs don't auto-review
diff --git a/scripts/verify.sh b/scripts/verify.sh
new file mode 100755
index 0000000..c0705b2
--- /dev/null
+++ b/scripts/verify.sh
@@ -0,0 +1,26 @@
+#!/bin/sh
+# The repo gate: everything mechanical that "done" requires, one
+# entrypoint, identical locally and in CI. Live-evidence recipes that a
+# green gate cannot replace live in CLAUDE.md (Verification).
+set -eu
+cd "$(dirname "$0")/.."
+
+echo "==> fmt"
+cargo fmt --check
+
+echo "==> clippy"
+cargo clippy --all-targets -- -D warnings
+
+echo "==> tests"
+cargo test
+
+echo "==> plane configs validate (bb check)"
+cargo run --quiet -- --config plane check
+cargo run --quiet -- --config examples/demo-plane check
+
+echo "==> spine LOC budget (<= 5000; the Python conductor died of bloat)"
+loc=$(find src -name '*.rs' -exec cat {} + | grep -vc '^\s*$')
+echo "    src LOC: $loc"
+[ "$loc" -le 5000 ] || { echo "spine over the 5k LOC budget"; exit 1; }
+
+echo "==> verify: all gates green"
diff --git a/src/dispatch.rs b/src/dispatch.rs
index 510edc3..1087c95 100644
--- a/src/dispatch.rs
+++ b/src/dispatch.rs
@@ -213,6 +213,7 @@ fn attempt_on_host(
         remote_workspace: format!("/home/sprite/bb/{}", task.name),
         checkpoint: task.spec.workspace.checkpoint.clone(),
         secrets,
+        hermetic: matches!(task.agent.auth_class(), Ok(crate::spec::AuthClass::Api)),
     };
     if let Err(e) = session.prepare(&plan) {
         let _ = session.release();
diff --git a/src/harness.rs b/src/harness.rs
index 5cb7ee4..ec72c53 100644
--- a/src/harness.rs
+++ b/src/harness.rs
@@ -47,7 +47,40 @@ pub fn build_command(agent: &AgentSpec, budget: &TaskBudget) -> Result<Vec<Strin
             agent.model.clone(),
             "-".into(),
         ],
-        "pi" => vec![bin, "--mode".into(), "json".into(), "-p".into()],
+        "pi" => {
+            // pi --mode json re-streams the entire partial message on every
+            // token (`message_update`): a 15-minute run produced 718 MB of
+            // stdout (measured live 2026-06-10). Drop those deltas before
+            // they hit the capture file; everything the parser needs
+            // (message_end, turn_end) passes through. The pipeline makes
+            // grep's exit status the command's — pi crashes still surface
+            // because a stream with no assistant message_end fails parsing.
+            let mut inner = vec![
+                bin,
+                "--provider".into(),
+                agent.provider().into(),
+                "--model".into(),
+                agent.model.clone(),
+                "--no-session".into(),
+                "--mode".into(),
+                "json".into(),
+                "-p".into(),
+            ];
+            inner.extend(agent.args.iter().cloned());
+            let quoted: Vec<String> = inner
+                .iter()
+                .map(|a| crate::substrate::local::shell_quote(a))
+                .collect();
+            return Ok(vec![
+                "sh".into(),
+                "-c".into(),
+                format!(
+                    "{} | grep -v -F '\"type\":\"message_update\"'",
+                    quoted.join(" ")
+                ),
+                "sh".into(),
+            ]);
+        }
         other => bail!(
             "unknown harness '{other}' (known: {})",
             HARNESSES.join(", ")
@@ -146,26 +179,62 @@ fn parse_codex(stdout: &str) -> Result<ParsedOutput> {
     })
 }
 
-/// pi `--mode json` emits a final JSON object with `result` and `usage`.
+/// pi `--mode json` emits JSONL events (verified live 2026-06-10 against
+/// pi via OpenRouter): the final assistant message rides on `message_end`
+/// with `message.content[]` text items and `message.usage`
+/// `{input, output, cost: {total}}`; turns close with `turn_end`.
 fn parse_pi(stdout: &str) -> Result<ParsedOutput> {
-    let v = last_json_object(stdout).context("pi output: no JSON object found")?;
-    let result = v
-        .get("result")
-        .or_else(|| v.get("output"))
-        .and_then(Value::as_str)
-        .context("pi output: no 'result' or 'output' string")?
-        .to_string();
-    if result.trim().is_empty() {
-        bail!("pi output: empty result");
+    let mut turns: i64 = 0;
+    let mut last_message: Option<Value> = None;
+    let mut saw_event = false;
+    for line in stdout.lines() {
+        let Ok(v) = serde_json::from_str::<Value>(line) else {
+            continue;
+        };
+        saw_event = true;
+        match v.get("type").and_then(Value::as_str) {
+            Some("turn_end") => turns += 1,
+            Some("message_end") => {
+                if let Some(m) = v.get("message") {
+                    if m.get("role").and_then(Value::as_str) == Some("assistant") {
+                        last_message = Some(m.clone());
+                    }
+                }
+            }
+            _ => {}
+        }
     }
-    let usage = v.get("usage").cloned().unwrap_or(Value::Null);
+    if !saw_event {
+        bail!("pi output: no JSONL events found");
+    }
+    let message = last_message.context("pi output: no assistant message_end — incomplete run")?;
+    let result: String = message
+        .get("content")
+        .and_then(Value::as_array)
+        .map(|items| {
+            items
+                .iter()
+                .filter(|c| c.get("type").and_then(Value::as_str) == Some("text"))
+                .filter_map(|c| c.get("text").and_then(Value::as_str))
+                .collect::<Vec<_>>()
+                .join("\n")
+        })
+        .unwrap_or_default();
+    let result = result.trim().to_string();
+    if result.is_empty() {
+        bail!("pi output: assistant message has no text content");
+    }
+    let usage = message.get("usage").cloned().unwrap_or(Value::Null);
     Ok(ParsedOutput {
         result,
         stats: AttemptStats {
-            tokens_in: usage.get("input_tokens").and_then(Value::as_i64),
-            tokens_out: usage.get("output_tokens").and_then(Value::as_i64),
-            turns: v.get("num_turns").and_then(Value::as_i64),
-            cost_usd: v.get("total_cost_usd").and_then(Value::as_f64),
+            tokens_in: usage.get("input").and_then(Value::as_i64),
+            tokens_out: usage.get("output").and_then(Value::as_i64),
+            turns: Some(turns),
+            cost_usd: usage
+                .get("cost")
+                .and_then(|c| c.get("total"))
+                .and_then(Value::as_f64),
         },
     })
 }
@@ -228,6 +297,50 @@ mod tests {
         assert_eq!(parsed.stats.tokens_out, Some(6598));
     }
 
+    #[test]
+    fn pi_command_carries_provider_and_model() {
+        let agent: crate::spec::AgentSpec =
+            toml::from_str("harness = \"pi\"\nmodel = \"moonshotai/kimi-k2.6\"\n").unwrap();
+        let cmd = build_command(&agent, &crate::spec::TaskBudget::default()).unwrap();
+        let joined = cmd.join(" ");
+        assert!(joined.contains("--provider' 'openrouter"), "{joined}");
+        assert!(joined.contains("moonshotai/kimi-k2.6"), "{joined}");
+        assert!(joined.contains("--no-session"), "{joined}");
+        // The message_update flood filter wraps the invocation.
+        assert_eq!(cmd[0], "sh");
+        assert!(joined.contains("grep -v -F"), "{joined}");
+    }
+
+    #[test]
+    fn parse_pi_jsonl_events() {
+        // Condensed from a live pi run via OpenRouter (2026-06-10).
+        let out = concat!(
+            r#"{"type":"message_update","assistantMessageEvent":{"type":"text_delta"}}"#,
+            "\n",
+            r#"{"type":"message_end","message":{"role":"assistant","content":[{"type":"thinking","thinking":"..."},{"type":"text","text":" BB-PI-OK"}],"usage":{"input":1117,"output":21,"totalTokens":1138,"cost":{"input":0.0007,"output":0.00007,"total":0.000835848}}}}"#,
+            "\n",
+            r#"{"type":"turn_end","message":{"role":"assistant"}}"#,
+            "\n",
+            r#"{"type":"agent_end","messages":[]}"#,
+        );
+        let parsed = parse_output("pi", out).unwrap();
+        assert_eq!(parsed.result, "BB-PI-OK");
+        assert_eq!(parsed.stats.tokens_in, Some(1117));
+        assert_eq!(parsed.stats.tokens_out, Some(21));
+        assert_eq!(parsed.stats.turns, Some(1));
+        assert_eq!(parsed.stats.cost_usd, Some(0.000835848));
+    }
+
+    #[test]
+    fn parse_pi_rejects_incomplete_runs() {
+        assert!(parse_output("pi", "not json").is_err());
+        // Events but no assistant message_end: incomplete.
+        assert!(parse_output("pi", r#"{"type":"message_update"}"#).is_err());
+        // Assistant message with no text content.
+        let no_text = r#"{"type":"message_end","message":{"role":"assistant","content":[{"type":"thinking","thinking":"x"}],"usage":{}}}"#;
+        assert!(parse_output("pi", no_text).is_err());
+    }
+
     #[test]
     fn parse_claude_rejects_empty_and_error() {
         assert!(parse_output("claude", r#"{"type":"result","result":""}"#).is_err());
diff --git a/src/ingress.rs b/src/ingress.rs
index 4f3b298..c837e8b 100644
--- a/src/ingress.rs
+++ b/src/ingress.rs
@@ -51,6 +51,7 @@ pub fn handle_webhook(
     let TriggerSpec::Webhook {
         secret_env,
         dedupe_key,
+        filter,
         ..
     } = trigger
     else {
@@ -73,6 +74,30 @@ pub fn handle_webhook(
         });
     }
 
+    // Containment filters run after authentication: a verified delivery
+    // that fails any condition is acknowledged (2xx, so the sender does
+    // not retry) but never becomes a run. Fail-closed on unparseable
+    // payloads and missing pointers.
+    if !filter.is_empty() {
+        let payload: serde_json::Value = match serde_json::from_str(body) {
+            Ok(v) => v,
+            Err(_) => {
+                return Ok(WebhookResponse {
+                    status: 200,
+                    body: "{\"filtered\":\"payload is not JSON\"}".to_string(),
+                })
+            }
+        };
+        for f in filter {
+            if let Some(reason) = f.reject_reason(&payload) {
+                return Ok(WebhookResponse {
+                    status: 200,
+                    body: serde_json::json!({ "filtered": reason }).to_string(),
+                });
+            }
+        }
+    }
+
     let derived = match dedupe_key {
         Some(expr) => Some(derive_dedupe_key(expr, headers, body)?),
         // No derivation declared: the body hash is the event identity.
diff --git a/src/serve.rs b/src/serve.rs
index 57b9606..900bdcd 100644
--- a/src/serve.rs
+++ b/src/serve.rs
@@ -169,11 +169,16 @@ fn http_loop(root: &Path, plane: &Plane) -> Result<()> {
             Ok(r) => r,
             Err(e) => (500, format!("{{\"error\":\"{e}\"}}")),
         };
+        let content_type: &[u8] = if body.starts_with("<!doctype") {
+            b"text/html; charset=utf-8"
+        } else {
+            b"application/json"
+        };
         let _ = request.respond(
             tiny_http::Response::from_string(body)
                 .with_status_code(status)
                 .with_header(
-                    tiny_http::Header::from_bytes(&b"Content-Type"[..], &b"application/json"[..])
+                    tiny_http::Header::from_bytes(&b"Content-Type"[..], content_type)
                         .expect("static header"),
                 ),
         );
@@ -181,10 +186,70 @@ fn http_loop(root: &Path, plane: &Plane) -> Result<()> {
     Ok(())
 }
 
+/// Bearer-token gate for the read surface. With BB_API_TOKEN unset the
+/// surface is open — acceptable only because the default bind is
+/// loopback; set the token before binding wider.
+fn read_authorized(request: &tiny_http::Request, url: &str) -> bool {
+    let Ok(token) = std::env::var("BB_API_TOKEN") else {
+        return true;
+    };
+    let header_ok = request.headers().iter().any(|h| {
+        h.field
+            .as_str()
+            .as_str()
+            .eq_ignore_ascii_case("authorization")
+            && h.value.as_str() == format!("Bearer {token}")
+    });
+    // ?token= lets a browser reach the HTML view; tokens in URLs are
+    // weaker than headers — fine for a loopback operator page.
+    header_ok || url.contains(&format!("token={token}"))
+}
+
+fn query_param(url: &str, name: &str) -> Option<String> {
+    let (_, query) = url.split_once('?')?;
+    query.split('&').find_map(|kv| {
+        let (k, v) = kv.split_once('=')?;
+        (k == name && !v.is_empty()).then(|| v.to_string())
+    })
+}
+
 fn handle_request(root: &Path, request: &mut tiny_http::Request) -> Result<(u16, String)> {
     let method = request.method().to_string();
     let url = request.url().to_string();
 
+    if method == "GET" && (url.starts_with("/api/") || url == "/" || url.starts_with("/?")) {
+        if !read_authorized(request, &url) {
+            return Ok((401, "{\"error\":\"missing or bad bearer token\"}".into()));
+        }
+        let plane = Plane::load(root)?;
+        let ledger = Ledger::open(&plane.db_path())?;
+        let path = url.split('?').next().unwrap_or(&url);
+        return match path {
+            "/" => html_view(&plane, &ledger).map(|body| (200, body)),
+            "/api/runs" => {
+                let task = query_param(&url, "task");
+                let state = query_param(&url, "state");
+                let runs = ledger.list_runs(task.as_deref(), state.as_deref())?;
+                Ok((200, serde_json::to_string(&runs)?))
+            }
+            "/api/dlq" => Ok((200, serde_json::to_string(&ledger.list_dead_letters()?)?)),
+            "/api/tasks" => Ok((200, serde_json::to_string(&tasks_view(&plane, &ledger)?)?)),
+            _ => {
+                if let Some(id) = path.strip_prefix("/api/runs/") {
+                    let run = ledger.run(id)?;
+                    let body = serde_json::json!({
+                        "run": run,
+                        "attempts": ledger.attempts(id)?,
+                        "events": ledger.events(id)?,
+                    });
+                    Ok((200, body.to_string()))
+                } else {
+                    Ok((404, "{\"error\":\"not found\"}".into()))
+                }
+            }
+        };
+    }
+
     if method == "GET" && url == "/health" {
         let plane = Plane::load(root)?;
         let ledger = Ledger::open(&plane.db_path())?;
@@ -229,3 +294,105 @@ fn handle_request(root: &Path, request: &mut tiny_http::Request) -> Result<(u16,
 
     Ok((404, "{\"error\":\"not found\"}".to_string()))
 }
+
+/// Per-task posture: agent binding, parked state, today's runs, budgets.
+fn tasks_view(plane: &Plane, ledger: &Ledger) -> Result<Vec<serde_json::Value>> {
+    let mut out = Vec::new();
+    for task in plane.tasks.values() {
+        out.push(serde_json::json!({
+            "task": task.name,
+            "agent": format!("{}@v{}", task.agent_name, task.agent.version),
+            "harness": task.agent.harness,
+            "model": task.agent.model,
+            "substrate": task.spec.substrate,
+            "parked": ledger.parked_reason(&task.name)?,
+            "runs_today": ledger.runs_today(&task.name)?,
+            "max_runs_per_day": task.spec.budget.max_runs_per_day,
+            "max_cost_per_run_usd": task.spec.budget.max_cost_per_run_usd,
+            "timeout_minutes": task.spec.budget.timeout_minutes,
+        }));
+    }
+    Ok(out)
+}
+
+/// The operator view: one server-rendered page, no JS, no build step.
+fn html_view(plane: &Plane, ledger: &Ledger) -> Result<String> {
+    let esc = |s: &str| {
+        s.replace('&', "&amp;")
+            .replace('<', "&lt;")
+            .replace('>', "&gt;")
+    };
+    let cost_today = ledger.cost_today()?;
+    let ceiling = plane
+        .spec
+        .budget
+        .max_cost_per_day_usd
+        .map(|c| format!(" / ${c:.2} ceiling"))
+        .unwrap_or_default();
+
+    let mut tasks_rows = String::new();
+    for t in tasks_view(plane, ledger)? {
+        let parked = t["parked"]
+            .as_str()
+            .map(|r| format!("<b>parked</b>: {}", esc(r)))
+            .unwrap_or_else(|| "active".into());
+        tasks_rows.push_str(&format!(
+            "<tr><td>{}</td><td>{}</td><td>{} {}</td><td>{}</td><td>{}{}</td><td>{}</td></tr>\n",
+            esc(t["task"].as_str().unwrap_or("-")),
+            esc(t["agent"].as_str().unwrap_or("-")),
+            esc(t["harness"].as_str().unwrap_or("-")),
+            esc(t["model"].as_str().unwrap_or("-")),
+            esc(t["substrate"].as_str().unwrap_or("-")),
+            t["runs_today"],
+            t["max_runs_per_day"]
+                .as_i64()
+                .map(|m| format!(" / {m}"))
+                .unwrap_or_default(),
+            parked,
+        ));
+    }
+
+    let mut run_rows = String::new();
+    for r in ledger.list_runs(None, None)?.into_iter().take(50) {
+        run_rows.push_str(&format!(
+            "<tr><td><a href=\"/api/runs/{id}\">{id}</a></td><td>{}</td><td class=\"{state}\">{state}</td><td>{}</td><td>{}</td><td>{}</td><td>{}</td></tr>\n",
+            esc(&r.task),
+            r.agent_name
+                .as_deref()
+                .map(esc)
+                .unwrap_or_else(|| "-".into()),
+            r.cost_usd
+                .map(|c| format!("${c:.4}"))
+                .unwrap_or_else(|| "-".into()),
+            r.duration_ms
+                .map(|d| format!("{:.1}s", d as f64 / 1000.0))
+                .unwrap_or_else(|| "-".into()),
+            esc(&r.created_at),
+            id = esc(&r.id),
+            state = esc(&r.state),
+        ));
+    }
+
+    Ok(format!(
+        r#"<!doctype html><html><head><meta charset="utf-8">
+<meta http-equiv="refresh" content="15">
+<title>bitterblossom</title>
+<style>
+body{{font:14px/1.5 ui-monospace,monospace;margin:2rem;background:#101014;color:#d8d8e0}}
+h1{{font-size:1.2rem}} h2{{font-size:1rem;margin-top:2rem}}
+table{{border-collapse:collapse;width:100%}}
+td,th{{padding:.3rem .6rem;border-bottom:1px solid #2a2a33;text-align:left}}
+a{{color:#9db4ff}} .success{{color:#7dce82}} .failure{{color:#e07a7a}}
+.running{{color:#e0c97a}} .pending,.blocked_budget{{color:#8a8a96}}
+</style></head><body>
+<h1>bitterblossom — the event plane</h1>
+<p>today's spend: ${cost_today:.4}{ceiling}</p>
+<h2>tasks</h2>
+<table><tr><th>task</th><th>agent</th><th>binding</th><th>substrate</th><th>runs today</th><th>state</th></tr>
+{tasks_rows}</table>
+<h2>recent runs</h2>
+<table><tr><th>run</th><th>task</th><th>state</th><th>agent</th><th>cost</th><th>duration</th><th>created</th></tr>
+{run_rows}</table>
+</body></html>"#
+    ))
+}
diff --git a/src/spec.rs b/src/spec.rs
index 03ea932..d62e30f 100644
--- a/src/spec.rs
+++ b/src/spec.rs
@@ -11,6 +11,11 @@ use serde::{Deserialize, Serialize};
 pub struct PlaneSpec {
     #[serde(default = "default_db_path")]
     pub db_path: String,
+    /// Dev planes may run workloads as local processes (tests, config
+    /// hacking). Production planes always dispatch to a remote substrate —
+    /// the plane never manages workload processes on the operator's machine.
+    #[serde(default)]
+    pub dev: bool,
     #[serde(default)]
     pub ingress: IngressSpec,
     #[serde(default)]
@@ -23,6 +28,7 @@ impl Default for PlaneSpec {
     fn default() -> Self {
         Self {
             db_path: default_db_path(),
+            dev: false,
             ingress: IngressSpec::default(),
             notify: NotifySpec::default(),
             budget: GlobalBudget::default(),
@@ -68,6 +74,12 @@ pub struct AgentSpec {
     pub version: u32,
     pub harness: String,
     pub model: String,
+    /// Model provider for open harnesses (pi); defaults to "openrouter".
+    pub provider: Option<String>,
+    /// "subscription" (operator identity: claude/codex OAuth) or "api"
+    /// (hermetic: only declared secrets cross the exec boundary).
+    /// Defaults by harness: claude/codex → subscription, pi → api.
+    pub auth: Option<String>,
     pub bin: Option<String>,
     #[serde(default)]
     pub args: Vec<String>,
@@ -77,6 +89,33 @@ pub struct AgentSpec {
     pub secrets: Vec<String>,
 }
 
+/// How an agent authenticates — the policy hinge. Subscription agents act
+/// as the operator and may only run dispatch (manual) work; api agents are
+/// hermetic and are the only class allowed on reflex (webhook/cron) work.
+#[derive(Debug, Clone, Copy, PartialEq, Eq)]
+pub enum AuthClass {
+    Subscription,
+    Api,
+}
+
+impl AgentSpec {
+    pub fn auth_class(&self) -> Result<AuthClass> {
+        match self.auth.as_deref() {
+            Some("subscription") => Ok(AuthClass::Subscription),
+            Some("api") => Ok(AuthClass::Api),
+            Some(other) => bail!("unknown auth '{other}' (known: subscription, api)"),
+            None => Ok(match self.harness.as_str() {
+                "claude" | "codex" => AuthClass::Subscription,
+                _ => AuthClass::Api,
+            }),
+        }
+    }
+
+    pub fn provider(&self) -> &str {
+        self.provider.as_deref().unwrap_or("openrouter")
+    }
+}
+
 fn default_version() -> u32 {
     1
 }
@@ -142,9 +181,71 @@ pub enum TriggerSpec {
         secret_env: String,
         /// Dedupe key derivation: "header:<Name>" or "json:<pointer>".
         dedupe_key: Option<String>,
+        /// Payload conditions, ANDed; a delivery failing any is
+        /// acknowledged but never becomes a run. Fail-closed: a missing
+        /// pointer rejects. Workload-agnostic containment — repo
+        /// allowlists, action filters, size caps — lives here, not in
+        /// card prose an agent may or may not honor.
+        #[serde(default)]
+        filter: Vec<FilterSpec>,
     },
 }
 
+/// One payload condition: an RFC 6901 pointer plus exactly one predicate.
+#[derive(Debug, Clone, Deserialize)]
+pub struct FilterSpec {
+    pub pointer: String,
+    pub equals: Option<serde_json::Value>,
+    pub any_of: Option<Vec<serde_json::Value>>,
+    /// Numeric ceiling (inclusive): e.g. cap PR additions.
+    pub max: Option<f64>,
+}
+
+impl FilterSpec {
+    fn validate(&self) -> Result<()> {
+        let n = [
+            self.equals.is_some(),
+            self.any_of.is_some(),
+            self.max.is_some(),
+        ]
+        .iter()
+        .filter(|b| **b)
+        .count();
+        if n != 1 {
+            bail!(
+                "filter on '{}' needs exactly one of equals / any_of / max",
+                self.pointer
+            );
+        }
+        Ok(())
+    }
+
+    /// `Some(reason)` when the payload fails this condition.
+    pub fn reject_reason(&self, payload: &serde_json::Value) -> Option<String> {
+        let Some(found) = payload.pointer(&self.pointer) else {
+            return Some(format!("pointer '{}' missing from payload", self.pointer));
+        };
+        if let Some(expected) = &self.equals {
+            if found != expected {
+                return Some(format!("'{}' is {found}, want {expected}", self.pointer));
+            }
+        }
+        if let Some(allowed) = &self.any_of {
+            if !allowed.contains(found) {
+                return Some(format!("'{}' is {found}, not in allowlist", self.pointer));
+            }
+        }
+        if let Some(max) = self.max {
+            match found.as_f64() {
+                Some(v) if v <= max => {}
+                Some(v) => return Some(format!("'{}' is {v}, max {max}", self.pointer)),
+                None => return Some(format!("'{}' is not numeric", self.pointer)),
+            }
+        }
+        None
+    }
+}
+
 /// A fully loaded task: spec + lane card + resolved agent.
 #[derive(Debug, Clone)]
 pub struct Task {
@@ -179,6 +280,11 @@ impl Plane {
     /// plane.toml is allowed — defaults apply — but a missing agent
     /// referenced by a task is an error.
     pub fn load(root: &Path) -> Result<Self> {
+        // Absolute root: artifact paths cross process-cwd boundaries (the
+        // sprite relay runs from $HOME), so relative roots corrupt uploads.
+        let root = &root
+            .canonicalize()
+            .with_context(|| format!("plane root {}", root.display()))?;
         let plane_path = root.join("plane.toml");
         let spec: PlaneSpec = if plane_path.exists() {
             toml::from_str(&std::fs::read_to_string(&plane_path)?)
@@ -237,6 +343,13 @@ impl Plane {
                 if task_spec.substrate == "sprites" && task_spec.workspace.host.is_none() {
                     bail!("task '{name}': substrate = \"sprites\" requires workspace.host");
                 }
+                if task_spec.substrate == "local" && !spec.dev {
+                    bail!(
+                        "task '{name}': the local substrate is dev/test machinery — \
+                         production planes dispatch to a remote substrate (sprites). \
+                         Set `dev = true` in plane.toml only for a development plane."
+                    );
+                }
                 tasks.insert(
                     name.clone(),
                     Task {
@@ -250,15 +363,64 @@ impl Plane {
             }
         }
 
+        // Model & auth policy is code, not intent: violations fail at load
+        // (and therefore at `bb check`), never at first dispatch.
+        for (name, agent) in &agents {
+            let auth = agent
+                .auth_class()
+                .with_context(|| format!("agent '{name}'"))?;
+            match (agent.harness.as_str(), auth) {
+                ("claude" | "codex", AuthClass::Api) => bail!(
+                    "agent '{name}': {} runs on subscription auth only — \
+                     Anthropic/OpenAI API keys are forbidden on this plane",
+                    agent.harness
+                ),
+                ("pi", AuthClass::Subscription) => {
+                    bail!("agent '{name}': pi has no subscription auth; use auth = \"api\"")
+                }
+                _ => {}
+            }
+            for secret in &agent.secrets {
+                if secret == "ANTHROPIC_API_KEY" || secret == "OPENAI_API_KEY" {
+                    bail!(
+                        "agent '{name}': secret '{secret}' is forbidden — \
+                         claude/codex run on subscription auth, never API keys"
+                    );
+                }
+            }
+        }
+
         // Bad trigger config fails at load, not at first delivery.
         let mut routes = std::collections::BTreeSet::new();
         for task in tasks.values() {
+            let auth = task
+                .agent
+                .auth_class()
+                .with_context(|| format!("task '{}'", task.name))?;
+            let reflex = task
+                .spec
+                .triggers
+                .iter()
+                .any(|t| !matches!(t, TriggerSpec::Manual));
+            if reflex && auth == AuthClass::Subscription {
+                bail!(
+                    "task '{}': reflex triggers (webhook/cron) require an auth = \"api\" \
+                     agent on an open harness — subscription agents ({}) run dispatch \
+                     (manual) work only",
+                    task.name,
+                    task.agent_name
+                );
+            }
             for trigger in &task.spec.triggers {
                 match trigger {
-                    TriggerSpec::Webhook { route, .. } => {
+                    TriggerSpec::Webhook { route, filter, .. } => {
                         if !routes.insert(route.clone()) {
                             bail!("webhook route '{route}' declared by more than one trigger");
                         }
+                        for f in filter {
+                            f.validate()
+                                .with_context(|| format!("task '{}'", task.name))?;
+                        }
                     }
                     TriggerSpec::Cron { schedule } => {
                         crate::ingress::parse_schedule(schedule)
diff --git a/src/substrate/local.rs b/src/substrate/local.rs
index 30d4ce2..b8b93ec 100644
--- a/src/substrate/local.rs
+++ b/src/substrate/local.rs
@@ -28,6 +28,7 @@ impl Substrate for LocalSubstrate {
             workspace,
             artifacts: attempt_dir.to_path_buf(),
             secrets: Vec::new(),
+            hermetic: true,
         }))
     }
 
@@ -52,6 +53,7 @@ pub struct LocalSession {
     workspace: PathBuf,
     artifacts: PathBuf,
     secrets: Vec<(String, String)>,
+    hermetic: bool,
 }
 
 impl LocalSession {
@@ -61,15 +63,50 @@ impl LocalSession {
             None,
             &self.workspace,
             &[],
+            false,
             None,
             timeout,
         )
     }
+
+    /// The only env vars that cross the exec boundary. Provider API keys in
+    /// the plane's environment (OPENAI_API_KEY, ANTHROPIC_API_KEY, …) never
+    /// reach a workload unless declared as agent secrets.
+    fn workload_env(&self) -> Result<Vec<(String, String)>> {
+        const PASS: &[&str] = &[
+            "PATH",
+            "TMPDIR",
+            "TERM",
+            "COLORTERM",
+            "LANG",
+            "LC_ALL",
+            "LC_CTYPE",
+            "USER",
+            "LOGNAME",
+            "SHELL",
+            "TZ",
+        ];
+        let mut env: Vec<(String, String)> = PASS
+            .iter()
+            .filter_map(|k| std::env::var(k).ok().map(|v| (k.to_string(), v)))
+            .collect();
+        let home = if self.hermetic {
+            let home = self.workspace.join(".home");
+            std::fs::create_dir_all(&home)?;
+            home.to_string_lossy().into_owned()
+        } else {
+            std::env::var("HOME").context("HOME not set")?
+        };
+        env.push(("HOME".to_string(), home));
+        env.extend(self.secrets.iter().cloned());
+        Ok(env)
+    }
 }
 
 impl Session for LocalSession {
     fn prepare(&mut self, plan: &WorkspacePlan) -> Result<()> {
         self.secrets = plan.secrets.clone();
+        self.hermetic = plan.hermetic;
         for repo in &plan.repos {
             let dest = self
                 .workspace
@@ -110,7 +147,8 @@ impl Session for LocalSession {
             cmd,
             stdin,
             &self.workspace,
-            &self.secrets,
+            &self.workload_env()?,
+            true,
             Some(&self.artifacts.join(PIDFILE)),
             timeout,
         )
@@ -141,7 +179,7 @@ fn repo_dir_name(url: &str) -> String {
         .to_string()
 }
 
-pub(crate) fn shell_quote(s: &str) -> String {
+pub fn shell_quote(s: &str) -> String {
     format!("'{}'", s.replace('\'', "'\\''"))
 }
 
@@ -150,6 +188,7 @@ pub(crate) fn run_with_timeout(
     stdin: Option<&str>,
     cwd: &Path,
     envs: &[(String, String)],
+    clear_env: bool,
     pidfile: Option<&Path>,
     timeout: Duration,
 ) -> Result<ExecResult> {
@@ -180,6 +219,9 @@ pub(crate) fn run_with_timeout(
         use std::os::unix::process::CommandExt;
         command.process_group(0);
     }
+    if clear_env {
+        command.env_clear();
+    }
     for (k, v) in envs {
         command.env(k, v);
     }
diff --git a/src/substrate/mod.rs b/src/substrate/mod.rs
index a3c064c..36e04be 100644
--- a/src/substrate/mod.rs
+++ b/src/substrate/mod.rs
@@ -32,6 +32,11 @@ pub struct WorkspacePlan {
     pub checkpoint: Option<String>,
     /// Resolved per-exec credentials (env name, value). Never persisted.
     pub secrets: Vec<(String, String)>,
+    /// Hermetic execs (auth = "api" agents) get a scrubbed environment and
+    /// a workspace-local HOME: nothing of the operator's identity crosses
+    /// the exec boundary except the declared secrets. Subscription agents
+    /// keep the real HOME — they run *as* the operator by design.
+    pub hermetic: bool,
 }
 
 #[derive(Debug)]
diff --git a/src/substrate/sprites.rs b/src/substrate/sprites.rs
index 29ca1aa..5afe797 100644
--- a/src/substrate/sprites.rs
+++ b/src/substrate/sprites.rs
@@ -71,7 +71,15 @@ impl Substrate for SpritesSubstrate {
         let mut cmd = vec![sprite_bin(), "exec".into()];
         cmd.extend(selector_args(host));
         cmd.extend(["--".into(), "sh".into(), "-c".into(), script]);
-        match run_with_timeout(&cmd, None, &relay_cwd(), &[], None, Duration::from_secs(60)) {
+        match run_with_timeout(
+            &cmd,
+            None,
+            &relay_cwd(),
+            &[],
+            false,
+            None,
+            Duration::from_secs(60),
+        ) {
             Ok(out) => match out.exit_code {
                 0 => ProbeResult::Alive,
                 4 => ProbeResult::Dead,
@@ -110,7 +118,7 @@ impl SpriteSession {
         // The sprite CLI parses remote args as its own flags without this.
         cmd.push("--".into());
         cmd.extend(remote.iter().cloned());
-        run_with_timeout(&cmd, stdin, &relay_cwd(), &[], None, timeout)
+        run_with_timeout(&cmd, stdin, &relay_cwd(), &[], false, None, timeout)
     }
 
     fn sprite_exec(
@@ -142,6 +150,7 @@ impl Session for SpriteSession {
                 None,
                 &relay_cwd(),
                 &[],
+                false,
                 None,
                 Duration::from_secs(300),
             )?;
diff --git a/tests/budgets.rs b/tests/budgets.rs
index a40f2d4..d7a84ee 100644
--- a/tests/budgets.rs
+++ b/tests/budgets.rs
@@ -56,9 +56,7 @@ fn setup(root: &Path, plane_toml: &str, budget_toml: &str) -> Plane {
         ),
     )
     .unwrap();
-    if !plane_toml.is_empty() {
-        fs::write(root.join("plane.toml"), plane_toml).unwrap();
-    }
+    fs::write(root.join("plane.toml"), format!("dev = true\n{plane_toml}")).unwrap();
     Plane::load(root).unwrap()
 }
 
diff --git a/tests/e2e_local.rs b/tests/e2e_local.rs
index cbdf682..dad98b1 100644
--- a/tests/e2e_local.rs
+++ b/tests/e2e_local.rs
@@ -31,6 +31,7 @@ fn write_executable(path: &Path, content: &str) {
 fn make_plane(root: &Path, stub: &str, task_extra: &str) -> Plane {
     fs::create_dir_all(root.join("agents")).unwrap();
     fs::create_dir_all(root.join("tasks/demo")).unwrap();
+    fs::write(root.join("plane.toml"), "dev = true\n").unwrap();
     let stub_path = root.join("stub-harness.sh");
     write_executable(&stub_path, stub);
     fs::write(
@@ -197,6 +198,58 @@ fn timeout_kills_the_harness_and_fails_the_run() {
     assert!(run.state_reason.unwrap().contains("timeout"));
 }
 
+#[test]
+fn hermetic_exec_passes_secrets_but_never_the_planes_env() {
+    // The stub echoes back what it sees: a plane env var that must NOT
+    // cross, the declared secret that must, and HOME which must be
+    // workspace-local for api-auth (hermetic) agents.
+    const PI_ENV_STUB: &str = r#"#!/bin/sh
+cat > /dev/null
+printf '{"type":"turn_end"}\n'
+printf '{"type":"message_end","message":{"role":"assistant","content":[{"type":"text","text":"leak=[%s] secret=[%s] home=[%s]"}],"usage":{"input":1,"output":2,"cost":{"total":0.0001}}}}\n' "$BB_TEST_LEAK" "$BB_TEST_SECRET" "$HOME"
+"#;
+    std::env::set_var("BB_TEST_LEAK", "plane-private");
+    std::env::set_var("BB_TEST_SECRET", "declared-secret");
+
+    let dir = tempfile::tempdir().unwrap();
+    let root = dir.path();
+    fs::create_dir_all(root.join("agents")).unwrap();
+    fs::create_dir_all(root.join("tasks/demo")).unwrap();
+    fs::write(root.join("plane.toml"), "dev = true\n").unwrap();
+    let stub_path = root.join("stub-pi.sh");
+    write_executable(&stub_path, PI_ENV_STUB);
+    fs::write(
+        root.join("agents/stub.toml"),
+        format!(
+            "harness = \"pi\"\nmodel = \"m\"\nbin = \"{}\"\nsecrets = [\"BB_TEST_SECRET\"]\n",
+            stub_path.display()
+        ),
+    )
+    .unwrap();
+    fs::write(root.join("tasks/demo/card.md"), "card\n").unwrap();
+    fs::write(
+        root.join("tasks/demo/task.toml"),
+        "agent = \"stub\"\nsubstrate = \"local\"\n[[trigger]]\nkind = \"manual\"\n",
+    )
+    .unwrap();
+    let plane = Plane::load(root).unwrap();
+    let mut ledger = Ledger::open(&plane.db_path()).unwrap();
+
+    let run_id = manual_run(&mut ledger, "demo", None);
+    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
+    assert_eq!(run.state, "success", "{:?}", run.state_reason);
+
+    let attempts = ledger.attempts(&run_id).unwrap();
+    let artifact_dir = Path::new(attempts[0].artifact_dir.as_deref().unwrap());
+    let result = fs::read_to_string(artifact_dir.join("result.md")).unwrap();
+    assert!(result.contains("leak=[]"), "plane env leaked: {result}");
+    assert!(result.contains("secret=[declared-secret]"), "{result}");
+    assert!(
+        result.contains("workspace/.home"),
+        "HOME not hermetic: {result}"
+    );
+}
+
 #[test]
 fn duplicate_manual_key_yields_one_run_two_ingress_events() {
     let dir = tempfile::tempdir().unwrap();
diff --git a/tests/ingress.rs b/tests/ingress.rs
index 6795235..500e81b 100644
--- a/tests/ingress.rs
+++ b/tests/ingress.rs
@@ -16,9 +16,10 @@ const SECRET_ENV: &str = "BB_TEST_HOOK_SECRET";
 fn make_plane(root: &Path) -> Plane {
     fs::create_dir_all(root.join("agents")).unwrap();
     fs::create_dir_all(root.join("tasks/demo")).unwrap();
+    fs::write(root.join("plane.toml"), "dev = true\n").unwrap();
     fs::write(
         root.join("agents/a.toml"),
-        "harness = \"claude\"\nmodel = \"m\"\n",
+        "harness = \"pi\"\nmodel = \"m\"\n",
     )
     .unwrap();
     fs::write(root.join("tasks/demo/card.md"), "card\n").unwrap();
@@ -163,7 +164,7 @@ fn five_field_cron_schedules_are_accepted_and_bad_ones_fail_at_load() {
     fs::create_dir_all(root.join("tasks/bad")).unwrap();
     fs::write(
         root.join("agents/a.toml"),
-        "harness = \"claude\"\nmodel = \"m\"\n",
+        "harness = \"pi\"\nmodel = \"m\"\n",
     )
     .unwrap();
     fs::write(root.join("tasks/bad/card.md"), "card\n").unwrap();
@@ -174,3 +175,55 @@ fn five_field_cron_schedules_are_accepted_and_bad_ones_fail_at_load() {
     .unwrap();
     assert!(Plane::load(root).is_err());
 }
+
+#[test]
+fn webhook_filters_reject_out_of_scope_deliveries_without_a_run() {
+    let dir = tempfile::tempdir().unwrap();
+    let root = dir.path();
+    fs::create_dir_all(root.join("agents")).unwrap();
+    fs::create_dir_all(root.join("tasks/rev")).unwrap();
+    fs::write(root.join("plane.toml"), "dev = true\n").unwrap();
+    fs::write(
+        root.join("agents/a.toml"),
+        "harness = \"pi\"\nmodel = \"m\"\n",
+    )
+    .unwrap();
+    fs::write(root.join("tasks/rev/card.md"), "card\n").unwrap();
+    fs::write(
+        root.join("tasks/rev/task.toml"),
+        "agent = \"a\"\nsubstrate = \"local\"\n\n\
+         [[trigger]]\nkind = \"webhook\"\nroute = \"rev\"\nsecret_env = \"BB_TEST_FILTER_SECRET\"\n\
+         [[trigger.filter]]\npointer = \"/repository/full_name\"\nany_of = [\"good/repo\"]\n\
+         [[trigger.filter]]\npointer = \"/action\"\nany_of = [\"opened\", \"synchronize\"]\n\
+         [[trigger.filter]]\npointer = \"/pull_request/draft\"\nequals = false\n\
+         [[trigger.filter]]\npointer = \"/pull_request/additions\"\nmax = 4000\n",
+    )
+    .unwrap();
+    let plane = Plane::load(root).unwrap();
+    let mut ledger = Ledger::open(&plane.db_path()).unwrap();
+    std::env::set_var("BB_TEST_FILTER_SECRET", "s3cret");
+
+    let deliver = |ledger: &mut Ledger, body: &str| {
+        let sig = sign_hmac("s3cret", body.as_bytes());
+        handle_webhook(&plane, ledger, "rev", &headers(&sig, "d1"), body).unwrap()
+    };
+
+    let in_scope = r#"{"action":"opened","repository":{"full_name":"good/repo"},"pull_request":{"draft":false,"additions":12,"head":{"sha":"a1"}}}"#;
+    assert_eq!(deliver(&mut ledger, in_scope).status, 202);
+
+    // Wrong repo, draft PR, ignored action, oversized diff, missing field:
+    // all acknowledged with 200 and no run row.
+    let cases = [
+        r#"{"action":"opened","repository":{"full_name":"evil/repo"},"pull_request":{"draft":false,"additions":1}}"#,
+        r#"{"action":"opened","repository":{"full_name":"good/repo"},"pull_request":{"draft":true,"additions":1}}"#,
+        r#"{"action":"labeled","repository":{"full_name":"good/repo"},"pull_request":{"draft":false,"additions":1}}"#,
+        r#"{"action":"opened","repository":{"full_name":"good/repo"},"pull_request":{"draft":false,"additions":99999}}"#,
+        r#"{"action":"opened","repository":{"full_name":"good/repo"}}"#,
+    ];
+    for body in cases {
+        let resp = deliver(&mut ledger, body);
+        assert_eq!(resp.status, 200, "{body} -> {}", resp.body);
+        assert!(resp.body.contains("filtered"), "{}", resp.body);
+    }
+    assert_eq!(ledger.list_runs(None, None).unwrap().len(), 1);
+}
diff --git a/tests/policy.rs b/tests/policy.rs
new file mode 100644
index 0000000..f9cb1af
--- /dev/null
+++ b/tests/policy.rs
@@ -0,0 +1,109 @@
+//! Model & auth policy is enforced at config load (`bb check`), not at
+//! dispatch: subscription harnesses never take API keys, reflex triggers
+//! never bind subscription agents.
+
+use std::fs;
+use std::path::Path;
+
+use bitterblossom::spec::Plane;
+
+fn plane_with(root: &Path, agent_toml: &str, task_toml: &str) -> anyhow::Result<Plane> {
+    fs::create_dir_all(root.join("agents")).unwrap();
+    fs::create_dir_all(root.join("tasks/t")).unwrap();
+    fs::write(root.join("plane.toml"), "dev = true\n").unwrap();
+    fs::write(root.join("agents/a.toml"), agent_toml).unwrap();
+    fs::write(root.join("tasks/t/card.md"), "card\n").unwrap();
+    fs::write(root.join("tasks/t/task.toml"), task_toml).unwrap();
+    Plane::load(root)
+}
+
+const MANUAL: &str = "agent = \"a\"\n[[trigger]]\nkind = \"manual\"\n";
+const CRON: &str = "agent = \"a\"\n[[trigger]]\nkind = \"cron\"\nschedule = \"0 * * * *\"\n";
+
+#[test]
+fn claude_with_api_auth_is_rejected_at_load() {
+    let dir = tempfile::tempdir().unwrap();
+    let err = plane_with(
+        dir.path(),
+        "harness = \"claude\"\nmodel = \"m\"\nauth = \"api\"\n",
+        MANUAL,
+    )
+    .unwrap_err();
+    assert!(err.to_string().contains("subscription auth only"), "{err}");
+}
+
+#[test]
+fn anthropic_or_openai_api_keys_are_forbidden_secrets() {
+    let dir = tempfile::tempdir().unwrap();
+    let err = plane_with(
+        dir.path(),
+        "harness = \"pi\"\nmodel = \"m\"\nsecrets = [\"OPENAI_API_KEY\"]\n",
+        MANUAL,
+    )
+    .unwrap_err();
+    assert!(err.to_string().contains("forbidden"), "{err}");
+}
+
+#[test]
+fn reflex_triggers_require_api_auth_agents() {
+    let dir = tempfile::tempdir().unwrap();
+    // claude defaults to subscription; a cron (reflex) trigger must fail.
+    let err = plane_with(dir.path(), "harness = \"claude\"\nmodel = \"m\"\n", CRON).unwrap_err();
+    assert!(err.to_string().contains("reflex"), "{err}");
+
+    // The same agent on a manual-only (dispatch) task is fine.
+    let dir2 = tempfile::tempdir().unwrap();
+    plane_with(dir2.path(), "harness = \"claude\"\nmodel = \"m\"\n", MANUAL).unwrap();
+}
+
+#[test]
+fn pi_agents_default_to_api_auth_and_openrouter_and_may_run_reflex() {
+    let dir = tempfile::tempdir().unwrap();
+    let plane = plane_with(
+        dir.path(),
+        "harness = \"pi\"\nmodel = \"moonshotai/kimi-k2.6\"\nsecrets = [\"OPENROUTER_API_KEY\"]\n",
+        CRON,
+    )
+    .unwrap();
+    let agent = &plane.tasks["t"].agent;
+    assert_eq!(
+        agent.auth_class().unwrap(),
+        bitterblossom::spec::AuthClass::Api
+    );
+    assert_eq!(agent.provider(), "openrouter");
+}
+
+#[test]
+fn pi_cannot_claim_subscription_auth() {
+    let dir = tempfile::tempdir().unwrap();
+    let err = plane_with(
+        dir.path(),
+        "harness = \"pi\"\nmodel = \"m\"\nauth = \"subscription\"\n",
+        MANUAL,
+    )
+    .unwrap_err();
+    assert!(err.to_string().contains("no subscription auth"), "{err}");
+}
+
+#[test]
+fn local_substrate_is_rejected_outside_dev_planes() {
+    let dir = tempfile::tempdir().unwrap();
+    let root = dir.path();
+    fs::create_dir_all(root.join("agents")).unwrap();
+    fs::create_dir_all(root.join("tasks/t")).unwrap();
+    // No dev flag: this is a production plane.
+    fs::write(root.join("plane.toml"), "").unwrap();
+    fs::write(
+        root.join("agents/a.toml"),
+        "harness = \"pi\"\nmodel = \"m\"\n",
+    )
+    .unwrap();
+    fs::write(root.join("tasks/t/card.md"), "card\n").unwrap();
+    fs::write(
+        root.join("tasks/t/task.toml"),
+        "agent = \"a\"\nsubstrate = \"local\"\n[[trigger]]\nkind = \"manual\"\n",
+    )
+    .unwrap();
+    let err = Plane::load(root).unwrap_err();
+    assert!(err.to_string().contains("dev/test machinery"), "{err}");
+}
diff --git a/tests/recovery.rs b/tests/recovery.rs
index 39ba408..b07225b 100644
--- a/tests/recovery.rs
+++ b/tests/recovery.rs
@@ -13,6 +13,7 @@ use bitterblossom::spec::Plane;
 fn make_plane(root: &Path) -> Plane {
     fs::create_dir_all(root.join("agents")).unwrap();
     fs::create_dir_all(root.join("tasks/demo")).unwrap();
+    fs::write(root.join("plane.toml"), "dev = true\n").unwrap();
     fs::write(
         root.join("agents/a.toml"),
         "harness = \"claude\"\nmodel = \"m\"\n",

2026-06-11T03:01:36.958288Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when UnexpectedContentType(Some("text/plain; body: token revoked"))
hook: SessionStart
hook: SessionStart Completed
codex
I’ll review this as a read-only diff audit. I’m going to use the code-review workflow for the reviewer stance, then inspect only the changed areas and surrounding repo context needed to validate the oracle.
exec
/bin/zsh -lc "sed -n '1,220p' /Users/phaedrus/Development/harness-kit/skills/code-review/SKILL.md" in /Users/phaedrus/Development/harness-kit
 succeeded in 0ms:
---
name: code-review
description: |
  Dispatch-shaped code review: fan the diff out to fresh-context reviewers
  across diverse providers and model families, synthesize, fix blockers,
  re-review until clean. Use when: "review this", "code review", "is this
  ready to ship", "second-model review". Trigger: /code-review, /review.
argument-hint: "[branch|diff|files]"
---

# /code-review

You are the marshal. Read the diff, dispatch diverse fresh-context
reviewers, synthesize, fix blockers, loop until clean. The authoring agent
never ships on its own review — that is the one hard rule.

## Dispatch

1. **Scope = the diff.** `git diff <base>...HEAD` (default base: the repo's
   default branch). Classify what changed: API, UI, security surface, data
   model, infra, docs.
2. **Fan out in parallel, decorrelated.** Native subagents for focused
   lenses (pick 2–4 that fit the diff: correctness, security, simplicity,
   tests); peer harness CLIs (`/roster`) for cross-model judgment — a
   different model family has decorrelated failure modes; `thinktank
   review` when installed for a wide bench. If the harness can run a
   large-scale background orchestration where reviewers adversarially
   cross-check each other's findings before reporting, a substantive diff
   is a natural fit — that scale costs tokens, so routine diffs don't get
   it. Reviewers get the diff, the acceptance oracle, and a risk lens —
   **never the author's reasoning trail.**
3. **Aim reviewers at production embarrassment, not nitpicks.** Tell each
   one what to ignore (style, naming, speculative "consider…") as
   explicitly as what to find.

## What reviewers hunt

Plausible-but-wrong is the failure mode of model-written code:

- Stub or specification-shaped implementations that pass tests but don't work
- Wrong complexity (O(n²) hiding behind a clean interface)
- Tests that never invoke the changed entrypoint (adjacent green lanes)
- Missing invariant checks that only matter at scale or under concurrency
- Unnecessary abstraction — wrappers, modes, layers that don't earn their keep
- Swallowed errors, magic fallbacks, internal mocks

If the diff adds or changes an executable path (CLI, script, migration, job),
someone must run it once or cite the gate that does — otherwise it's an
**unverified runtime path** and blocks Ship. If the diff touches a visual or
user-facing surface, at least one reviewer exercises it live.

## Synthesize and verdict

Dedupe across reviewers; rank **blocking** (correctness, security, unverified
runtime path) > **important** (architecture, test strength) > **advisory**
(everything else). Blocking findings get fixed and the fix re-reviewed —
full pass, not a spot-check. Max 3 fix-review iterations, then escalate to
the operator with the open findings. Ship / Don't-ship is the lead's call on
the reviewers' evidence; advisory findings never block.

## Gotchas

- **Monoculture.** Same-model subagents alone are groupthink with extra
  steps. Substantive diffs get at least one other model family.
- **Reviewing the repo instead of the diff.** Scope discipline keeps
  findings actionable.
- **Treating all findings equally.** Severity ranking is the marshal's job;
  a wall of undifferentiated comments is review theater.
- **Skipping re-review after fixes.** A fix can introduce the next bug;
  blockers get a fresh pass.

exec
/bin/zsh -lc "pwd && rg --files -g 'AGENTS.md' -g 'CLAUDE.md' -g 'Cargo.toml' -g 'src/*.rs' -g 'tests/*.rs'" in /Users/phaedrus/Development/bitterblossom
 succeeded in 143ms:
/Users/phaedrus/Development/bitterblossom
CLAUDE.md
src/main.rs
src/harness.rs
src/lib.rs
src/recovery.rs
src/ingress.rs
src/dispatch.rs
src/budget.rs
src/ledger.rs
src/serve.rs
src/spec.rs
src/notify.rs
Cargo.toml
tests/e2e_sprites.rs
tests/budgets.rs
tests/e2e_local.rs
tests/recovery.rs
tests/ingress.rs
tests/policy.rs

codex
The review skill expects fanout for substantive diffs, but this request explicitly asks for my adversarial findings only and read-only repo context. I’m keeping scope tight: changed security/config/API paths plus nearby execution code for line-accurate findings.
exec
/bin/zsh -lc "nl -ba src/serve.rs | sed -n '150,420p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba src/substrate/local.rs | sed -n '1,280p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba src/spec.rs | sed -n '60,440p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba src/harness.rs | sed -n '1,380p'" in /Users/phaedrus/Development/bitterblossom
 succeeded in 205ms:
   150	        // dispatch_run owns state-transition notifications (dead letters,
   151	        // budget parks), so the CLI path notifies identically.
   152	        let run = dispatch::dispatch_run(&plane, &mut ledger, run_id)?;
   153	        eprintln!("run {} {} (task={})", run.id, run.state, run.task);
   154	        Ok(())
   155	    })();
   156	    if let Err(e) = result {
   157	        eprintln!("run {run_id}: {e:#}");
   158	    }
   159	}
   160	
   161	fn http_loop(root: &Path, plane: &Plane) -> Result<()> {
   162	    let bind = plane.spec.ingress.bind.clone();
   163	    let server = tiny_http::Server::http(&bind).map_err(|e| anyhow::anyhow!("bind {bind}: {e}"))?;
   164	    eprintln!("bb serve listening on {bind}");
   165	
   166	    for mut request in server.incoming_requests() {
   167	        let response = handle_request(root, &mut request);
   168	        let (status, body) = match response {
   169	            Ok(r) => r,
   170	            Err(e) => (500, format!("{{\"error\":\"{e}\"}}")),
   171	        };
   172	        let content_type: &[u8] = if body.starts_with("<!doctype") {
   173	            b"text/html; charset=utf-8"
   174	        } else {
   175	            b"application/json"
   176	        };
   177	        let _ = request.respond(
   178	            tiny_http::Response::from_string(body)
   179	                .with_status_code(status)
   180	                .with_header(
   181	                    tiny_http::Header::from_bytes(&b"Content-Type"[..], content_type)
   182	                        .expect("static header"),
   183	                ),
   184	        );
   185	    }
   186	    Ok(())
   187	}
   188	
   189	/// Bearer-token gate for the read surface. With BB_API_TOKEN unset the
   190	/// surface is open — acceptable only because the default bind is
   191	/// loopback; set the token before binding wider.
   192	fn read_authorized(request: &tiny_http::Request, url: &str) -> bool {
   193	    let Ok(token) = std::env::var("BB_API_TOKEN") else {
   194	        return true;
   195	    };
   196	    let header_ok = request.headers().iter().any(|h| {
   197	        h.field
   198	            .as_str()
   199	            .as_str()
   200	            .eq_ignore_ascii_case("authorization")
   201	            && h.value.as_str() == format!("Bearer {token}")
   202	    });
   203	    // ?token= lets a browser reach the HTML view; tokens in URLs are
   204	    // weaker than headers — fine for a loopback operator page.
   205	    header_ok || url.contains(&format!("token={token}"))
   206	}
   207	
   208	fn query_param(url: &str, name: &str) -> Option<String> {
   209	    let (_, query) = url.split_once('?')?;
   210	    query.split('&').find_map(|kv| {
   211	        let (k, v) = kv.split_once('=')?;
   212	        (k == name && !v.is_empty()).then(|| v.to_string())
   213	    })
   214	}
   215	
   216	fn handle_request(root: &Path, request: &mut tiny_http::Request) -> Result<(u16, String)> {
   217	    let method = request.method().to_string();
   218	    let url = request.url().to_string();
   219	
   220	    if method == "GET" && (url.starts_with("/api/") || url == "/" || url.starts_with("/?")) {
   221	        if !read_authorized(request, &url) {
   222	            return Ok((401, "{\"error\":\"missing or bad bearer token\"}".into()));
   223	        }
   224	        let plane = Plane::load(root)?;
   225	        let ledger = Ledger::open(&plane.db_path())?;
   226	        let path = url.split('?').next().unwrap_or(&url);
   227	        return match path {
   228	            "/" => html_view(&plane, &ledger).map(|body| (200, body)),
   229	            "/api/runs" => {
   230	                let task = query_param(&url, "task");
   231	                let state = query_param(&url, "state");
   232	                let runs = ledger.list_runs(task.as_deref(), state.as_deref())?;
   233	                Ok((200, serde_json::to_string(&runs)?))
   234	            }
   235	            "/api/dlq" => Ok((200, serde_json::to_string(&ledger.list_dead_letters()?)?)),
   236	            "/api/tasks" => Ok((200, serde_json::to_string(&tasks_view(&plane, &ledger)?)?)),
   237	            _ => {
   238	                if let Some(id) = path.strip_prefix("/api/runs/") {
   239	                    let run = ledger.run(id)?;
   240	                    let body = serde_json::json!({
   241	                        "run": run,
   242	                        "attempts": ledger.attempts(id)?,
   243	                        "events": ledger.events(id)?,
   244	                    });
   245	                    Ok((200, body.to_string()))
   246	                } else {
   247	                    Ok((404, "{\"error\":\"not found\"}".into()))
   248	                }
   249	            }
   250	        };
   251	    }
   252	
   253	    if method == "GET" && url == "/health" {
   254	        let plane = Plane::load(root)?;
   255	        let ledger = Ledger::open(&plane.db_path())?;
   256	        let pending = ledger.runs_in_state("pending")?;
   257	        let running = ledger.runs_in_state("running")?;
   258	        let oldest_pending = pending.last().map(|r| r.created_at.clone());
   259	        return Ok((
   260	            200,
   261	            serde_json::json!({
   262	                "pending": pending.len(),
   263	                "running": running.len(),
   264	                "oldest_pending": oldest_pending,
   265	            })
   266	            .to_string(),
   267	        ));
   268	    }
   269	
   270	    if method == "POST" {
   271	        if let Some(route) = url.strip_prefix("/hooks/") {
   272	            let route = route.trim_end_matches('/').to_string();
   273	            let headers: Vec<(String, String)> = request
   274	                .headers()
   275	                .iter()
   276	                .map(|h| {
   277	                    (
   278	                        h.field.as_str().as_str().to_string(),
   279	                        h.value.as_str().to_string(),
   280	                    )
   281	                })
   282	                .collect();
   283	            let mut body = String::new();
   284	            request
   285	                .as_reader()
   286	                .read_to_string(&mut body)
   287	                .context("read body")?;
   288	            let plane = Plane::load(root)?;
   289	            let mut ledger = Ledger::open(&plane.db_path())?;
   290	            let resp = ingress::handle_webhook(&plane, &mut ledger, &route, &headers, &body)?;
   291	            return Ok((resp.status, resp.body));
   292	        }
   293	    }
   294	
   295	    Ok((404, "{\"error\":\"not found\"}".to_string()))
   296	}
   297	
   298	/// Per-task posture: agent binding, parked state, today's runs, budgets.
   299	fn tasks_view(plane: &Plane, ledger: &Ledger) -> Result<Vec<serde_json::Value>> {
   300	    let mut out = Vec::new();
   301	    for task in plane.tasks.values() {
   302	        out.push(serde_json::json!({
   303	            "task": task.name,
   304	            "agent": format!("{}@v{}", task.agent_name, task.agent.version),
   305	            "harness": task.agent.harness,
   306	            "model": task.agent.model,
   307	            "substrate": task.spec.substrate,
   308	            "parked": ledger.parked_reason(&task.name)?,
   309	            "runs_today": ledger.runs_today(&task.name)?,
   310	            "max_runs_per_day": task.spec.budget.max_runs_per_day,
   311	            "max_cost_per_run_usd": task.spec.budget.max_cost_per_run_usd,
   312	            "timeout_minutes": task.spec.budget.timeout_minutes,
   313	        }));
   314	    }
   315	    Ok(out)
   316	}
   317	
   318	/// The operator view: one server-rendered page, no JS, no build step.
   319	fn html_view(plane: &Plane, ledger: &Ledger) -> Result<String> {
   320	    let esc = |s: &str| {
   321	        s.replace('&', "&amp;")
   322	            .replace('<', "&lt;")
   323	            .replace('>', "&gt;")
   324	    };
   325	    let cost_today = ledger.cost_today()?;
   326	    let ceiling = plane
   327	        .spec
   328	        .budget
   329	        .max_cost_per_day_usd
   330	        .map(|c| format!(" / ${c:.2} ceiling"))
   331	        .unwrap_or_default();
   332	
   333	    let mut tasks_rows = String::new();
   334	    for t in tasks_view(plane, ledger)? {
   335	        let parked = t["parked"]
   336	            .as_str()
   337	            .map(|r| format!("<b>parked</b>: {}", esc(r)))
   338	            .unwrap_or_else(|| "active".into());
   339	        tasks_rows.push_str(&format!(
   340	            "<tr><td>{}</td><td>{}</td><td>{} {}</td><td>{}</td><td>{}{}</td><td>{}</td></tr>\n",
   341	            esc(t["task"].as_str().unwrap_or("-")),
   342	            esc(t["agent"].as_str().unwrap_or("-")),
   343	            esc(t["harness"].as_str().unwrap_or("-")),
   344	            esc(t["model"].as_str().unwrap_or("-")),
   345	            esc(t["substrate"].as_str().unwrap_or("-")),
   346	            t["runs_today"],
   347	            t["max_runs_per_day"]
   348	                .as_i64()
   349	                .map(|m| format!(" / {m}"))
   350	                .unwrap_or_default(),
   351	            parked,
   352	        ));
   353	    }
   354	
   355	    let mut run_rows = String::new();
   356	    for r in ledger.list_runs(None, None)?.into_iter().take(50) {
   357	        run_rows.push_str(&format!(
   358	            "<tr><td><a href=\"/api/runs/{id}\">{id}</a></td><td>{}</td><td class=\"{state}\">{state}</td><td>{}</td><td>{}</td><td>{}</td><td>{}</td></tr>\n",
   359	            esc(&r.task),
   360	            r.agent_name
   361	                .as_deref()
   362	                .map(esc)
   363	                .unwrap_or_else(|| "-".into()),
   364	            r.cost_usd
   365	                .map(|c| format!("${c:.4}"))
   366	                .unwrap_or_else(|| "-".into()),
   367	            r.duration_ms
   368	                .map(|d| format!("{:.1}s", d as f64 / 1000.0))
   369	                .unwrap_or_else(|| "-".into()),
   370	            esc(&r.created_at),
   371	            id = esc(&r.id),
   372	            state = esc(&r.state),
   373	        ));
   374	    }
   375	
   376	    Ok(format!(
   377	        r#"<!doctype html><html><head><meta charset="utf-8">
   378	<meta http-equiv="refresh" content="15">
   379	<title>bitterblossom</title>
   380	<style>
   381	body{{font:14px/1.5 ui-monospace,monospace;margin:2rem;background:#101014;color:#d8d8e0}}
   382	h1{{font-size:1.2rem}} h2{{font-size:1rem;margin-top:2rem}}
   383	table{{border-collapse:collapse;width:100%}}
   384	td,th{{padding:.3rem .6rem;border-bottom:1px solid #2a2a33;text-align:left}}
   385	a{{color:#9db4ff}} .success{{color:#7dce82}} .failure{{color:#e07a7a}}
   386	.running{{color:#e0c97a}} .pending,.blocked_budget{{color:#8a8a96}}
   387	</style></head><body>
   388	<h1>bitterblossom — the event plane</h1>
   389	<p>today's spend: ${cost_today:.4}{ceiling}</p>
   390	<h2>tasks</h2>
   391	<table><tr><th>task</th><th>agent</th><th>binding</th><th>substrate</th><th>runs today</th><th>state</th></tr>
   392	{tasks_rows}</table>
   393	<h2>recent runs</h2>
   394	<table><tr><th>run</th><th>task</th><th>state</th><th>agent</th><th>cost</th><th>duration</th><th>created</th></tr>
   395	{run_rows}</table>
   396	</body></html>"#
   397	    ))
   398	}

 succeeded in 202ms:
     1	//! Local-process substrate: a workspace directory on this machine. The
     2	//! degenerate case that keeps every task terminal-runnable and tests cheap.
     3	
     4	use std::io::Write as _;
     5	use std::path::{Path, PathBuf};
     6	use std::process::{Command, Stdio};
     7	use std::time::{Duration, Instant};
     8	
     9	use anyhow::{Context, Result};
    10	
    11	use super::{ExecResult, ProbeResult, Session, Substrate, WorkspacePlan, CARD_FILENAME};
    12	
    13	/// Pidfile written next to the artifacts so a restarted plane can probe
    14	/// whether the harness process survived.
    15	pub const PIDFILE: &str = "harness.pid";
    16	
    17	pub struct LocalSubstrate;
    18	
    19	impl Substrate for LocalSubstrate {
    20	    fn name(&self) -> &'static str {
    21	        "local"
    22	    }
    23	
    24	    fn acquire(&self, _host: &str, attempt_dir: &Path) -> Result<Box<dyn Session>> {
    25	        let workspace = attempt_dir.join("workspace");
    26	        std::fs::create_dir_all(&workspace)?;
    27	        Ok(Box::new(LocalSession {
    28	            workspace,
    29	            artifacts: attempt_dir.to_path_buf(),
    30	            secrets: Vec::new(),
    31	            hermetic: true,
    32	        }))
    33	    }
    34	
    35	    fn probe(&self, _host: &str, attempt_dir: &Path, _marker: &str) -> ProbeResult {
    36	        let pidfile = attempt_dir.join(PIDFILE);
    37	        let Ok(content) = std::fs::read_to_string(&pidfile) else {
    38	            return ProbeResult::Unknown(format!("no pidfile at {}", pidfile.display()));
    39	        };
    40	        let Ok(pid) = content.trim().parse::<i32>() else {
    41	            return ProbeResult::Unknown("unparseable pidfile".into());
    42	        };
    43	        // kill(pid, 0): existence check without signaling.
    44	        if unsafe { libc::kill(pid, 0) } == 0 {
    45	            ProbeResult::Alive
    46	        } else {
    47	            ProbeResult::Dead
    48	        }
    49	    }
    50	}
    51	
    52	pub struct LocalSession {
    53	    workspace: PathBuf,
    54	    artifacts: PathBuf,
    55	    secrets: Vec<(String, String)>,
    56	    hermetic: bool,
    57	}
    58	
    59	impl LocalSession {
    60	    fn run_shell(&self, script: &str, timeout: Duration) -> Result<ExecResult> {
    61	        run_with_timeout(
    62	            &["sh".into(), "-c".into(), script.into()],
    63	            None,
    64	            &self.workspace,
    65	            &[],
    66	            false,
    67	            None,
    68	            timeout,
    69	        )
    70	    }
    71	
    72	    /// The only env vars that cross the exec boundary. Provider API keys in
    73	    /// the plane's environment (OPENAI_API_KEY, ANTHROPIC_API_KEY, …) never
    74	    /// reach a workload unless declared as agent secrets.
    75	    fn workload_env(&self) -> Result<Vec<(String, String)>> {
    76	        const PASS: &[&str] = &[
    77	            "PATH",
    78	            "TMPDIR",
    79	            "TERM",
    80	            "COLORTERM",
    81	            "LANG",
    82	            "LC_ALL",
    83	            "LC_CTYPE",
    84	            "USER",
    85	            "LOGNAME",
    86	            "SHELL",
    87	            "TZ",
    88	        ];
    89	        let mut env: Vec<(String, String)> = PASS
    90	            .iter()
    91	            .filter_map(|k| std::env::var(k).ok().map(|v| (k.to_string(), v)))
    92	            .collect();
    93	        let home = if self.hermetic {
    94	            let home = self.workspace.join(".home");
    95	            std::fs::create_dir_all(&home)?;
    96	            home.to_string_lossy().into_owned()
    97	        } else {
    98	            std::env::var("HOME").context("HOME not set")?
    99	        };
   100	        env.push(("HOME".to_string(), home));
   101	        env.extend(self.secrets.iter().cloned());
   102	        Ok(env)
   103	    }
   104	}
   105	
   106	impl Session for LocalSession {
   107	    fn prepare(&mut self, plan: &WorkspacePlan) -> Result<()> {
   108	        self.secrets = plan.secrets.clone();
   109	        self.hermetic = plan.hermetic;
   110	        for repo in &plan.repos {
   111	            let dest = self
   112	                .workspace
   113	                .join(repo_dir_name(&repo.url))
   114	                .to_string_lossy()
   115	                .into_owned();
   116	            let clone = format!(
   117	                "git clone --depth 1 --branch {ref_} {url} {dest} && git -C {dest} reset --hard",
   118	                ref_ = shell_quote(&repo.r#ref),
   119	                url = shell_quote(&repo.url),
   120	                dest = shell_quote(&dest),
   121	            );
   122	            let out = self.run_shell(&clone, Duration::from_secs(300))?;
   123	            if out.exit_code != 0 {
   124	                anyhow::bail!("clone {} failed: {}", repo.url, out.stderr.trim());
   125	            }
   126	        }
   127	        std::fs::write(self.workspace.join(CARD_FILENAME), &plan.card)?;
   128	        if let Some(payload) = &plan.payload {
   129	            std::fs::write(self.workspace.join(super::EVENT_FILENAME), payload)?;
   130	        }
   131	        if let Some(pre) = &plan.pre_command {
   132	            let out = self.run_shell(pre, Duration::from_secs(600))?;
   133	            if out.exit_code != 0 {
   134	                anyhow::bail!("pre_command failed: {}", out.stderr.trim());
   135	            }
   136	        }
   137	        Ok(())
   138	    }
   139	
   140	    fn execute(
   141	        &mut self,
   142	        cmd: &[String],
   143	        stdin: Option<&str>,
   144	        timeout: Duration,
   145	    ) -> Result<ExecResult> {
   146	        run_with_timeout(
   147	            cmd,
   148	            stdin,
   149	            &self.workspace,
   150	            &self.workload_env()?,
   151	            true,
   152	            Some(&self.artifacts.join(PIDFILE)),
   153	            timeout,
   154	        )
   155	    }
   156	
   157	    fn write_artifact(&mut self, name: &str, data: &[u8]) -> Result<()> {
   158	        let path = self.artifacts.join(name);
   159	        if let Some(parent) = path.parent() {
   160	            std::fs::create_dir_all(parent)?;
   161	        }
   162	        std::fs::write(path, data)?;
   163	        Ok(())
   164	    }
   165	
   166	    fn release(&mut self) -> Result<()> {
   167	        // Workspace is kept on disk under the attempt dir for diagnosis;
   168	        // nothing to tear down for a local process.
   169	        Ok(())
   170	    }
   171	}
   172	
   173	fn repo_dir_name(url: &str) -> String {
   174	    url.trim_end_matches('/')
   175	        .trim_end_matches(".git")
   176	        .rsplit('/')
   177	        .next()
   178	        .unwrap_or("repo")
   179	        .to_string()
   180	}
   181	
   182	pub fn shell_quote(s: &str) -> String {
   183	    format!("'{}'", s.replace('\'', "'\\''"))
   184	}
   185	
   186	pub(crate) fn run_with_timeout(
   187	    cmd: &[String],
   188	    stdin: Option<&str>,
   189	    cwd: &Path,
   190	    envs: &[(String, String)],
   191	    clear_env: bool,
   192	    pidfile: Option<&Path>,
   193	    timeout: Duration,
   194	) -> Result<ExecResult> {
   195	    use std::sync::atomic::{AtomicU64, Ordering};
   196	    static CAPTURE_SEQ: AtomicU64 = AtomicU64::new(0);
   197	
   198	    let (program, args) = cmd.split_first().context("empty command")?;
   199	    let seq = CAPTURE_SEQ.fetch_add(1, Ordering::Relaxed);
   200	    let capture = |stream: &str| {
   201	        std::env::temp_dir().join(format!("bb-cap-{}-{seq}.{stream}", std::process::id()))
   202	    };
   203	    let stdout_path = capture("out");
   204	    let stderr_path = capture("err");
   205	    let mut command = Command::new(program);
   206	    command
   207	        .args(args)
   208	        .current_dir(cwd)
   209	        .stdin(if stdin.is_some() {
   210	            Stdio::piped()
   211	        } else {
   212	            Stdio::null()
   213	        })
   214	        .stdout(std::fs::File::create(&stdout_path)?)
   215	        .stderr(std::fs::File::create(&stderr_path)?);
   216	    // Own process group so a timeout kills the harness AND everything it
   217	    // spawned — a surviving grandchild is hidden concurrent work.
   218	    {
   219	        use std::os::unix::process::CommandExt;
   220	        command.process_group(0);
   221	    }
   222	    if clear_env {
   223	        command.env_clear();
   224	    }
   225	    for (k, v) in envs {
   226	        command.env(k, v);
   227	    }
   228	    let mut child = command
   229	        .spawn()
   230	        .with_context(|| format!("spawn {program}"))?;
   231	    if let Some(path) = pidfile {
   232	        let _ = std::fs::write(path, child.id().to_string());
   233	    }
   234	
   235	    if let Some(input) = stdin {
   236	        let mut pipe = child.stdin.take().context("stdin pipe")?;
   237	        pipe.write_all(input.as_bytes())?;
   238	        // Drop closes the pipe so the child sees EOF.
   239	    }
   240	
   241	    let started = Instant::now();
   242	    let mut timed_out = false;
   243	    let exit_code = loop {
   244	        if let Some(status) = child.try_wait()? {
   245	            break status.code().unwrap_or(-1) as i64;
   246	        }
   247	        if started.elapsed() >= timeout {
   248	            timed_out = true;
   249	            // Kill the whole process group, not just the direct child.
   250	            unsafe {
   251	                libc::kill(-(child.id() as i32), libc::SIGKILL);
   252	            }
   253	            let _ = child.kill();
   254	            let status = child.wait()?;
   255	            break status.code().unwrap_or(-1) as i64;
   256	        }
   257	        std::thread::sleep(Duration::from_millis(100));
   258	    };
   259	
   260	    let stdout = std::fs::read_to_string(&stdout_path).unwrap_or_default();
   261	    let stderr = std::fs::read_to_string(&stderr_path).unwrap_or_default();
   262	    let _ = std::fs::remove_file(&stdout_path);
   263	    let _ = std::fs::remove_file(&stderr_path);
   264	    Ok(ExecResult {
   265	        exit_code,
   266	        stdout,
   267	        stderr,
   268	        timed_out,
   269	    })
   270	}

 succeeded in 203ms:
    60	
    61	#[derive(Debug, Clone, Deserialize, Default)]
    62	pub struct NotifySpec {
    63	    pub webhook_url: Option<String>,
    64	}
    65	
    66	#[derive(Debug, Clone, Deserialize, Default)]
    67	pub struct GlobalBudget {
    68	    pub max_cost_per_day_usd: Option<f64>,
    69	}
    70	
    71	#[derive(Debug, Clone, Deserialize, Serialize)]
    72	pub struct AgentSpec {
    73	    #[serde(default = "default_version")]
    74	    pub version: u32,
    75	    pub harness: String,
    76	    pub model: String,
    77	    /// Model provider for open harnesses (pi); defaults to "openrouter".
    78	    pub provider: Option<String>,
    79	    /// "subscription" (operator identity: claude/codex OAuth) or "api"
    80	    /// (hermetic: only declared secrets cross the exec boundary).
    81	    /// Defaults by harness: claude/codex → subscription, pi → api.
    82	    pub auth: Option<String>,
    83	    pub bin: Option<String>,
    84	    #[serde(default)]
    85	    pub args: Vec<String>,
    86	    /// Env var names resolved from the plane's environment at dispatch and
    87	    /// passed per-exec; values are never persisted (on disk or remotely).
    88	    #[serde(default)]
    89	    pub secrets: Vec<String>,
    90	}
    91	
    92	/// How an agent authenticates — the policy hinge. Subscription agents act
    93	/// as the operator and may only run dispatch (manual) work; api agents are
    94	/// hermetic and are the only class allowed on reflex (webhook/cron) work.
    95	#[derive(Debug, Clone, Copy, PartialEq, Eq)]
    96	pub enum AuthClass {
    97	    Subscription,
    98	    Api,
    99	}
   100	
   101	impl AgentSpec {
   102	    pub fn auth_class(&self) -> Result<AuthClass> {
   103	        match self.auth.as_deref() {
   104	            Some("subscription") => Ok(AuthClass::Subscription),
   105	            Some("api") => Ok(AuthClass::Api),
   106	            Some(other) => bail!("unknown auth '{other}' (known: subscription, api)"),
   107	            None => Ok(match self.harness.as_str() {
   108	                "claude" | "codex" => AuthClass::Subscription,
   109	                _ => AuthClass::Api,
   110	            }),
   111	        }
   112	    }
   113	
   114	    pub fn provider(&self) -> &str {
   115	        self.provider.as_deref().unwrap_or("openrouter")
   116	    }
   117	}
   118	
   119	fn default_version() -> u32 {
   120	    1
   121	}
   122	
   123	#[derive(Debug, Clone, Deserialize)]
   124	pub struct TaskSpec {
   125	    pub agent: String,
   126	    #[serde(default = "default_substrate")]
   127	    pub substrate: String,
   128	    #[serde(default)]
   129	    pub workspace: WorkspaceSpec,
   130	    #[serde(default)]
   131	    pub budget: TaskBudget,
   132	    #[serde(default, rename = "trigger")]
   133	    pub triggers: Vec<TriggerSpec>,
   134	    pub pre_command: Option<String>,
   135	    pub post_command: Option<String>,
   136	}
   137	
   138	fn default_substrate() -> String {
   139	    "local".to_string()
   140	}
   141	
   142	#[derive(Debug, Clone, Deserialize, Default)]
   143	pub struct WorkspaceSpec {
   144	    /// Substrate resource identity: the host-lease key. Local substrate
   145	    /// defaults to "local"; sprites substrate requires the sprite name.
   146	    pub host: Option<String>,
   147	    #[serde(default)]
   148	    pub repos: Vec<RepoSpec>,
   149	    /// Sprite checkpoint to restore during prepare (sprites substrate).
   150	    pub checkpoint: Option<String>,
   151	}
   152	
   153	#[derive(Debug, Clone, Deserialize, Serialize)]
   154	pub struct RepoSpec {
   155	    pub url: String,
   156	    #[serde(default = "default_ref")]
   157	    pub r#ref: String,
   158	}
   159	
   160	fn default_ref() -> String {
   161	    "master".to_string()
   162	}
   163	
   164	#[derive(Debug, Clone, Deserialize, Default)]
   165	pub struct TaskBudget {
   166	    pub timeout_minutes: Option<u64>,
   167	    pub max_runs_per_day: Option<u32>,
   168	    pub max_cost_per_run_usd: Option<f64>,
   169	    pub turn_cap: Option<u32>,
   170	}
   171	
   172	#[derive(Debug, Clone, Deserialize)]
   173	#[serde(tag = "kind", rename_all = "snake_case")]
   174	pub enum TriggerSpec {
   175	    Manual,
   176	    Cron {
   177	        schedule: String,
   178	    },
   179	    Webhook {
   180	        route: String,
   181	        secret_env: String,
   182	        /// Dedupe key derivation: "header:<Name>" or "json:<pointer>".
   183	        dedupe_key: Option<String>,
   184	        /// Payload conditions, ANDed; a delivery failing any is
   185	        /// acknowledged but never becomes a run. Fail-closed: a missing
   186	        /// pointer rejects. Workload-agnostic containment — repo
   187	        /// allowlists, action filters, size caps — lives here, not in
   188	        /// card prose an agent may or may not honor.
   189	        #[serde(default)]
   190	        filter: Vec<FilterSpec>,
   191	    },
   192	}
   193	
   194	/// One payload condition: an RFC 6901 pointer plus exactly one predicate.
   195	#[derive(Debug, Clone, Deserialize)]
   196	pub struct FilterSpec {
   197	    pub pointer: String,
   198	    pub equals: Option<serde_json::Value>,
   199	    pub any_of: Option<Vec<serde_json::Value>>,
   200	    /// Numeric ceiling (inclusive): e.g. cap PR additions.
   201	    pub max: Option<f64>,
   202	}
   203	
   204	impl FilterSpec {
   205	    fn validate(&self) -> Result<()> {
   206	        let n = [
   207	            self.equals.is_some(),
   208	            self.any_of.is_some(),
   209	            self.max.is_some(),
   210	        ]
   211	        .iter()
   212	        .filter(|b| **b)
   213	        .count();
   214	        if n != 1 {
   215	            bail!(
   216	                "filter on '{}' needs exactly one of equals / any_of / max",
   217	                self.pointer
   218	            );
   219	        }
   220	        Ok(())
   221	    }
   222	
   223	    /// `Some(reason)` when the payload fails this condition.
   224	    pub fn reject_reason(&self, payload: &serde_json::Value) -> Option<String> {
   225	        let Some(found) = payload.pointer(&self.pointer) else {
   226	            return Some(format!("pointer '{}' missing from payload", self.pointer));
   227	        };
   228	        if let Some(expected) = &self.equals {
   229	            if found != expected {
   230	                return Some(format!("'{}' is {found}, want {expected}", self.pointer));
   231	            }
   232	        }
   233	        if let Some(allowed) = &self.any_of {
   234	            if !allowed.contains(found) {
   235	                return Some(format!("'{}' is {found}, not in allowlist", self.pointer));
   236	            }
   237	        }
   238	        if let Some(max) = self.max {
   239	            match found.as_f64() {
   240	                Some(v) if v <= max => {}
   241	                Some(v) => return Some(format!("'{}' is {v}, max {max}", self.pointer)),
   242	                None => return Some(format!("'{}' is not numeric", self.pointer)),
   243	            }
   244	        }
   245	        None
   246	    }
   247	}
   248	
   249	/// A fully loaded task: spec + lane card + resolved agent.
   250	#[derive(Debug, Clone)]
   251	pub struct Task {
   252	    pub name: String,
   253	    pub spec: TaskSpec,
   254	    pub card: String,
   255	    pub agent_name: String,
   256	    pub agent: AgentSpec,
   257	}
   258	
   259	impl Task {
   260	    pub fn host(&self) -> String {
   261	        self.spec
   262	            .workspace
   263	            .host
   264	            .clone()
   265	            .unwrap_or_else(|| "local".to_string())
   266	    }
   267	}
   268	
   269	/// The plane's config directory, loaded eagerly so bad config fails fast.
   270	#[derive(Debug, Clone)]
   271	pub struct Plane {
   272	    pub root: PathBuf,
   273	    pub spec: PlaneSpec,
   274	    pub agents: BTreeMap<String, AgentSpec>,
   275	    pub tasks: BTreeMap<String, Task>,
   276	}
   277	
   278	impl Plane {
   279	    /// Load from `root` (a directory containing plane.toml). Missing
   280	    /// plane.toml is allowed — defaults apply — but a missing agent
   281	    /// referenced by a task is an error.
   282	    pub fn load(root: &Path) -> Result<Self> {
   283	        // Absolute root: artifact paths cross process-cwd boundaries (the
   284	        // sprite relay runs from $HOME), so relative roots corrupt uploads.
   285	        let root = &root
   286	            .canonicalize()
   287	            .with_context(|| format!("plane root {}", root.display()))?;
   288	        let plane_path = root.join("plane.toml");
   289	        let spec: PlaneSpec = if plane_path.exists() {
   290	            toml::from_str(&std::fs::read_to_string(&plane_path)?)
   291	                .with_context(|| format!("parse {}", plane_path.display()))?
   292	        } else {
   293	            PlaneSpec::default()
   294	        };
   295	
   296	        let mut agents = BTreeMap::new();
   297	        let agents_dir = root.join("agents");
   298	        if agents_dir.is_dir() {
   299	            for entry in std::fs::read_dir(&agents_dir)? {
   300	                let path = entry?.path();
   301	                if path.extension().and_then(|e| e.to_str()) != Some("toml") {
   302	                    continue;
   303	                }
   304	                let name = path
   305	                    .file_stem()
   306	                    .and_then(|s| s.to_str())
   307	                    .context("agent file name")?
   308	                    .to_string();
   309	                let agent: AgentSpec = toml::from_str(&std::fs::read_to_string(&path)?)
   310	                    .with_context(|| format!("parse {}", path.display()))?;
   311	                agents.insert(name, agent);
   312	            }
   313	        }
   314	
   315	        let mut tasks = BTreeMap::new();
   316	        let tasks_dir = root.join("tasks");
   317	        if tasks_dir.is_dir() {
   318	            for entry in std::fs::read_dir(&tasks_dir)? {
   319	                let dir = entry?.path();
   320	                if !dir.is_dir() {
   321	                    continue;
   322	                }
   323	                let name = dir
   324	                    .file_name()
   325	                    .and_then(|s| s.to_str())
   326	                    .context("task dir name")?
   327	                    .to_string();
   328	                let spec_path = dir.join("task.toml");
   329	                if !spec_path.exists() {
   330	                    continue;
   331	                }
   332	                let task_spec: TaskSpec = toml::from_str(&std::fs::read_to_string(&spec_path)?)
   333	                    .with_context(|| format!("parse {}", spec_path.display()))?;
   334	                let card_path = dir.join("card.md");
   335	                let card = std::fs::read_to_string(&card_path)
   336	                    .with_context(|| format!("read {}", card_path.display()))?;
   337	                let agent = agents
   338	                    .get(&task_spec.agent)
   339	                    .with_context(|| {
   340	                        format!("task '{name}' binds unknown agent '{}'", task_spec.agent)
   341	                    })?
   342	                    .clone();
   343	                if task_spec.substrate == "sprites" && task_spec.workspace.host.is_none() {
   344	                    bail!("task '{name}': substrate = \"sprites\" requires workspace.host");
   345	                }
   346	                if task_spec.substrate == "local" && !spec.dev {
   347	                    bail!(
   348	                        "task '{name}': the local substrate is dev/test machinery — \
   349	                         production planes dispatch to a remote substrate (sprites). \
   350	                         Set `dev = true` in plane.toml only for a development plane."
   351	                    );
   352	                }
   353	                tasks.insert(
   354	                    name.clone(),
   355	                    Task {
   356	                        name,
   357	                        agent_name: task_spec.agent.clone(),
   358	                        agent,
   359	                        spec: task_spec,
   360	                        card,
   361	                    },
   362	                );
   363	            }
   364	        }
   365	
   366	        // Model & auth policy is code, not intent: violations fail at load
   367	        // (and therefore at `bb check`), never at first dispatch.
   368	        for (name, agent) in &agents {
   369	            let auth = agent
   370	                .auth_class()
   371	                .with_context(|| format!("agent '{name}'"))?;
   372	            match (agent.harness.as_str(), auth) {
   373	                ("claude" | "codex", AuthClass::Api) => bail!(
   374	                    "agent '{name}': {} runs on subscription auth only — \
   375	                     Anthropic/OpenAI API keys are forbidden on this plane",
   376	                    agent.harness
   377	                ),
   378	                ("pi", AuthClass::Subscription) => {
   379	                    bail!("agent '{name}': pi has no subscription auth; use auth = \"api\"")
   380	                }
   381	                _ => {}
   382	            }
   383	            for secret in &agent.secrets {
   384	                if secret == "ANTHROPIC_API_KEY" || secret == "OPENAI_API_KEY" {
   385	                    bail!(
   386	                        "agent '{name}': secret '{secret}' is forbidden — \
   387	                         claude/codex run on subscription auth, never API keys"
   388	                    );
   389	                }
   390	            }
   391	        }
   392	
   393	        // Bad trigger config fails at load, not at first delivery.
   394	        let mut routes = std::collections::BTreeSet::new();
   395	        for task in tasks.values() {
   396	            let auth = task
   397	                .agent
   398	                .auth_class()
   399	                .with_context(|| format!("task '{}'", task.name))?;
   400	            let reflex = task
   401	                .spec
   402	                .triggers
   403	                .iter()
   404	                .any(|t| !matches!(t, TriggerSpec::Manual));
   405	            if reflex && auth == AuthClass::Subscription {
   406	                bail!(
   407	                    "task '{}': reflex triggers (webhook/cron) require an auth = \"api\" \
   408	                     agent on an open harness — subscription agents ({}) run dispatch \
   409	                     (manual) work only",
   410	                    task.name,
   411	                    task.agent_name
   412	                );
   413	            }
   414	            for trigger in &task.spec.triggers {
   415	                match trigger {
   416	                    TriggerSpec::Webhook { route, filter, .. } => {
   417	                        if !routes.insert(route.clone()) {
   418	                            bail!("webhook route '{route}' declared by more than one trigger");
   419	                        }
   420	                        for f in filter {
   421	                            f.validate()
   422	                                .with_context(|| format!("task '{}'", task.name))?;
   423	                        }
   424	                    }
   425	                    TriggerSpec::Cron { schedule } => {
   426	                        crate::ingress::parse_schedule(schedule)
   427	                            .with_context(|| format!("task '{}': bad cron trigger", task.name))?;
   428	                    }
   429	                    TriggerSpec::Manual => {}
   430	                }
   431	            }
   432	        }
   433	
   434	        Ok(Self {
   435	            root: root.to_path_buf(),
   436	            spec,
   437	            agents,
   438	            tasks,
   439	        })
   440	    }

 succeeded in 200ms:
     1	//! Harness adapters: build the CLI invocation for an agent binding and
     2	//! parse its output into result + cost/token stats. Harness ids and flags
     3	//! live here and nowhere else; adding a harness is one adapter, not a
     4	//! schema change.
     5	
     6	use anyhow::{bail, Context, Result};
     7	use serde_json::Value;
     8	
     9	use crate::ledger::AttemptStats;
    10	use crate::spec::{AgentSpec, TaskBudget};
    11	
    12	#[derive(Debug)]
    13	pub struct ParsedOutput {
    14	    pub result: String,
    15	    pub stats: AttemptStats,
    16	}
    17	
    18	pub const HARNESSES: &[&str] = &["claude", "codex", "pi"];
    19	
    20	/// The lane card is passed on stdin; the command must read its prompt from
    21	/// stdin so card size never hits argv limits.
    22	pub fn build_command(agent: &AgentSpec, budget: &TaskBudget) -> Result<Vec<String>> {
    23	    let bin = agent.bin.clone().unwrap_or_else(|| agent.harness.clone());
    24	    let mut cmd = match agent.harness.as_str() {
    25	        "claude" => {
    26	            let mut c = vec![
    27	                bin,
    28	                "-p".into(),
    29	                "--output-format".into(),
    30	                "json".into(),
    31	                "--model".into(),
    32	                agent.model.clone(),
    33	                "--dangerously-skip-permissions".into(),
    34	            ];
    35	            if let Some(turns) = budget.turn_cap {
    36	                c.push("--max-turns".into());
    37	                c.push(turns.to_string());
    38	            }
    39	            c
    40	        }
    41	        "codex" => vec![
    42	            bin,
    43	            "exec".into(),
    44	            "--json".into(),
    45	            "--dangerously-bypass-approvals-and-sandbox".into(),
    46	            "--model".into(),
    47	            agent.model.clone(),
    48	            "-".into(),
    49	        ],
    50	        "pi" => {
    51	            // pi --mode json re-streams the entire partial message on every
    52	            // token (`message_update`): a 15-minute run produced 718 MB of
    53	            // stdout (measured live 2026-06-10). Drop those deltas before
    54	            // they hit the capture file; everything the parser needs
    55	            // (message_end, turn_end) passes through. The pipeline makes
    56	            // grep's exit status the command's — pi crashes still surface
    57	            // because a stream with no assistant message_end fails parsing.
    58	            let mut inner = vec![
    59	                bin,
    60	                "--provider".into(),
    61	                agent.provider().into(),
    62	                "--model".into(),
    63	                agent.model.clone(),
    64	                "--no-session".into(),
    65	                "--mode".into(),
    66	                "json".into(),
    67	                "-p".into(),
    68	            ];
    69	            inner.extend(agent.args.iter().cloned());
    70	            let quoted: Vec<String> = inner
    71	                .iter()
    72	                .map(|a| crate::substrate::local::shell_quote(a))
    73	                .collect();
    74	            return Ok(vec![
    75	                "sh".into(),
    76	                "-c".into(),
    77	                format!(
    78	                    "{} | grep -v -F '\"type\":\"message_update\"'",
    79	                    quoted.join(" ")
    80	                ),
    81	                "sh".into(),
    82	            ]);
    83	        }
    84	        other => bail!(
    85	            "unknown harness '{other}' (known: {})",
    86	            HARNESSES.join(", ")
    87	        ),
    88	    };
    89	    cmd.extend(agent.args.iter().cloned());
    90	    Ok(cmd)
    91	}
    92	
    93	/// Unparseable output is an error — the attempt fails with raw output
    94	/// preserved. Never a silent zero-cost success.
    95	pub fn parse_output(harness: &str, stdout: &str) -> Result<ParsedOutput> {
    96	    match harness {
    97	        "claude" => parse_claude(stdout),
    98	        "codex" => parse_codex(stdout),
    99	        "pi" => parse_pi(stdout),
   100	        other => bail!("unknown harness '{other}'"),
   101	    }
   102	}
   103	
   104	/// `claude -p --output-format json` emits one JSON object:
   105	/// `{"type":"result","result":...,"total_cost_usd":...,"num_turns":...,"usage":{...}}`
   106	fn parse_claude(stdout: &str) -> Result<ParsedOutput> {
   107	    let v = last_json_object(stdout).context("claude output: no JSON object found")?;
   108	    if v.get("is_error").and_then(Value::as_bool) == Some(true) {
   109	        bail!("claude reported is_error=true");
   110	    }
   111	    let result = v
   112	        .get("result")
   113	        .and_then(Value::as_str)
   114	        .context("claude output: result object has no 'result' string")?
   115	        .to_string();
   116	    if result.trim().is_empty() {
   117	        bail!("claude output: empty result");
   118	    }
   119	    let usage = v.get("usage").cloned().unwrap_or(Value::Null);
   120	    Ok(ParsedOutput {
   121	        result,
   122	        stats: AttemptStats {
   123	            tokens_in: usage.get("input_tokens").and_then(Value::as_i64),
   124	            tokens_out: usage.get("output_tokens").and_then(Value::as_i64),
   125	            turns: v.get("num_turns").and_then(Value::as_i64),
   126	            cost_usd: v.get("total_cost_usd").and_then(Value::as_f64),
   127	        },
   128	    })
   129	}
   130	
   131	/// `codex exec --json` emits JSONL events; token usage arrives on
   132	/// `turn.completed`, the answer on `item.completed` agent messages.
   133	fn parse_codex(stdout: &str) -> Result<ParsedOutput> {
   134	    let mut tokens_in: i64 = 0;
   135	    let mut tokens_out: i64 = 0;
   136	    let mut turns: i64 = 0;
   137	    let mut result = String::new();
   138	    let mut saw_event = false;
   139	    for line in stdout.lines() {
   140	        let Ok(v) = serde_json::from_str::<Value>(line) else {
   141	            continue;
   142	        };
   143	        saw_event = true;
   144	        match v.get("type").and_then(Value::as_str) {
   145	            Some("turn.completed") => {
   146	                turns += 1;
   147	                if let Some(u) = v.get("usage") {
   148	                    tokens_in += u.get("input_tokens").and_then(Value::as_i64).unwrap_or(0);
   149	                    tokens_out += u.get("output_tokens").and_then(Value::as_i64).unwrap_or(0);
   150	                }
   151	            }
   152	            Some("item.completed") => {
   153	                if let Some(item) = v.get("item") {
   154	                    if item.get("type").and_then(Value::as_str) == Some("agent_message") {
   155	                        if let Some(text) = item.get("text").and_then(Value::as_str) {
   156	                            result = text.to_string();
   157	                        }
   158	                    }
   159	                }
   160	            }
   161	            _ => {}
   162	        }
   163	    }
   164	    if !saw_event {
   165	        bail!("codex output: no JSONL events found");
   166	    }
   167	    if result.is_empty() {
   168	        bail!("codex output: events present but no agent_message — incomplete run");
   169	    }
   170	    Ok(ParsedOutput {
   171	        result,
   172	        stats: AttemptStats {
   173	            tokens_in: Some(tokens_in),
   174	            tokens_out: Some(tokens_out),
   175	            turns: Some(turns),
   176	            // codex does not report dollar cost; unknown, not zero.
   177	            cost_usd: None,
   178	        },
   179	    })
   180	}
   181	
   182	/// pi `--mode json` emits JSONL events (verified live 2026-06-10 against
   183	/// pi via OpenRouter): the final assistant message rides on `message_end`
   184	/// with `message.content[]` text items and `message.usage`
   185	/// `{input, output, cost: {total}}`; turns close with `turn_end`.
   186	fn parse_pi(stdout: &str) -> Result<ParsedOutput> {
   187	    let mut turns: i64 = 0;
   188	    let mut last_message: Option<Value> = None;
   189	    let mut saw_event = false;
   190	    // Each assistant message_end carries that API call's usage, not a
   191	    // running total — cost and tokens must sum across the run (verified
   192	    // live 2026-06-10: a 12-minute review reported only its final
   193	    // message's cost until this summed).
   194	    let (mut tokens_in, mut tokens_out, mut cost) = (0i64, 0i64, 0f64);
   195	    let mut saw_usage = false;
   196	    for line in stdout.lines() {
   197	        let Ok(v) = serde_json::from_str::<Value>(line) else {
   198	            continue;
   199	        };
   200	        saw_event = true;
   201	        match v.get("type").and_then(Value::as_str) {
   202	            Some("turn_end") => turns += 1,
   203	            Some("message_end") => {
   204	                if let Some(m) = v.get("message") {
   205	                    if m.get("role").and_then(Value::as_str) == Some("assistant") {
   206	                        if let Some(u) = m.get("usage") {
   207	                            saw_usage = true;
   208	                            tokens_in += u.get("input").and_then(Value::as_i64).unwrap_or(0);
   209	                            tokens_out += u.get("output").and_then(Value::as_i64).unwrap_or(0);
   210	                            cost += u
   211	                                .get("cost")
   212	                                .and_then(|c| c.get("total"))
   213	                                .and_then(Value::as_f64)
   214	                                .unwrap_or(0.0);
   215	                        }
   216	                        last_message = Some(m.clone());
   217	                    }
   218	                }
   219	            }
   220	            _ => {}
   221	        }
   222	    }
   223	    if !saw_event {
   224	        bail!("pi output: no JSONL events found");
   225	    }
   226	    let message = last_message.context("pi output: no assistant message_end — incomplete run")?;
   227	    let result: String = message
   228	        .get("content")
   229	        .and_then(Value::as_array)
   230	        .map(|items| {
   231	            items
   232	                .iter()
   233	                .filter(|c| c.get("type").and_then(Value::as_str) == Some("text"))
   234	                .filter_map(|c| c.get("text").and_then(Value::as_str))
   235	                .collect::<Vec<_>>()
   236	                .join("\n")
   237	        })
   238	        .unwrap_or_default();
   239	    let result = result.trim().to_string();
   240	    if result.is_empty() {
   241	        bail!("pi output: assistant message has no text content");
   242	    }
   243	    Ok(ParsedOutput {
   244	        result,
   245	        stats: AttemptStats {
   246	            tokens_in: saw_usage.then_some(tokens_in),
   247	            tokens_out: saw_usage.then_some(tokens_out),
   248	            turns: Some(turns),
   249	            cost_usd: saw_usage.then_some(cost),
   250	        },
   251	    })
   252	}
   253	
   254	/// Find the last parseable JSON object in output (harnesses may print
   255	/// noise before the final result object).
   256	fn last_json_object(stdout: &str) -> Option<Value> {
   257	    fn result_from_array(v: Value) -> Option<Value> {
   258	        match v {
   259	            Value::Array(items) => items
   260	                .iter()
   261	                .rev()
   262	                .find(|v| v.get("type").and_then(Value::as_str) == Some("result"))
   263	                .cloned(),
   264	            other => Some(other),
   265	        }
   266	    }
   267	    for line in stdout.lines().rev() {
   268	        let line = line.trim();
   269	        if line.starts_with('{') {
   270	            if let Ok(v) = serde_json::from_str::<Value>(line) {
   271	                return Some(v);
   272	            }
   273	        }
   274	        // claude (mid-2026) wraps the transcript in a JSON array whose
   275	        // `type: "result"` element carries the verdict and usage.
   276	        if line.starts_with('[') {
   277	            if let Ok(v) = serde_json::from_str::<Value>(line) {
   278	                if let Some(result) = result_from_array(v) {
   279	                    return Some(result);
   280	                }
   281	            }
   282	        }
   283	    }
   284	    serde_json::from_str::<Value>(stdout.trim())
   285	        .ok()
   286	        .and_then(result_from_array)
   287	}
   288	
   289	#[cfg(test)]
   290	mod tests {
   291	    use super::*;
   292	
   293	    #[test]
   294	    fn parse_claude_bare_object() {
   295	        let out = r#"{"type":"result","result":"done","total_cost_usd":0.01,"num_turns":2,"usage":{"input_tokens":5,"output_tokens":3}}"#;
   296	        let parsed = parse_output("claude", out).unwrap();
   297	        assert_eq!(parsed.result, "done");
   298	        assert_eq!(parsed.stats.cost_usd, Some(0.01));
   299	    }
   300	
   301	    #[test]
   302	    fn parse_claude_transcript_array() {
   303	        // claude ≥ mid-2026 wraps the transcript in a JSON array; the
   304	        // `type: "result"` element carries verdict + usage.
   305	        let out = r#"[{"type":"system","subtype":"init"},{"type":"assistant","message":"..."},{"type":"result","subtype":"success","result":"review posted","total_cost_usd":2.0459,"num_turns":12,"usage":{"input_tokens":9216,"output_tokens":6598}}]"#;
   306	        let parsed = parse_output("claude", out).unwrap();
   307	        assert_eq!(parsed.result, "review posted");
   308	        assert_eq!(parsed.stats.cost_usd, Some(2.0459));
   309	        assert_eq!(parsed.stats.tokens_out, Some(6598));
   310	    }
   311	
   312	    #[test]
   313	    fn pi_command_carries_provider_and_model() {
   314	        let agent: crate::spec::AgentSpec =
   315	            toml::from_str("harness = \"pi\"\nmodel = \"moonshotai/kimi-k2.6\"\n").unwrap();
   316	        let cmd = build_command(&agent, &crate::spec::TaskBudget::default()).unwrap();
   317	        let joined = cmd.join(" ");
   318	        assert!(joined.contains("--provider' 'openrouter"), "{joined}");
   319	        assert!(joined.contains("moonshotai/kimi-k2.6"), "{joined}");
   320	        assert!(joined.contains("--no-session"), "{joined}");
   321	        // The message_update flood filter wraps the invocation.
   322	        assert_eq!(cmd[0], "sh");
   323	        assert!(joined.contains("grep -v -F"), "{joined}");
   324	    }
   325	
   326	    #[test]
   327	    fn parse_pi_jsonl_events_sums_usage_across_messages() {
   328	        // Condensed from a live pi run via OpenRouter (2026-06-10). Each
   329	        // assistant message_end carries per-call usage; totals must sum.
   330	        let out = concat!(
   331	            r#"{"type":"message_update","assistantMessageEvent":{"type":"text_delta"}}"#,
   332	            "\n",
   333	            r#"{"type":"message_end","message":{"role":"assistant","content":[{"type":"text","text":"thinking about it"},{"type":"toolCall","name":"bash"}],"usage":{"input":1000,"output":50,"cost":{"total":0.001}}}}"#,
   334	            "\n",
   335	            r#"{"type":"turn_end","message":{"role":"assistant"}}"#,
   336	            "\n",
   337	            r#"{"type":"message_end","message":{"role":"assistant","content":[{"type":"thinking","thinking":"..."},{"type":"text","text":" BB-PI-OK"}],"usage":{"input":1117,"output":21,"totalTokens":1138,"cost":{"input":0.0007,"output":0.00007,"total":0.0008}}}}"#,
   338	            "\n",
   339	            r#"{"type":"turn_end","message":{"role":"assistant"}}"#,
   340	            "\n",
   341	            r#"{"type":"agent_end","messages":[]}"#,
   342	        );
   343	        let parsed = parse_output("pi", out).unwrap();
   344	        assert_eq!(parsed.result, "BB-PI-OK");
   345	        assert_eq!(parsed.stats.tokens_in, Some(2117));
   346	        assert_eq!(parsed.stats.tokens_out, Some(71));
   347	        assert_eq!(parsed.stats.turns, Some(2));
   348	        assert_eq!(parsed.stats.cost_usd, Some(0.0018));
   349	    }
   350	
   351	    #[test]
   352	    fn parse_pi_rejects_incomplete_runs() {
   353	        assert!(parse_output("pi", "not json").is_err());
   354	        // Events but no assistant message_end: incomplete.
   355	        assert!(parse_output("pi", r#"{"type":"message_update"}"#).is_err());
   356	        // Assistant message with no text content.
   357	        let no_text = r#"{"type":"message_end","message":{"role":"assistant","content":[{"type":"thinking","thinking":"x"}],"usage":{}}}"#;
   358	        assert!(parse_output("pi", no_text).is_err());
   359	    }
   360	
   361	    #[test]
   362	    fn parse_claude_rejects_empty_and_error() {
   363	        assert!(parse_output("claude", r#"{"type":"result","result":""}"#).is_err());
   364	        assert!(parse_output(
   365	            "claude",
   366	            r#"{"type":"result","result":"x","is_error":true}"#
   367	        )
   368	        .is_err());
   369	        assert!(parse_output("claude", "not json at all").is_err());
   370	    }
   371	}

codex
I’ve got enough context for the changed load-time policy, local exec path, and read API. I’m checking the sprite execution path now because the oracle’s hermetic guarantee applies to api-auth agents generally, not just the local test substrate.
exec
/bin/zsh -lc "nl -ba src/substrate/sprites.rs | sed -n '1,280p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "rg -n \"secrets|post_command|pre_command|execute\\(|WorkspacePlan|run_with_timeout|env_clear|read_authorized|query_param|list_runs|run\\(\" src tests" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba src/ingress.rs | sed -n '1,260p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba src/dispatch.rs | sed -n '1,340p'" in /Users/phaedrus/Development/bitterblossom
 succeeded in 149ms:
     1	//! Fly Sprites substrate: shell out to the `sprite` CLI, which talks
     2	//! WebSocket exec (never `--http-post`; cold sprites 502 on HTTP POST —
     3	//! earned in this repo). Workspaces persist on the sprite overlay between
     4	//! wakes: repos sync with fetch + hard-reset instead of re-cloning.
     5	
     6	use std::path::{Path, PathBuf};
     7	use std::time::Duration;
     8	
     9	use anyhow::{bail, Context, Result};
    10	
    11	use super::local::{run_with_timeout, shell_quote};
    12	use super::{ExecResult, ProbeResult, Session, Substrate, WorkspacePlan, CARD_FILENAME};
    13	
    14	/// Override the sprite binary (tests point this at a stub; the real CLI is
    15	/// an external network boundary).
    16	pub const SPRITE_BIN_ENV: &str = "BB_SPRITE_BIN";
    17	
    18	fn sprite_bin() -> String {
    19	    std::env::var(SPRITE_BIN_ENV).unwrap_or_else(|_| "sprite".to_string())
    20	}
    21	
    22	/// `-o org -s name` for a `org/name` host, `-s host` otherwise.
    23	fn selector_args(host: &str) -> Vec<String> {
    24	    match host.split_once('/') {
    25	        Some((org, name)) => vec!["-o".into(), org.into(), "-s".into(), name.into()],
    26	        None => vec!["-s".into(), host.into()],
    27	    }
    28	}
    29	
    30	/// The sprite CLI resolves its org from cwd path history; a temp-dir cwd
    31	/// picks the wrong org. Run the relay from $HOME, where the operator's
    32	/// default resolution lives.
    33	fn relay_cwd() -> PathBuf {
    34	    std::env::var_os("HOME")
    35	        .map(PathBuf::from)
    36	        .unwrap_or_else(std::env::temp_dir)
    37	}
    38	
    39	pub struct SpritesSubstrate;
    40	
    41	impl Substrate for SpritesSubstrate {
    42	    fn name(&self) -> &'static str {
    43	        "sprites"
    44	    }
    45	
    46	    fn acquire(&self, host: &str, attempt_dir: &Path) -> Result<Box<dyn Session>> {
    47	        std::fs::create_dir_all(attempt_dir)?;
    48	        let session = SpriteSession {
    49	            host: host.to_string(),
    50	            artifacts: attempt_dir.to_path_buf(),
    51	            workspace: String::new(),
    52	            marker: String::new(),
    53	            secrets: Vec::new(),
    54	        };
    55	        // Wake-on-exec: a cold sprite takes a few seconds to wake.
    56	        let out = session.sprite_exec(&["true".into()], None, Duration::from_secs(120))?;
    57	        if out.exit_code != 0 {
    58	            bail!(
    59	                "sprite '{host}' unreachable: {}",
    60	                out.stderr.trim().lines().last().unwrap_or("")
    61	            );
    62	        }
    63	        Ok(Box::new(session))
    64	    }
    65	
    66	    fn probe(&self, host: &str, _attempt_dir: &Path, marker: &str) -> ProbeResult {
    67	        let script = format!(
    68	            "[ -f /tmp/{marker}.pid ] || exit 3; \
    69	             kill -0 \"$(cat /tmp/{marker}.pid)\" 2>/dev/null && exit 0 || exit 4"
    70	        );
    71	        let mut cmd = vec![sprite_bin(), "exec".into()];
    72	        cmd.extend(selector_args(host));
    73	        cmd.extend(["--".into(), "sh".into(), "-c".into(), script]);
    74	        match run_with_timeout(
    75	            &cmd,
    76	            None,
    77	            &relay_cwd(),
    78	            &[],
    79	            false,
    80	            None,
    81	            Duration::from_secs(60),
    82	        ) {
    83	            Ok(out) => match out.exit_code {
    84	                0 => ProbeResult::Alive,
    85	                4 => ProbeResult::Dead,
    86	                3 => ProbeResult::Unknown(format!("no pidfile /tmp/{marker}.pid on {host}")),
    87	                code => ProbeResult::Unknown(format!(
    88	                    "probe exit {code}: {}",
    89	                    out.stderr.trim().lines().last().unwrap_or("")
    90	                )),
    91	            },
    92	            Err(e) => ProbeResult::Unknown(format!("probe failed: {e:#}")),
    93	        }
    94	    }
    95	}
    96	
    97	pub struct SpriteSession {
    98	    host: String,
    99	    artifacts: PathBuf,
   100	    workspace: String,
   101	    marker: String,
   102	    secrets: Vec<(String, String)>,
   103	}
   104	
   105	impl SpriteSession {
   106	    /// Run a command on the sprite via the CLI relay; extra_args go before
   107	    /// the remote command (e.g. --file uploads).
   108	    fn sprite_exec_with(
   109	        &self,
   110	        extra_args: &[String],
   111	        remote: &[String],
   112	        stdin: Option<&str>,
   113	        timeout: Duration,
   114	    ) -> Result<ExecResult> {
   115	        let mut cmd = vec![sprite_bin(), "exec".into()];
   116	        cmd.extend(selector_args(&self.host));
   117	        cmd.extend(extra_args.iter().cloned());
   118	        // The sprite CLI parses remote args as its own flags without this.
   119	        cmd.push("--".into());
   120	        cmd.extend(remote.iter().cloned());
   121	        run_with_timeout(&cmd, stdin, &relay_cwd(), &[], false, None, timeout)
   122	    }
   123	
   124	    fn sprite_exec(
   125	        &self,
   126	        remote: &[String],
   127	        stdin: Option<&str>,
   128	        timeout: Duration,
   129	    ) -> Result<ExecResult> {
   130	        self.sprite_exec_with(&[], remote, stdin, timeout)
   131	    }
   132	
   133	    fn remote_shell(&self, script: &str, timeout: Duration) -> Result<ExecResult> {
   134	        self.sprite_exec(&["sh".into(), "-c".into(), script.into()], None, timeout)
   135	    }
   136	}
   137	
   138	impl Session for SpriteSession {
   139	    fn prepare(&mut self, plan: &WorkspacePlan) -> Result<()> {
   140	        self.workspace = plan.remote_workspace.clone();
   141	        self.marker = plan.marker.clone();
   142	        self.secrets = plan.secrets.clone();
   143	
   144	        if let Some(checkpoint) = &plan.checkpoint {
   145	            let mut cmd = vec![sprite_bin(), "restore".into()];
   146	            cmd.extend(selector_args(&self.host));
   147	            cmd.push(checkpoint.clone());
   148	            let out = run_with_timeout(
   149	                &cmd,
   150	                None,
   151	                &relay_cwd(),
   152	                &[],
   153	                false,
   154	                None,
   155	                Duration::from_secs(300),
   156	            )?;
   157	            if out.exit_code != 0 {
   158	                bail!(
   159	                    "checkpoint restore {checkpoint} failed: {}",
   160	                    out.stderr.trim()
   161	                );
   162	            }
   163	        }
   164	
   165	        let ws = shell_quote(&self.workspace);
   166	        let out = self.remote_shell(&format!("mkdir -p {ws}"), Duration::from_secs(60))?;
   167	        if out.exit_code != 0 {
   168	            bail!("mkdir workspace failed: {}", out.stderr.trim());
   169	        }
   170	
   171	        for repo in &plan.repos {
   172	            let dir = repo_dir_name(&repo.url);
   173	            let dest = shell_quote(&format!("{}/{dir}", self.workspace));
   174	            let url = shell_quote(&repo.url);
   175	            let r#ref = shell_quote(&repo.r#ref);
   176	            // Overlay persists between wakes: fetch + hard-reset + clean on
   177	            // reuse beats re-clone.
   178	            let script = format!(
   179	                "if [ -d {dest}/.git ]; then \
   180	                   git -C {dest} fetch origin {ref_} --depth 1 && \
   181	                   git -C {dest} checkout -q FETCH_HEAD && \
   182	                   git -C {dest} reset --hard && git -C {dest} clean -fd; \
   183	                 else \
   184	                   git clone --depth 1 --branch {ref_} {url} {dest}; \
   185	                 fi",
   186	                dest = dest,
   187	                url = url,
   188	                ref_ = r#ref,
   189	            );
   190	            let out = self.remote_shell(&script, Duration::from_secs(600))?;
   191	            if out.exit_code != 0 {
   192	                bail!("repo sync {} failed: {}", repo.url, out.stderr.trim());
   193	            }
   194	        }
   195	
   196	        // Upload the lane card via --file (write locally, upload on exec).
   197	        let card_local = self.artifacts.join(CARD_FILENAME);
   198	        std::fs::write(&card_local, &plan.card)?;
   199	        let upload = format!(
   200	            "{}:{}/{CARD_FILENAME}",
   201	            card_local.display(),
   202	            self.workspace
   203	        );
   204	        let out = self.sprite_exec_with(
   205	            &["--file".into(), upload],
   206	            &["true".into()],
   207	            None,
   208	            Duration::from_secs(120),
   209	        )?;
   210	        if out.exit_code != 0 {
   211	            bail!("card upload failed: {}", out.stderr.trim());
   212	        }
   213	
   214	        if let Some(payload) = &plan.payload {
   215	            let event_local = self.artifacts.join(super::EVENT_FILENAME);
   216	            std::fs::write(&event_local, payload)?;
   217	            let upload = format!(
   218	                "{}:{}/{}",
   219	                event_local.display(),
   220	                self.workspace,
   221	                super::EVENT_FILENAME
   222	            );
   223	            let out = self.sprite_exec_with(
   224	                &["--file".into(), upload],
   225	                &["true".into()],
   226	                None,
   227	                Duration::from_secs(120),
   228	            )?;
   229	            if out.exit_code != 0 {
   230	                bail!("event payload upload failed: {}", out.stderr.trim());
   231	            }
   232	        }
   233	
   234	        if let Some(pre) = &plan.pre_command {
   235	            let script = format!("cd {ws} && {pre}");
   236	            let out = self.remote_shell(&script, Duration::from_secs(600))?;
   237	            if out.exit_code != 0 {
   238	                bail!("pre_command failed: {}", out.stderr.trim());
   239	            }
   240	        }
   241	        Ok(())
   242	    }
   243	
   244	    fn execute(
   245	        &mut self,
   246	        cmd: &[String],
   247	        stdin: Option<&str>,
   248	        timeout: Duration,
   249	    ) -> Result<ExecResult> {
   250	        // The whole script travels on stdin — secrets and prompt never
   251	        // appear in local or remote argv (argv is visible in process
   252	        // tables and CLI telemetry). The prompt reaches the harness via a
   253	        // heredoc with a per-attempt delimiter; the pidfile makes the
   254	        // attempt probeable and cancellable after a plane restart.
   255	        let exports: String = self
   256	            .secrets
   257	            .iter()
   258	            .map(|(k, v)| format!("export {k}={}\n", shell_quote(v)))
   259	            .collect();
   260	        let quoted: Vec<String> = cmd.iter().map(|c| shell_quote(c)).collect();
   261	        // Unguessable delimiter, re-rolled on the astronomically unlikely
   262	        // collision, so prompt content can never close the heredoc early
   263	        // and execute as shell.
   264	        let delimiter = loop {
   265	            let candidate = format!("BB_STDIN_EOF_{}", crate::ledger::new_id());
   266	            let collides = stdin
   267	                .map(|input| input.lines().any(|l| l.trim() == candidate))
   268	                .unwrap_or(false);
   269	            if !collides {
   270	                break candidate;
   271	            }
   272	        };
   273	        let body = match stdin {
   274	            Some(input) => format!(
   275	                "exec {cmd} <<'{delimiter}'\n{input}\n{delimiter}\n",
   276	                cmd = quoted.join(" "),
   277	            ),
   278	            None => format!("exec {} < /dev/null\n", quoted.join(" ")),
   279	        };
   280	        let script = format!(

 succeeded in 174ms:
     1	//! Dispatch: take a pending run through budget check, host lease,
     2	//! prepare, execute, collect, finalize. Mechanical — no judgment about
     3	//! what to fix or whether the agent did a good job.
     4	
     5	use std::path::PathBuf;
     6	use std::time::{Duration, Instant};
     7	
     8	use anyhow::{Context, Result};
     9	
    10	use crate::budget;
    11	use crate::harness;
    12	use crate::ledger::{AttemptStats, Ledger, RunRow};
    13	use crate::spec::{Plane, Task};
    14	use crate::substrate::{self, WorkspacePlan, CARD_FILENAME};
    15	
    16	/// Mechanical retries after the initial attempt, for pre-execute failures only.
    17	const MAX_RETRIES: i64 = 2;
    18	const DEFAULT_TIMEOUT_MINUTES: u64 = 60;
    19	const LEASE_WAIT: Duration = Duration::from_secs(60);
    20	const LEASE_POLL: Duration = Duration::from_millis(250);
    21	
    22	/// Outcome of one attempt, with the phase it died in.
    23	enum AttemptOutcome {
    24	    Success { stats: AttemptStats },
    25	    Failure { phase_executed: bool, error: String },
    26	}
    27	
    28	pub fn dispatch_run(plane: &Plane, ledger: &mut Ledger, run_id: &str) -> Result<RunRow> {
    29	    let run = ledger.run(run_id)?;
    30	    if run.state != "pending" {
    31	        // Replays and racing workers land here; never re-dispatch a run
    32	        // that already left pending.
    33	        return Ok(run);
    34	    }
    35	    let task = plane.task(&run.task)?;
    36	
    37	    if let Some(v) = budget::pre_dispatch_check(plane, ledger, task)? {
    38	        ledger.record_budget_event(Some(&task.name), v.kind, &v.detail)?;
    39	        if v.kind == "max_runs_per_day" {
    40	            ledger.park_task(&task.name, &v.detail)?;
    41	        }
    42	        ledger.transition(run_id, "blocked_budget", Some(&v.detail))?;
    43	        crate::notify::notify(
    44	            plane,
    45	            "budget_blocked",
    46	            &serde_json::json!({ "run_id": run_id, "task": task.name, "kind": v.kind, "detail": v.detail }),
    47	        );
    48	        return ledger.run(run_id);
    49	    }
    50	
    51	    // Atomic claim: if another worker (or another plane process) already
    52	    // moved this run out of pending, walk away without a second attempt.
    53	    if !ledger.try_transition(run_id, "running", None)? {
    54	        return ledger.run(run_id);
    55	    }
    56	    ledger.set_run_agent(run_id, &task.agent_name, task.agent.version)?;
    57	    let started = Instant::now();
    58	
    59	    let mut attempt_n = ledger.attempt_count(run_id)?;
    60	    loop {
    61	        attempt_n += 1;
    62	        let outcome = run_attempt(plane, ledger, run_id, task, attempt_n)?;
    63	        match outcome {
    64	            AttemptOutcome::Success { stats } => {
    65	                ledger.finalize_run(
    66	                    run_id,
    67	                    stats.cost_usd,
    68	                    started.elapsed().as_millis() as i64,
    69	                )?;
    70	                ledger.transition(run_id, "success", None)?;
    71	                if let Some(v) = budget::post_run_check(task, stats.cost_usd) {
    72	                    ledger.record_budget_event(Some(&task.name), v.kind, &v.detail)?;
    73	                    ledger.park_task(&task.name, &v.detail)?;
    74	                    crate::notify::notify(
    75	                        plane,
    76	                        "budget_breach_parked",
    77	                        &serde_json::json!({ "run_id": run_id, "task": task.name, "detail": v.detail }),
    78	                    );
    79	                }
    80	                break;
    81	            }
    82	            AttemptOutcome::Failure {
    83	                phase_executed: true,
    84	                error,
    85	            } => {
    86	                // The agent may have had external side effects; "re-run it"
    87	                // is not a recovery semantic. No mechanical retry.
    88	                ledger.finalize_run(run_id, None, started.elapsed().as_millis() as i64)?;
    89	                ledger.transition(run_id, "failure", Some(&error))?;
    90	                break;
    91	            }
    92	            AttemptOutcome::Failure {
    93	                phase_executed: false,
    94	                error,
    95	            } => {
    96	                if attempt_n - 1 < MAX_RETRIES {
    97	                    ledger.record_event(run_id, "retry", Some(&error))?;
    98	                    continue;
    99	                }
   100	                let payload = ledger.run_payload(run_id)?;
   101	                let dl =
   102	                    ledger.record_dead_letter(run_id, &task.name, payload.as_deref(), &error)?;
   103	                ledger.finalize_run(run_id, None, started.elapsed().as_millis() as i64)?;
   104	                ledger.transition(
   105	                    run_id,
   106	                    "failure",
   107	                    Some(&format!("dead_letter:{dl} {error}")),
   108	                )?;
   109	                crate::notify::notify(
   110	                    plane,
   111	                    "run_dead_lettered",
   112	                    &serde_json::json!({ "run_id": run_id, "task": task.name, "dead_letter": dl, "error": error }),
   113	                );
   114	                break;
   115	            }
   116	        }
   117	    }
   118	    ledger.run(run_id)
   119	}
   120	
   121	fn run_attempt(
   122	    plane: &Plane,
   123	    ledger: &Ledger,
   124	    run_id: &str,
   125	    task: &Task,
   126	    n: i64,
   127	) -> Result<AttemptOutcome> {
   128	    let host = task.host();
   129	    let lease_started = Instant::now();
   130	    loop {
   131	        if ledger.try_acquire_host_lease(&host, run_id)? {
   132	            break;
   133	        }
   134	        if lease_started.elapsed() >= LEASE_WAIT {
   135	            return Ok(AttemptOutcome::Failure {
   136	                phase_executed: false,
   137	                error: format!(
   138	                    "host '{host}' lease held by run {:?}",
   139	                    ledger.lease_holder(&host)?
   140	                ),
   141	            });
   142	        }
   143	        std::thread::sleep(LEASE_POLL);
   144	    }
   145	
   146	    let result = attempt_on_host(plane, ledger, run_id, task, n);
   147	    ledger.release_host_lease(&host, run_id)?;
   148	    result
   149	}
   150	
   151	fn attempt_on_host(
   152	    plane: &Plane,
   153	    ledger: &Ledger,
   154	    run_id: &str,
   155	    task: &Task,
   156	    n: i64,
   157	) -> Result<AttemptOutcome> {
   158	    let attempt_dir = attempt_dir(plane, run_id, n);
   159	    std::fs::create_dir_all(&attempt_dir)?;
   160	
   161	    let attempt_id = ledger.create_attempt(
   162	        run_id,
   163	        n,
   164	        &task.agent_name,
   165	        task.agent.version,
   166	        &task.agent.harness,
   167	        &task.agent.model,
   168	    )?;
   169	    let artifact_dir = attempt_dir.to_string_lossy().into_owned();
   170	
   171	    let fail = |phase_executed: bool, error: String| -> Result<AttemptOutcome> {
   172	        ledger.finish_attempt(
   173	            attempt_id,
   174	            "failure",
   175	            Some(&error),
   176	            None,
   177	            &AttemptStats::default(),
   178	            Some(&artifact_dir),
   179	        )?;
   180	        Ok(AttemptOutcome::Failure {
   181	            phase_executed,
   182	            error,
   183	        })
   184	    };
   185	
   186	    let substrate = match substrate::for_task(&task.spec.substrate) {
   187	        Ok(s) => s,
   188	        Err(e) => return fail(false, e.to_string()),
   189	    };
   190	    let mut session = match substrate.acquire(&task.host(), &attempt_dir) {
   191	        Ok(s) => s,
   192	        Err(e) => return fail(false, format!("acquire: {e:#}")),
   193	    };
   194	
   195	    let mut secrets = Vec::new();
   196	    for name in &task.agent.secrets {
   197	        match std::env::var(name) {
   198	            Ok(value) => secrets.push((name.clone(), value)),
   199	            Err(_) => {
   200	                let _ = session.release();
   201	                return fail(false, format!("secret env var '{name}' not set"));
   202	            }
   203	        }
   204	    }
   205	    let plan = WorkspacePlan {
   206	        host: task.host(),
   207	        repos: task.spec.workspace.repos.clone(),
   208	        card: task.card.clone(),
   209	        payload: ledger.run_payload(run_id)?,
   210	        pre_command: task.spec.pre_command.clone(),
   211	        post_command: task.spec.post_command.clone(),
   212	        marker: attempt_marker(attempt_id),
   213	        remote_workspace: format!("/home/sprite/bb/{}", task.name),
   214	        checkpoint: task.spec.workspace.checkpoint.clone(),
   215	        secrets,
   216	        hermetic: matches!(task.agent.auth_class(), Ok(crate::spec::AuthClass::Api)),
   217	    };
   218	    if let Err(e) = session.prepare(&plan) {
   219	        let _ = session.release();
   220	        return fail(false, format!("prepare: {e:#}"));
   221	    }
   222	    ledger.set_attempt_phase(attempt_id, "prepared")?;
   223	
   224	    let cmd = match harness::build_command(&task.agent, &task.spec.budget) {
   225	        Ok(c) => c,
   226	        Err(e) => {
   227	            let _ = session.release();
   228	            return fail(false, format!("build_command: {e:#}"));
   229	        }
   230	    };
   231	
   232	    ledger.set_attempt_phase(attempt_id, "executing")?;
   233	    let timeout = Duration::from_secs(
   234	        60 * task
   235	            .spec
   236	            .budget
   237	            .timeout_minutes
   238	            .unwrap_or(DEFAULT_TIMEOUT_MINUTES),
   239	    );
   240	    let card_prompt = format!(
   241	        "Read {CARD_FILENAME} in this directory — it is your entire commission. Execute it.\n\n{}",
   242	        task.card
   243	    );
   244	    let exec = match session.execute(&cmd, Some(&card_prompt), timeout) {
   245	        Ok(r) => r,
   246	        Err(e) => {
   247	            let _ = session.release();
   248	            // The process may or may not have started; treat as executed —
   249	            // side effects are possible once we tried to spawn the agent.
   250	            return fail(true, format!("execute: {e:#}"));
   251	        }
   252	    };
   253	
   254	    ledger.set_attempt_phase(attempt_id, "collecting")?;
   255	    session.write_artifact("stdout.txt", exec.stdout.as_bytes())?;
   256	    session.write_artifact("stderr.txt", exec.stderr.as_bytes())?;
   257	
   258	    if exec.timed_out {
   259	        let _ = session.release();
   260	        ledger.finish_attempt(
   261	            attempt_id,
   262	            "failure",
   263	            Some("wall-clock timeout (killed)"),
   264	            Some(exec.exit_code),
   265	            &AttemptStats::default(),
   266	            Some(&artifact_dir),
   267	        )?;
   268	        return Ok(AttemptOutcome::Failure {
   269	            phase_executed: true,
   270	            error: format!("timeout after {}s", timeout.as_secs()),
   271	        });
   272	    }
   273	    if exec.exit_code != 0 {
   274	        let _ = session.release();
   275	        let error = format!(
   276	            "harness exit {}: {}",
   277	            exec.exit_code,
   278	            truncate(exec.stderr.trim(), 500)
   279	        );
   280	        ledger.finish_attempt(
   281	            attempt_id,
   282	            "failure",
   283	            Some(&error),
   284	            Some(exec.exit_code),
   285	            &AttemptStats::default(),
   286	            Some(&artifact_dir),
   287	        )?;
   288	        return Ok(AttemptOutcome::Failure {
   289	            phase_executed: true,
   290	            error,
   291	        });
   292	    }
   293	
   294	    let parsed = match harness::parse_output(&task.agent.harness, &exec.stdout) {
   295	        Ok(p) => p,
   296	        Err(e) => {
   297	            // Unparseable output is a failure with raw output preserved —
   298	            // never a silent zero-cost success.
   299	            let _ = session.release();
   300	            let error = format!("unparseable harness output: {e:#}");
   301	            ledger.finish_attempt(
   302	                attempt_id,
   303	                "failure",
   304	                Some(&error),
   305	                Some(exec.exit_code),
   306	                &AttemptStats::default(),
   307	                Some(&artifact_dir),
   308	            )?;
   309	            return Ok(AttemptOutcome::Failure {
   310	                phase_executed: true,
   311	                error,
   312	            });
   313	        }
   314	    };
   315	    session.write_artifact("result.md", parsed.result.as_bytes())?;
   316	
   317	    ledger.set_attempt_phase(attempt_id, "finalizing")?;
   318	    if let Some(post) = &plan.post_command {
   319	        // post_command is finalization (publish artifacts, post replies);
   320	        // a failed finalization must not masquerade as a successful run.
   321	        let post_result = session.execute(
   322	            &["sh".into(), "-c".into(), post.clone()],
   323	            None,
   324	            Duration::from_secs(600),
   325	        );
   326	        let post_error = match post_result {
   327	            Ok(res) if res.exit_code == 0 => None,
   328	            Ok(res) => Some(format!(
   329	                "post_command exit {}: {}",
   330	                res.exit_code,
   331	                truncate(res.stderr.trim(), 500)
   332	            )),
   333	            Err(e) => Some(format!("post_command: {e:#}")),
   334	        };
   335	        if let Some(error) = post_error {
   336	            let _ = session.release();
   337	            ledger.finish_attempt(
   338	                attempt_id,
   339	                "failure",
   340	                Some(&error),

 succeeded in 180ms:
     1	//! Ingress: webhook, cron, and manual all converge on the same path —
     2	//! validate → trigger-defined dedupe key → durable run row → ack. The
     3	//! HTTP/scheduler plumbing lives in `serve`; everything here is testable
     4	//! without a socket.
     5	
     6	use anyhow::{bail, Context, Result};
     7	use chrono::{DateTime, Utc};
     8	use hmac::{Hmac, Mac};
     9	use sha2::Sha256;
    10	
    11	use crate::ledger::{IngressOutcome, IngressRequest, Ledger};
    12	use crate::spec::{Plane, Task, TriggerSpec};
    13	
    14	/// A webhook response the HTTP layer can serialize directly.
    15	#[derive(Debug)]
    16	pub struct WebhookResponse {
    17	    pub status: u16,
    18	    pub body: String,
    19	}
    20	
    21	/// Locate the unique (task, webhook trigger) pair for a route. Route
    22	/// collisions are a config error caught at plane load.
    23	pub fn webhook_target<'p>(plane: &'p Plane, route: &str) -> Option<(&'p Task, &'p TriggerSpec)> {
    24	    for task in plane.tasks.values() {
    25	        for trigger in &task.spec.triggers {
    26	            if let TriggerSpec::Webhook { route: r, .. } = trigger {
    27	                if r == route {
    28	                    return Some((task, trigger));
    29	                }
    30	            }
    31	        }
    32	    }
    33	    None
    34	}
    35	
    36	/// Handle a webhook delivery. The run row is durable before this returns
    37	/// a 2xx; an invalid signature is rejected with no row at all.
    38	pub fn handle_webhook(
    39	    plane: &Plane,
    40	    ledger: &mut Ledger,
    41	    route: &str,
    42	    headers: &[(String, String)],
    43	    body: &str,
    44	) -> Result<WebhookResponse> {
    45	    let Some((task, trigger)) = webhook_target(plane, route) else {
    46	        return Ok(WebhookResponse {
    47	            status: 404,
    48	            body: format!("{{\"error\":\"no webhook route '{route}'\"}}"),
    49	        });
    50	    };
    51	    let TriggerSpec::Webhook {
    52	        secret_env,
    53	        dedupe_key,
    54	        filter,
    55	        ..
    56	    } = trigger
    57	    else {
    58	        unreachable!("webhook_target returns webhook triggers only");
    59	    };
    60	
    61	    let Ok(secret) = std::env::var(secret_env) else {
    62	        return Ok(WebhookResponse {
    63	            status: 503,
    64	            body: format!("{{\"error\":\"secret env '{secret_env}' not set on the plane\"}}"),
    65	        });
    66	    };
    67	    let signature = header(headers, "x-hub-signature-256")
    68	        .or_else(|| header(headers, "x-signature-256"))
    69	        .unwrap_or_default();
    70	    if !verify_hmac(&secret, body.as_bytes(), &signature) {
    71	        return Ok(WebhookResponse {
    72	            status: 401,
    73	            body: "{\"error\":\"invalid signature\"}".to_string(),
    74	        });
    75	    }
    76	
    77	    // Containment filters run after authentication: a verified delivery
    78	    // that fails any condition is acknowledged (2xx, so the sender does
    79	    // not retry) but never becomes a run. Fail-closed on unparseable
    80	    // payloads and missing pointers.
    81	    if !filter.is_empty() {
    82	        let payload: serde_json::Value = match serde_json::from_str(body) {
    83	            Ok(v) => v,
    84	            Err(_) => {
    85	                return Ok(WebhookResponse {
    86	                    status: 200,
    87	                    body: "{\"filtered\":\"payload is not JSON\"}".to_string(),
    88	                })
    89	            }
    90	        };
    91	        for f in filter {
    92	            if let Some(reason) = f.reject_reason(&payload) {
    93	                return Ok(WebhookResponse {
    94	                    status: 200,
    95	                    body: serde_json::json!({ "filtered": reason }).to_string(),
    96	                });
    97	            }
    98	        }
    99	    }
   100	
   101	    let derived = match dedupe_key {
   102	        Some(expr) => Some(derive_dedupe_key(expr, headers, body)?),
   103	        // No derivation declared: the body hash is the event identity.
   104	        None => Some(format!("body:{}", body_hash(body))),
   105	    };
   106	    let key = derived.map(|k| format!("wh:{route}:{k}"));
   107	
   108	    let outcome = ledger.ingest(IngressRequest {
   109	        task: &task.name,
   110	        trigger_kind: "webhook",
   111	        idempotency_key: key.as_deref(),
   112	        source_event_id: header(headers, "x-github-delivery").as_deref(),
   113	        payload: Some(body),
   114	        parent_run_id: None,
   115	    })?;
   116	    Ok(WebhookResponse {
   117	        status: 202,
   118	        body: serde_json::json!({
   119	            "run_id": outcome.run_id,
   120	            "duplicate": outcome.duplicate,
   121	            "state": outcome.state,
   122	        })
   123	        .to_string(),
   124	    })
   125	}
   126	
   127	/// Trigger-defined dedupe key derivation: `header:<Name>` or
   128	/// `json:<pointer>` (RFC 6901, e.g. `json:/pull_request/head/sha`).
   129	pub fn derive_dedupe_key(expr: &str, headers: &[(String, String)], body: &str) -> Result<String> {
   130	    match expr.split_once(':') {
   131	        Some(("header", name)) => header(headers, &name.to_ascii_lowercase())
   132	            .with_context(|| format!("dedupe header '{name}' missing from delivery")),
   133	        Some(("json", pointer)) => {
   134	            let v: serde_json::Value =
   135	                serde_json::from_str(body).context("dedupe json: body is not JSON")?;
   136	            let found = v
   137	                .pointer(pointer)
   138	                .with_context(|| format!("dedupe pointer '{pointer}' missing from body"))?;
   139	            Ok(match found {
   140	                serde_json::Value::String(s) => s.clone(),
   141	                other => other.to_string(),
   142	            })
   143	        }
   144	        _ => bail!("unknown dedupe_key expression '{expr}' (use header:<Name> or json:<ptr>)"),
   145	    }
   146	}
   147	
   148	fn header(headers: &[(String, String)], name: &str) -> Option<String> {
   149	    headers
   150	        .iter()
   151	        .find(|(k, _)| k.eq_ignore_ascii_case(name))
   152	        .map(|(_, v)| v.clone())
   153	}
   154	
   155	/// GitHub-style `sha256=<hex>` HMAC over the raw body.
   156	pub fn verify_hmac(secret: &str, body: &[u8], signature_header: &str) -> bool {
   157	    let Some(hex_sig) = signature_header.strip_prefix("sha256=") else {
   158	        return false;
   159	    };
   160	    let Ok(expected) = hex_decode(hex_sig) else {
   161	        return false;
   162	    };
   163	    let mut mac = Hmac::<Sha256>::new_from_slice(secret.as_bytes()).expect("hmac accepts any key");
   164	    mac.update(body);
   165	    mac.verify_slice(&expected).is_ok()
   166	}
   167	
   168	pub fn sign_hmac(secret: &str, body: &[u8]) -> String {
   169	    let mut mac = Hmac::<Sha256>::new_from_slice(secret.as_bytes()).expect("hmac accepts any key");
   170	    mac.update(body);
   171	    format!("sha256={}", hex_encode(&mac.finalize().into_bytes()))
   172	}
   173	
   174	fn body_hash(body: &str) -> String {
   175	    use sha2::Digest;
   176	    let mut h = Sha256::new();
   177	    h.update(body.as_bytes());
   178	    hex_encode(&h.finalize())
   179	}
   180	
   181	fn hex_encode(bytes: &[u8]) -> String {
   182	    bytes.iter().map(|b| format!("{b:02x}")).collect()
   183	}
   184	
   185	fn hex_decode(s: &str) -> Result<Vec<u8>> {
   186	    if !s.len().is_multiple_of(2) {
   187	        bail!("odd-length hex");
   188	    }
   189	    (0..s.len())
   190	        .step_by(2)
   191	        .map(|i| u8::from_str_radix(&s[i..i + 2], 16).context("bad hex"))
   192	        .collect()
   193	}
   194	
   195	// ---- cron -----------------------------------------------------------------
   196	
   197	/// Parse a cron schedule; 5-field standard expressions get a seconds
   198	/// column prepended (the `cron` crate wants 6/7 fields).
   199	pub fn parse_schedule(expr: &str) -> Result<cron::Schedule> {
   200	    let normalized = if expr.split_whitespace().count() == 5 {
   201	        format!("0 {expr}")
   202	    } else {
   203	        expr.to_string()
   204	    };
   205	    normalized
   206	        .parse::<cron::Schedule>()
   207	        .with_context(|| format!("invalid cron schedule '{expr}'"))
   208	}
   209	
   210	/// Fire times due in `(after, until]`. The scheduled timestamp is the
   211	/// dedupe key, so a delayed or repeated tick can never double-fire.
   212	pub fn due_fires(
   213	    schedule: &cron::Schedule,
   214	    after: DateTime<Utc>,
   215	    until: DateTime<Utc>,
   216	) -> Vec<DateTime<Utc>> {
   217	    schedule.after(&after).take_while(|t| *t <= until).collect()
   218	}
   219	
   220	/// Ingest one cron fire; idempotent on the scheduled timestamp.
   221	pub fn ingest_cron_fire(
   222	    ledger: &mut Ledger,
   223	    task: &str,
   224	    scheduled: DateTime<Utc>,
   225	) -> Result<IngressOutcome> {
   226	    let key = format!("cron:{}", scheduled.to_rfc3339());
   227	    ledger.ingest(IngressRequest {
   228	        task,
   229	        trigger_kind: "cron",
   230	        idempotency_key: Some(&key),
   231	        source_event_id: None,
   232	        payload: None,
   233	        parent_run_id: None,
   234	    })
   235	}

 succeeded in 185ms:
tests/e2e_sprites.rs:89:            "version = 1\nharness = \"claude\"\nmodel = \"claude-fable-5\"\nbin = \"{}\"\nsecrets = [\"BB_TEST_SECRET\"]\n",
tests/e2e_sprites.rs:122:    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
src/main.rs:134:    if let Err(e) = run() {
src/main.rs:140:fn run() -> Result<()> {
src/main.rs:170:                print_run(&ledger, &outcome.run_id, json)?;
src/main.rs:175:                print_run(&ledger, &outcome.run_id, json)?;
src/main.rs:178:            let run = dispatch::dispatch_run(&plane, &mut ledger, &outcome.run_id)?;
src/main.rs:180:                print_run(&ledger, &run.id, true)?;
src/main.rs:204:                let runs = ledger.list_runs(task.as_deref(), state.as_deref())?;
src/main.rs:222:                print_run(&ledger, &run_id, json)?;
src/main.rs:225:                let state = ledger.run(&run_id)?.state;
src/main.rs:246:                ledger.release_leases_for_run(&run_id)?;
src/main.rs:250:                for r in ledger.list_runs(None, None)? {
src/main.rs:300:                let run = dispatch::dispatch_run(&plane, &mut ledger, &outcome.run_id)?;
src/main.rs:315:                    "unparked {task}; {} blocked run(s) now pending",
src/main.rs:375:fn print_run(ledger: &Ledger, run_id: &str, json: bool) -> Result<()> {
src/main.rs:376:    let run = ledger.run(run_id)?;
src/dispatch.rs:14:use crate::substrate::{self, WorkspacePlan, CARD_FILENAME};
src/dispatch.rs:28:pub fn dispatch_run(plane: &Plane, ledger: &mut Ledger, run_id: &str) -> Result<RunRow> {
src/dispatch.rs:29:    let run = ledger.run(run_id)?;
src/dispatch.rs:48:        return ledger.run(run_id);
src/dispatch.rs:54:        return ledger.run(run_id);
src/dispatch.rs:65:                ledger.finalize_run(
src/dispatch.rs:88:                ledger.finalize_run(run_id, None, started.elapsed().as_millis() as i64)?;
src/dispatch.rs:103:                ledger.finalize_run(run_id, None, started.elapsed().as_millis() as i64)?;
src/dispatch.rs:118:    ledger.run(run_id)
src/dispatch.rs:195:    let mut secrets = Vec::new();
src/dispatch.rs:196:    for name in &task.agent.secrets {
src/dispatch.rs:198:            Ok(value) => secrets.push((name.clone(), value)),
src/dispatch.rs:205:    let plan = WorkspacePlan {
src/dispatch.rs:210:        pre_command: task.spec.pre_command.clone(),
src/dispatch.rs:211:        post_command: task.spec.post_command.clone(),
src/dispatch.rs:215:        secrets,
src/dispatch.rs:244:    let exec = match session.execute(&cmd, Some(&card_prompt), timeout) {
src/dispatch.rs:318:    if let Some(post) = &plan.post_command {
src/dispatch.rs:319:        // post_command is finalization (publish artifacts, post replies);
src/dispatch.rs:321:        let post_result = session.execute(
src/dispatch.rs:329:                "post_command exit {}: {}",
src/dispatch.rs:333:            Err(e) => Some(format!("post_command: {e:#}")),
src/ledger.rs:268:        let inserted = tx.execute(
src/ledger.rs:299:        tx.execute(
src/ledger.rs:358:            .execute(&sql, params![run_id, to, reason, now()])?;
src/ledger.rs:374:    pub fn finalize_run(
src/ledger.rs:380:        self.conn.execute(
src/ledger.rs:388:        self.conn.execute(
src/ledger.rs:396:        self.conn.execute(
src/ledger.rs:414:        self.conn.execute(
src/ledger.rs:435:        self.conn.execute(
src/ledger.rs:460:        self.conn.execute(
src/ledger.rs:493:        let n = self.conn.execute(
src/ledger.rs:502:        self.conn.execute(
src/ledger.rs:529:        self.conn.execute(
src/ledger.rs:582:        let changed = self.conn.execute(
src/ledger.rs:591:    pub fn release_leases_for_run(&self, run_id: &str) -> Result<()> {
src/ledger.rs:593:            .execute("DELETE FROM host_leases WHERE run_id = ?1", params![run_id])?;
src/ledger.rs:600:        self.conn.execute(
src/ledger.rs:608:        self.conn.execute(
src/ledger.rs:618:            .execute("DELETE FROM parked_tasks WHERE task = ?1", params![task])?;
src/ledger.rs:668:    pub fn run(&self, run_id: &str) -> Result<RunRow> {
src/ledger.rs:686:    pub fn list_runs(&self, task: Option<&str>, state: Option<&str>) -> Result<Vec<RunRow>> {
src/ledger.rs:706:        self.list_runs(None, Some(state))
src/ledger.rs:784:fn row_to_run(r: &rusqlite::Row<'_>) -> rusqlite::Result<RunRow> {
src/ledger.rs:863:        assert_eq!(ledger.list_runs(Some("demo"), None).unwrap().len(), 1);
src/ledger.rs:877:    fn keyless_ingress_always_creates_a_run() {
src/ledger.rs:882:        assert_eq!(ledger.list_runs(Some("demo"), None).unwrap().len(), 2);
src/ledger.rs:975:        let replay_row = ledger.run(&replay.run_id).unwrap();
src/substrate/mod.rs:2://! not repos: workspace materialization is a declarative `WorkspacePlan`
src/substrate/mod.rs:16:pub struct WorkspacePlan {
src/substrate/mod.rs:25:    pub pre_command: Option<String>,
src/substrate/mod.rs:26:    pub post_command: Option<String>,
src/substrate/mod.rs:34:    pub secrets: Vec<(String, String)>,
src/substrate/mod.rs:37:    /// the exec boundary except the declared secrets. Subscription agents
src/substrate/mod.rs:73:    /// declared refs, card file, pre_command. Nothing here may start the
src/substrate/mod.rs:75:    fn prepare(&mut self, plan: &WorkspacePlan) -> Result<()>;
src/substrate/mod.rs:79:    fn execute(
tests/budgets.rs:75:    dispatch::dispatch_run(plane, ledger, &id).unwrap()
tests/budgets.rs:174:        "pre_command = \"exit 9\"\n",
tests/budgets.rs:207:        .list_runs(Some("demo"), None)
src/serve.rs:152:        let run = dispatch::dispatch_run(&plane, &mut ledger, run_id)?;
src/serve.rs:192:fn read_authorized(request: &tiny_http::Request, url: &str) -> bool {
src/serve.rs:208:fn query_param(url: &str, name: &str) -> Option<String> {
src/serve.rs:221:        if !read_authorized(request, &url) {
src/serve.rs:230:                let task = query_param(&url, "task");
src/serve.rs:231:                let state = query_param(&url, "state");
src/serve.rs:232:                let runs = ledger.list_runs(task.as_deref(), state.as_deref())?;
src/serve.rs:239:                    let run = ledger.run(id)?;
src/serve.rs:356:    for r in ledger.list_runs(None, None)?.into_iter().take(50) {
src/substrate/sprites.rs:11:use super::local::{run_with_timeout, shell_quote};
src/substrate/sprites.rs:12:use super::{ExecResult, ProbeResult, Session, Substrate, WorkspacePlan, CARD_FILENAME};
src/substrate/sprites.rs:53:            secrets: Vec::new(),
src/substrate/sprites.rs:74:        match run_with_timeout(
src/substrate/sprites.rs:102:    secrets: Vec<(String, String)>,
src/substrate/sprites.rs:121:        run_with_timeout(&cmd, stdin, &relay_cwd(), &[], false, None, timeout)
src/substrate/sprites.rs:139:    fn prepare(&mut self, plan: &WorkspacePlan) -> Result<()> {
src/substrate/sprites.rs:142:        self.secrets = plan.secrets.clone();
src/substrate/sprites.rs:148:            let out = run_with_timeout(
src/substrate/sprites.rs:234:        if let Some(pre) = &plan.pre_command {
src/substrate/sprites.rs:238:                bail!("pre_command failed: {}", out.stderr.trim());
src/substrate/sprites.rs:244:    fn execute(
src/substrate/sprites.rs:250:        // The whole script travels on stdin — secrets and prompt never
src/substrate/sprites.rs:256:            .secrets
tests/recovery.rs:31:fn running_run(ledger: &mut Ledger, task: &str) -> String {
tests/recovery.rs:53:    let run_id = running_run(&mut ledger, "demo");
tests/recovery.rs:72:    let run_id = running_run(&mut ledger, "demo");
tests/recovery.rs:102:    let run_id = running_run(&mut ledger, "demo");
tests/recovery.rs:129:    let run_id = running_run(&mut ledger, "demo");
tests/policy.rs:36:fn anthropic_or_openai_api_keys_are_forbidden_secrets() {
tests/policy.rs:40:        "harness = \"pi\"\nmodel = \"m\"\nsecrets = [\"OPENAI_API_KEY\"]\n",
tests/policy.rs:64:        "harness = \"pi\"\nmodel = \"moonshotai/kimi-k2.6\"\nsecrets = [\"OPENROUTER_API_KEY\"]\n",
tests/e2e_local.rs:60:fn manual_run(ledger: &mut Ledger, task: &str, key: Option<&str>) -> String {
tests/e2e_local.rs:80:    let run_id = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:81:    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
tests/e2e_local.rs:122:    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
tests/e2e_local.rs:137:    let run_id = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:138:    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
tests/e2e_local.rs:153:    // pre_command failure happens in prepare — before executing.
tests/e2e_local.rs:154:    let plane = make_plane(dir.path(), CLAUDE_STUB, "pre_command = \"exit 7\"");
tests/e2e_local.rs:157:    let run_id = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:158:    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
tests/e2e_local.rs:173:    let replay_id = manual_run(&mut ledger, "demo", Some("replay-key"));
tests/e2e_local.rs:185:fn timeout_kills_the_harness_and_fails_the_run() {
tests/e2e_local.rs:195:    let run_id = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:196:    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
tests/e2e_local.rs:202:fn hermetic_exec_passes_secrets_but_never_the_planes_env() {
tests/e2e_local.rs:224:            "harness = \"pi\"\nmodel = \"m\"\nbin = \"{}\"\nsecrets = [\"BB_TEST_SECRET\"]\n",
tests/e2e_local.rs:238:    let run_id = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:239:    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
tests/e2e_local.rs:259:    let first = manual_run(&mut ledger, "demo", Some("X"));
tests/e2e_local.rs:260:    let second = manual_run(&mut ledger, "demo", Some("X"));
tests/e2e_local.rs:262:    assert_eq!(ledger.list_runs(Some("demo"), None).unwrap().len(), 1);
tests/e2e_local.rs:273:    let r1 = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:275:        dispatch::dispatch_run(&plane, &mut ledger, &r1)
tests/e2e_local.rs:281:    let r2 = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:282:    let blocked = dispatch::dispatch_run(&plane, &mut ledger, &r2).unwrap();
tests/e2e_local.rs:287:    let r3 = manual_run(&mut ledger, "demo", None);
tests/e2e_local.rs:288:    assert_eq!(ledger.run(&r3).unwrap().state, "blocked_budget");
tests/ingress.rs:58:    let runs = ledger.list_runs(Some("demo"), None).unwrap();
tests/ingress.rs:79:    assert!(ledger.list_runs(Some("demo"), None).unwrap().is_empty());
tests/ingress.rs:88:fn webhook_redelivery_same_dedupe_key_records_event_no_second_run() {
tests/ingress.rs:105:    assert_eq!(ledger.list_runs(Some("demo"), None).unwrap().len(), 1);
tests/ingress.rs:151:    assert_eq!(ledger.list_runs(Some("demo"), None).unwrap().len(), 1);
tests/ingress.rs:180:fn webhook_filters_reject_out_of_scope_deliveries_without_a_run() {
tests/ingress.rs:228:    assert_eq!(ledger.list_runs(None, None).unwrap().len(), 1);
src/spec.rs:80:    /// (hermetic: only declared secrets cross the exec boundary).
src/spec.rs:89:    pub secrets: Vec<String>,
src/spec.rs:134:    pub pre_command: Option<String>,
src/spec.rs:135:    pub post_command: Option<String>,
src/spec.rs:383:            for secret in &agent.secrets {
src/substrate/local.rs:11:use super::{ExecResult, ProbeResult, Session, Substrate, WorkspacePlan, CARD_FILENAME};
src/substrate/local.rs:30:            secrets: Vec::new(),
src/substrate/local.rs:55:    secrets: Vec<(String, String)>,
src/substrate/local.rs:61:        run_with_timeout(
src/substrate/local.rs:74:    /// reach a workload unless declared as agent secrets.
src/substrate/local.rs:101:        env.extend(self.secrets.iter().cloned());
src/substrate/local.rs:107:    fn prepare(&mut self, plan: &WorkspacePlan) -> Result<()> {
src/substrate/local.rs:108:        self.secrets = plan.secrets.clone();
src/substrate/local.rs:131:        if let Some(pre) = &plan.pre_command {
src/substrate/local.rs:134:                anyhow::bail!("pre_command failed: {}", out.stderr.trim());
src/substrate/local.rs:140:    fn execute(
src/substrate/local.rs:146:        run_with_timeout(
src/substrate/local.rs:186:pub(crate) fn run_with_timeout(
src/substrate/local.rs:223:        command.env_clear();

codex
A few candidate issues are separating out: the local hermetic test is meaningful, but sprites is the production substrate, so I’m validating whether the same env/HOME policy is actually applied remotely. I’m also checking the new read API token parsing for bypass behavior, since it accepts both headers and query strings.
exec
/bin/zsh -lc "nl -ba src/substrate/sprites.rs | sed -n '244,360p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba src/ledger.rs | sed -n '660,730p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba src/main.rs | sed -n '130,235p'" in /Users/phaedrus/Development/bitterblossom
exec
/bin/zsh -lc "nl -ba tests/e2e_sprites.rs | sed -n '1,180p'" in /Users/phaedrus/Development/bitterblossom
 succeeded in 165ms:
   660	            "SELECT COALESCE(SUM(cost_usd), 0.0) FROM attempts WHERE substr(started_at, 1, 10) = ?1",
   661	            params![day],
   662	            |r| r.get(0),
   663	        )?)
   664	    }
   665	
   666	    // ---- queries -----------------------------------------------------------
   667	
   668	    pub fn run(&self, run_id: &str) -> Result<RunRow> {
   669	        self.conn
   670	            .query_row(
   671	                &format!("{RUN_SELECT} WHERE id = ?1"),
   672	                params![run_id],
   673	                row_to_run,
   674	            )
   675	            .with_context(|| format!("run {run_id} not found"))
   676	    }
   677	
   678	    pub fn run_payload(&self, run_id: &str) -> Result<Option<String>> {
   679	        Ok(self.conn.query_row(
   680	            "SELECT payload FROM runs WHERE id = ?1",
   681	            params![run_id],
   682	            |r| r.get(0),
   683	        )?)
   684	    }
   685	
   686	    pub fn list_runs(&self, task: Option<&str>, state: Option<&str>) -> Result<Vec<RunRow>> {
   687	        let mut sql = format!("{RUN_SELECT} WHERE 1=1");
   688	        let mut args: Vec<String> = Vec::new();
   689	        if let Some(t) = task {
   690	            sql.push_str(&format!(" AND task = ?{}", args.len() + 1));
   691	            args.push(t.to_string());
   692	        }
   693	        if let Some(s) = state {
   694	            sql.push_str(&format!(" AND state = ?{}", args.len() + 1));
   695	            args.push(s.to_string());
   696	        }
   697	        sql.push_str(" ORDER BY created_at DESC LIMIT 200");
   698	        let mut stmt = self.conn.prepare(&sql)?;
   699	        let rows = stmt
   700	            .query_map(rusqlite::params_from_iter(args), row_to_run)?
   701	            .collect::<rusqlite::Result<Vec<_>>>()?;
   702	        Ok(rows)
   703	    }
   704	
   705	    pub fn runs_in_state(&self, state: &str) -> Result<Vec<RunRow>> {
   706	        self.list_runs(None, Some(state))
   707	    }
   708	
   709	    /// Dispatch order: oldest pending first, across all tasks.
   710	    pub fn pending_runs_oldest_first(&self) -> Result<Vec<RunRow>> {
   711	        let mut stmt = self.conn.prepare(&format!(
   712	            "{RUN_SELECT} WHERE state = 'pending' ORDER BY created_at ASC"
   713	        ))?;
   714	        let rows = stmt
   715	            .query_map([], row_to_run)?
   716	            .collect::<rusqlite::Result<Vec<_>>>()?;
   717	        Ok(rows)
   718	    }
   719	
   720	    pub fn attempts(&self, run_id: &str) -> Result<Vec<AttemptRow>> {
   721	        let mut stmt = self.conn.prepare(
   722	            "SELECT id, run_id, n, agent_name, agent_version, harness, model, phase,
   723	               outcome, error, exit_code, tokens_in, tokens_out, turns, cost_usd,
   724	               artifact_dir, started_at, ended_at
   725	             FROM attempts WHERE run_id = ?1 ORDER BY n",
   726	        )?;
   727	        let rows = stmt
   728	            .query_map(params![run_id], |r| {
   729	                Ok(AttemptRow {
   730	                    id: r.get(0)?,

 succeeded in 168ms:
   244	    fn execute(
   245	        &mut self,
   246	        cmd: &[String],
   247	        stdin: Option<&str>,
   248	        timeout: Duration,
   249	    ) -> Result<ExecResult> {
   250	        // The whole script travels on stdin — secrets and prompt never
   251	        // appear in local or remote argv (argv is visible in process
   252	        // tables and CLI telemetry). The prompt reaches the harness via a
   253	        // heredoc with a per-attempt delimiter; the pidfile makes the
   254	        // attempt probeable and cancellable after a plane restart.
   255	        let exports: String = self
   256	            .secrets
   257	            .iter()
   258	            .map(|(k, v)| format!("export {k}={}\n", shell_quote(v)))
   259	            .collect();
   260	        let quoted: Vec<String> = cmd.iter().map(|c| shell_quote(c)).collect();
   261	        // Unguessable delimiter, re-rolled on the astronomically unlikely
   262	        // collision, so prompt content can never close the heredoc early
   263	        // and execute as shell.
   264	        let delimiter = loop {
   265	            let candidate = format!("BB_STDIN_EOF_{}", crate::ledger::new_id());
   266	            let collides = stdin
   267	                .map(|input| input.lines().any(|l| l.trim() == candidate))
   268	                .unwrap_or(false);
   269	            if !collides {
   270	                break candidate;
   271	            }
   272	        };
   273	        let body = match stdin {
   274	            Some(input) => format!(
   275	                "exec {cmd} <<'{delimiter}'\n{input}\n{delimiter}\n",
   276	                cmd = quoted.join(" "),
   277	            ),
   278	            None => format!("exec {} < /dev/null\n", quoted.join(" ")),
   279	        };
   280	        let script = format!(
   281	            "cd {ws} || exit 1\necho $$ > /tmp/{marker}.pid\n{exports}{body}",
   282	            ws = shell_quote(&self.workspace),
   283	            marker = self.marker,
   284	        );
   285	        // `setsid -w sh` makes the remote shell a session leader whose pid
   286	        // is the pidfile pid, so the timeout kill below takes out the whole
   287	        // process group — grandchildren included, exit status propagated.
   288	        let result = self.sprite_exec(
   289	            &["setsid".into(), "-w".into(), "sh".into()],
   290	            Some(&script),
   291	            timeout,
   292	        )?;
   293	        if result.timed_out {
   294	            // The local relay died but the remote process may live on;
   295	            // best-effort remote kill via the pidfile.
   296	            let kill = format!(
   297	                "pid=\"$(cat /tmp/{}.pid 2>/dev/null)\" && kill -9 -- \"-$pid\" 2>/dev/null; true",
   298	                self.marker
   299	            );
   300	            let _ = self.remote_shell(&kill, Duration::from_secs(30));
   301	        }
   302	        Ok(result)
   303	    }
   304	
   305	    fn write_artifact(&mut self, name: &str, data: &[u8]) -> Result<()> {
   306	        let path = self.artifacts.join(name);
   307	        if let Some(parent) = path.parent() {
   308	            std::fs::create_dir_all(parent).context("artifact dir")?;
   309	        }
   310	        std::fs::write(path, data)?;
   311	        Ok(())
   312	    }
   313	
   314	    fn release(&mut self) -> Result<()> {
   315	        if !self.marker.is_empty() {
   316	            let _ = self.remote_shell(
   317	                &format!("rm -f /tmp/{}.pid", self.marker),
   318	                Duration::from_secs(30),
   319	            );
   320	        }
   321	        Ok(())
   322	    }
   323	}
   324	
   325	fn repo_dir_name(url: &str) -> String {
   326	    url.trim_end_matches('/')
   327	        .trim_end_matches(".git")
   328	        .rsplit('/')
   329	        .next()
   330	        .unwrap_or("repo")
   331	        .to_string()
   332	}

 succeeded in 165ms:
   130	    // Unix CLIs instead of panicking on a broken-pipe write.
   131	    unsafe {
   132	        libc::signal(libc::SIGPIPE, libc::SIG_DFL);
   133	    }
   134	    if let Err(e) = run() {
   135	        eprintln!("error: {e:#}");
   136	        std::process::exit(1);
   137	    }
   138	}
   139	
   140	fn run() -> Result<()> {
   141	    let cli = Cli::parse();
   142	    let root = cli
   143	        .config
   144	        .or_else(|| std::env::var_os("BB_PLANE_DIR").map(PathBuf::from))
   145	        .unwrap_or_else(|| PathBuf::from("."));
   146	    let plane = Plane::load(&root)?;
   147	    let mut ledger = Ledger::open(&plane.db_path())?;
   148	
   149	    match cli.command {
   150	        Command::Run {
   151	            task,
   152	            idempotency_key,
   153	            payload,
   154	            json,
   155	        } => {
   156	            plane.task(&task)?;
   157	            let outcome = ledger.ingest(IngressRequest {
   158	                task: &task,
   159	                trigger_kind: "manual",
   160	                idempotency_key: idempotency_key.as_deref(),
   161	                source_event_id: None,
   162	                payload: payload.as_deref(),
   163	                parent_run_id: None,
   164	            })?;
   165	            if outcome.duplicate {
   166	                eprintln!(
   167	                    "duplicate idempotency key; existing run {} ({})",
   168	                    outcome.run_id, outcome.state
   169	                );
   170	                print_run(&ledger, &outcome.run_id, json)?;
   171	                return Ok(());
   172	            }
   173	            if outcome.state == "blocked_budget" {
   174	                eprintln!("run {} blocked: task is parked", outcome.run_id);
   175	                print_run(&ledger, &outcome.run_id, json)?;
   176	                return Ok(());
   177	            }
   178	            let run = dispatch::dispatch_run(&plane, &mut ledger, &outcome.run_id)?;
   179	            if json {
   180	                print_run(&ledger, &run.id, true)?;
   181	            } else {
   182	                println!(
   183	                    "run {} {} (task={} agent={}@v{} cost={} duration_ms={})",
   184	                    run.id,
   185	                    run.state,
   186	                    run.task,
   187	                    run.agent_name.as_deref().unwrap_or("-"),
   188	                    run.agent_version.unwrap_or(0),
   189	                    run.cost_usd
   190	                        .map(|c| format!("${c:.4}"))
   191	                        .unwrap_or_else(|| "-".into()),
   192	                    run.duration_ms.unwrap_or(0),
   193	                );
   194	                if run.state != "success" {
   195	                    eprintln!("reason: {}", run.state_reason.as_deref().unwrap_or("-"));
   196	                }
   197	            }
   198	            if run.state == "failure" {
   199	                std::process::exit(2);
   200	            }
   201	        }
   202	        Command::Runs { command } => match command {
   203	            RunsCommand::List { task, state, json } => {
   204	                let runs = ledger.list_runs(task.as_deref(), state.as_deref())?;
   205	                if json {
   206	                    println!("{}", serde_json::to_string_pretty(&runs)?);
   207	                } else {
   208	                    for r in runs {
   209	                        let agent = format!(
   210	                            "{}@v{}",
   211	                            r.agent_name.as_deref().unwrap_or("-"),
   212	                            r.agent_version.unwrap_or(0)
   213	                        );
   214	                        println!(
   215	                            "{}  {:<18} {:<10} {:<14} {}  {}",
   216	                            r.created_at, r.task, r.trigger_kind, r.state, agent, r.id,
   217	                        );
   218	                    }
   219	                }
   220	            }
   221	            RunsCommand::Show { run_id, json } => {
   222	                print_run(&ledger, &run_id, json)?;
   223	            }
   224	            RunsCommand::Cancel { run_id, reason } => {
   225	                let state = ledger.run(&run_id)?.state;
   226	                if state != "pending" {
   227	                    bail!("run {run_id} is {state}; only pending runs can be canceled");
   228	                }
   229	                if !ledger.try_transition(
   230	                    &run_id,
   231	                    "failure",
   232	                    Some(&format!("canceled: {reason}")),
   233	                )? {
   234	                    bail!("run {run_id} was claimed by a dispatcher before cancel");
   235	                }

 succeeded in 162ms:
     1	//! Sprites-substrate e2e against a stub `sprite` CLI (the real CLI is a
     2	//! network boundary). The stub emulates `exec -s` / `restore` locally,
     3	//! mapping /home/sprite onto a temp dir — everything else is the real
     4	//! spine, so the run row must come out identical in shape to local.
     5	
     6	use std::fs;
     7	use std::os::unix::fs::PermissionsExt;
     8	use std::path::Path;
     9	
    10	use bitterblossom::dispatch;
    11	use bitterblossom::ledger::{IngressRequest, Ledger};
    12	use bitterblossom::spec::Plane;
    13	
    14	const SPRITE_STUB: &str = r#"#!/bin/bash
    15	# Fake sprite CLI. Usage it must understand:
    16	#   sprite exec -s <host> [--file src:dest] [--dir d] <cmd...>
    17	#   sprite restore -s <host> <checkpoint-id>
    18	log="$SPRITE_STUB_LOG"
    19	home="$SPRITE_FAKE_HOME"
    20	cmd="$1"; shift
    21	echo "$cmd $*" >> "$log"
    22	case "$cmd" in
    23	  restore)
    24	    exit 0;;
    25	  exec)
    26	    declare -a rest
    27	    while [ $# -gt 0 ]; do
    28	      case "$1" in
    29	        -s|--dir|--env|-o) shift 2;;
    30	        --) shift; break;;
    31	        --file)
    32	          spec="$2"; shift 2
    33	          src="${spec%%:*}"; dest="${spec#*:}"
    34	          dest="${dest//\/home\/sprite/$home}"
    35	          mkdir -p "$(dirname "$dest")"
    36	          cp "$src" "$dest";;
    37	        *) break;;
    38	      esac
    39	    done
    40	    while [ $# -gt 0 ]; do
    41	      rest+=("${1//\/home\/sprite/$home}"); shift
    42	    done
    43	    # macOS test hosts have no setsid; the session-leader semantics are
    44	    # exercised on real sprites, not by the stub.
    45	    if [ "${rest[0]}" = "setsid" ]; then
    46	      rest=("${rest[@]:1}")
    47	      [ "${rest[0]}" = "-w" ] && rest=("${rest[@]:1}")
    48	    fi
    49	    # Bare `sh` means the script arrives on stdin; rewrite paths there too.
    50	    if [ "${rest[0]}" = "sh" ] && [ "${#rest[@]}" -eq 1 ]; then
    51	      sed "s|/home/sprite|$home|g" | sh
    52	      exit $?
    53	    fi
    54	    exec "${rest[@]}";;
    55	  *)
    56	    echo "stub: unknown command $cmd" >&2; exit 64;;
    57	esac
    58	"#;
    59	
    60	const CLAUDE_STUB: &str = r#"#!/bin/sh
    61	cat > /dev/null
    62	# Per-exec secret must arrive via the environment (stdin script exports).
    63	[ "$BB_TEST_SECRET" = "s3cret" ] || { echo "secret missing" >&2; exit 5; }
    64	echo '{"type":"result","subtype":"success","result":"sprite says hi","total_cost_usd":0.005,"num_turns":1,"usage":{"input_tokens":10,"output_tokens":5}}'
    65	"#;
    66	
    67	fn write_executable(path: &Path, content: &str) {
    68	    fs::write(path, content).unwrap();
    69	    fs::set_permissions(path, fs::Permissions::from_mode(0o755)).unwrap();
    70	}
    71	
    72	#[test]
    73	fn sprites_task_runs_end_to_end_with_identical_row_shape() {
    74	    let dir = tempfile::tempdir().unwrap();
    75	    let root = dir.path();
    76	    let fake_home = root.join("fake-sprite-home");
    77	    fs::create_dir_all(&fake_home).unwrap();
    78	
    79	    let sprite_stub = root.join("sprite-stub.sh");
    80	    write_executable(&sprite_stub, SPRITE_STUB);
    81	    let claude_stub = root.join("claude-stub.sh");
    82	    write_executable(&claude_stub, CLAUDE_STUB);
    83	
    84	    fs::create_dir_all(root.join("agents")).unwrap();
    85	    fs::create_dir_all(root.join("tasks/demo")).unwrap();
    86	    fs::write(
    87	        root.join("agents/remote.toml"),
    88	        format!(
    89	            "version = 1\nharness = \"claude\"\nmodel = \"claude-fable-5\"\nbin = \"{}\"\nsecrets = [\"BB_TEST_SECRET\"]\n",
    90	            claude_stub.display()
    91	        ),
    92	    )
    93	    .unwrap();
    94	    fs::write(root.join("tasks/demo/card.md"), "# Sprite demo card\n").unwrap();
    95	    fs::write(
    96	        root.join("tasks/demo/task.toml"),
    97	        "agent = \"remote\"\nsubstrate = \"sprites\"\n\n[workspace]\nhost = \"test-sprite\"\ncheckpoint = \"v999\"\n\n[[trigger]]\nkind = \"manual\"\n",
    98	    )
    99	    .unwrap();
   100	    let plane = Plane::load(root).unwrap();
   101	    let mut ledger = Ledger::open(&plane.db_path()).unwrap();
   102	
   103	    // Env is process-global: these tests must not run in parallel with
   104	    // other env-sensitive tests in this file (there are none).
   105	    let log = root.join("sprite-stub.log");
   106	    std::env::set_var("BB_SPRITE_BIN", &sprite_stub);
   107	    std::env::set_var("SPRITE_STUB_LOG", &log);
   108	    std::env::set_var("SPRITE_FAKE_HOME", &fake_home);
   109	    std::env::set_var("BB_TEST_SECRET", "s3cret");
   110	
   111	    let run_id = ledger
   112	        .ingest(IngressRequest {
   113	            task: "demo",
   114	            trigger_kind: "manual",
   115	            idempotency_key: None,
   116	            source_event_id: None,
   117	            payload: None,
   118	            parent_run_id: None,
   119	        })
   120	        .unwrap()
   121	        .run_id;
   122	    let run = dispatch::dispatch_run(&plane, &mut ledger, &run_id).unwrap();
   123	
   124	    assert_eq!(run.state, "success", "reason: {:?}", run.state_reason);
   125	    assert_eq!(run.cost_usd, Some(0.005));
   126	
   127	    let attempts = ledger.attempts(&run_id).unwrap();
   128	    assert_eq!(attempts.len(), 1);
   129	    let a = &attempts[0];
   130	    assert_eq!(a.outcome.as_deref(), Some("success"));
   131	    assert_eq!(a.phase, "released");
   132	    assert_eq!(a.tokens_in, Some(10));
   133	
   134	    // The card was materialized into the (fake) remote workspace.
   135	    assert!(fake_home.join("bb/demo/LANE_CARD.md").exists());
   136	    // Checkpoint restore was requested before preparing.
   137	    let log_text = fs::read_to_string(&log).unwrap();
   138	    assert!(
   139	        log_text.contains("restore -s test-sprite v999"),
   140	        "{log_text}"
   141	    );
   142	    // The secret reached the harness (the stub asserts it) but never
   143	    // appeared in argv — the stub logs every argv it sees — nor in the db.
   144	    assert!(
   145	        !log_text.contains("s3cret"),
   146	        "secret leaked into argv: {log_text}"
   147	    );
   148	    let db_bytes = fs::read(plane.db_path()).unwrap();
   149	    assert!(!String::from_utf8_lossy(&db_bytes).contains("s3cret"));
   150	
   151	    std::env::remove_var("BB_SPRITE_BIN");
   152	    std::env::remove_var("BB_TEST_SECRET");
   153	}

codex
Findings:

- blocking `src/substrate/sprites.rs:255`: api-auth runs on the production `sprites` substrate are not hermetic. `WorkspacePlan.hermetic` is never stored or used by `SpriteSession`; `execute` only exports declared secrets, then runs `sh` in the sprite’s inherited environment with its normal `HOME`, violating the oracle’s `env_clear + allowlist + workspace-local HOME` requirement.

- blocking `src/serve.rs:203`: read API token auth is bypassable because `read_authorized` accepts any URL containing `token=<secret>`, not a parsed `token` query parameter. For `BB_API_TOKEN=abc`, `/api/runs?notoken=abc` authorizes.

- serious `src/harness.rs:74`: the pi flood-filter wrapper masks producer failures because `sh -c "pi ... | grep -v ..."` returns `grep`’s status, not `pi`’s. A pi process that emits a parseable `message_end` and then exits nonzero can be recorded as success.

- serious `src/substrate/local.rs:60`: local `pre_command` still runs with the full plane environment and real `HOME` via `run_shell(... clear_env=false)`, before the hermetic workload env is applied. In dev planes this can leak plane env like `OPENAI_API_KEY` into api-auth workload preparation despite the stated hermetic boundary.

Verdict: blocking, oracle not satisfied.
tokens used
80,958
Findings:

- blocking `src/substrate/sprites.rs:255`: api-auth runs on the production `sprites` substrate are not hermetic. `WorkspacePlan.hermetic` is never stored or used by `SpriteSession`; `execute` only exports declared secrets, then runs `sh` in the sprite’s inherited environment with its normal `HOME`, violating the oracle’s `env_clear + allowlist + workspace-local HOME` requirement.

- blocking `src/serve.rs:203`: read API token auth is bypassable because `read_authorized` accepts any URL containing `token=<secret>`, not a parsed `token` query parameter. For `BB_API_TOKEN=abc`, `/api/runs?notoken=abc` authorizes.

- serious `src/harness.rs:74`: the pi flood-filter wrapper masks producer failures because `sh -c "pi ... | grep -v ..."` returns `grep`’s status, not `pi`’s. A pi process that emits a parseable `message_end` and then exits nonzero can be recorded as success.

- serious `src/substrate/local.rs:60`: local `pre_command` still runs with the full plane environment and real `HOME` via `run_shell(... clear_env=false)`, before the hermetic workload env is applied. In dev planes this can leak plane env like `OPENAI_API_KEY` into api-auth workload preparation despite the stated hermetic boundary.

Verdict: blocking, oracle not satisfied.
