# Dev/test plane for the powder-chew local drill (bitterblossom-959).
# `dev = true` permits local execution for this dev/test drill. Production
# planes must set `allow_local_substrate = true` as a separate conscious grant.
dev = true
db_path = ".bb/plane.db"

[ingress]
bind = "127.0.0.1:7078"

[budget]
max_cost_per_day_usd = 1.0
