# Walkthrough Terminal Transcripts

Historical terminal transcript companions from `docs/walkthroughs/`.

These files preserve exact Go/Python/conductor-era command evidence, but they
are not current operator instructions. Keep canonical walkthrough prose in
`docs/walkthroughs/*.md`; link here when historical command output is useful.
