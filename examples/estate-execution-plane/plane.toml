# Disposable contract proof. Production must use a configured remote
# substrate and the real Estate mutation adapter. Local remains dev/test by
# default; production local execution requires allow_local_substrate = true.
dev = true
db_path = ".bb/plane.db"

[budget]
max_cost_per_day_usd = 1.0
