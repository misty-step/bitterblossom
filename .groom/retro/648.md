## Entry: #648 -- Governance: rebase on merge conflict instead of full rebuild (2026-03-15)

**Effort:** predicted m -> actual m (but split across builder + operator)
**Scope changes:** Added GH_TOKEN injection fix (P1 blocker discovered during factory audit), shell quoting hardening, branch cleanup, fleet API key validation — all earned during the supervised run.
**Blockers:** Three preflight failures before dispatch: missing workspace, missing ANTHROPIC_API_KEY, missing GITHUB_TOKEN injection. Builder completed work but couldn't push.
**Pattern:** Factory audit is an effective forcing function — it surfaces real operational gaps that issue descriptions and code review miss. The builder's self-diagnosis was accurate and actionable. Supervised runs should be standard practice after any infrastructure change.

---
