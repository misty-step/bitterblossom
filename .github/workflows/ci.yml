name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  shellcheck:
    name: Shell Scripts
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Run shellcheck
        run: |
          find scripts -type f -name "*.sh" -print0 \
            | xargs -0 shellcheck -x -S error

  hooks:
    name: Hook Tests
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Test hooks
        run: pip install pytest && python3 -m pytest -q base/hooks/

  yaml:
    name: YAML Lint
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Install yamllint
        run: python -m pip install --upgrade yamllint==1.38.0
      - name: Lint composition YAML
        run: yamllint -c .yamllint.yml compositions

  go:
    name: Go Checks
    if: github.event_name == 'pull_request'
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Test
        run: make test
      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.8.0
      - name: Build
        run: make build

  merge_gate:
    name: merge-gate
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 5
    if: ${{ github.event_name == 'pull_request' && always() }}
    needs: [shellcheck, hooks, yaml, go]
    steps:
      - name: Require green merge inputs
        env:
          SHELLCHECK_RESULT: ${{ needs.shellcheck.result }}
          HOOKS_RESULT: ${{ needs.hooks.result }}
          YAML_RESULT: ${{ needs.yaml.result }}
          GO_RESULT: ${{ needs.go.result }}
        run: |
          set -euo pipefail
          for result in "$SHELLCHECK_RESULT" "$HOOKS_RESULT" "$YAML_RESULT" "$GO_RESULT"; do
            if [ "$result" != "success" ]; then
              echo "merge-gate blocked by dependency result: $result"
              exit 1
            fi
          done
