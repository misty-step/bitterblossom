agent = "bb-builder-rust"
substrate = "sprites"
required_artifacts = ["REPORT.json"]

[workspace]
host = "misty-step/lane-1"

[budget]
timeout_minutes = 90
max_runs_per_day = 5
# Calibrated from 074/075 dogfood: OMP/GLM dry-runs now land below $1, but
# successful live authoring has reached $3.21. Keep runaway protection while
# avoiding routine parking after expected successful builds.
max_cost_per_run_usd = 4.0
turn_cap = 80

[[trigger]]
kind = "manual"
