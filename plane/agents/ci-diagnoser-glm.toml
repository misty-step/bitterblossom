# CI failure diagnoser GLM-family variant. GLM 5.2 is present in the
# OpenRouter API catalog as of 2026-06-16; use it for runnable cohorts.
version = 1
role = "diagnoser"
harness = "pi"
model = "z-ai/glm-5.2"
secrets = ["OPENROUTER_API_KEY", "GH_TOKEN"]
skills = [
  "harness-kit/diagnose#ci-failure",
  "harness-kit/ci#failure-triage",
  "bitterblossom/operator-min",
]
