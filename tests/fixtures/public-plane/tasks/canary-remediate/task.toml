agent = "canary-remediator"
substrate = "sprites"
required_artifacts = ["REPORT.json"]

[rollout]
authority = "PR-only"
scorecard = "docs/rollout-scorecards.md#canary-remediate-pr-only-backlog-115"

[workspace]
host = "example-org/lane-1"
repos = [
  { url = "https://github.com/example-org/canary-example.git", ref = "main" },
]

[budget]
timeout_minutes = 30
max_runs_per_day = 3
max_cost_per_run_usd = 1.25
turn_cap = 60

[[trigger]]
kind = "manual"
